// Code generated by SQLBoiler 4.15.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// TermsOfService is an object representing the database table.
type TermsOfService struct {
	ID        string `boil:"id" json:"id" toml:"id" yaml:"id"`
	CreatedAt int64  `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UserID    string `boil:"user_id" json:"user_id" toml:"user_id" yaml:"user_id"`
	Text      string `boil:"text" json:"text" toml:"text" yaml:"text"`

	R *termsOfServiceR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L termsOfServiceL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var TermsOfServiceColumns = struct {
	ID        string
	CreatedAt string
	UserID    string
	Text      string
}{
	ID:        "id",
	CreatedAt: "created_at",
	UserID:    "user_id",
	Text:      "text",
}

var TermsOfServiceTableColumns = struct {
	ID        string
	CreatedAt string
	UserID    string
	Text      string
}{
	ID:        "terms_of_services.id",
	CreatedAt: "terms_of_services.created_at",
	UserID:    "terms_of_services.user_id",
	Text:      "terms_of_services.text",
}

// Generated where

var TermsOfServiceWhere = struct {
	ID        whereHelperstring
	CreatedAt whereHelperint64
	UserID    whereHelperstring
	Text      whereHelperstring
}{
	ID:        whereHelperstring{field: "\"terms_of_services\".\"id\""},
	CreatedAt: whereHelperint64{field: "\"terms_of_services\".\"created_at\""},
	UserID:    whereHelperstring{field: "\"terms_of_services\".\"user_id\""},
	Text:      whereHelperstring{field: "\"terms_of_services\".\"text\""},
}

// TermsOfServiceRels is where relationship names are stored.
var TermsOfServiceRels = struct {
}{}

// termsOfServiceR is where relationships are stored.
type termsOfServiceR struct {
}

// NewStruct creates a new relationship struct
func (*termsOfServiceR) NewStruct() *termsOfServiceR {
	return &termsOfServiceR{}
}

// termsOfServiceL is where Load methods for each relationship are stored.
type termsOfServiceL struct{}

var (
	termsOfServiceAllColumns            = []string{"id", "created_at", "user_id", "text"}
	termsOfServiceColumnsWithoutDefault = []string{"created_at", "user_id", "text"}
	termsOfServiceColumnsWithDefault    = []string{"id"}
	termsOfServicePrimaryKeyColumns     = []string{"id"}
	termsOfServiceGeneratedColumns      = []string{}
)

type (
	// TermsOfServiceSlice is an alias for a slice of pointers to TermsOfService.
	// This should almost always be used instead of []TermsOfService.
	TermsOfServiceSlice []*TermsOfService

	termsOfServiceQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	termsOfServiceType                 = reflect.TypeOf(&TermsOfService{})
	termsOfServiceMapping              = queries.MakeStructMapping(termsOfServiceType)
	termsOfServicePrimaryKeyMapping, _ = queries.BindMapping(termsOfServiceType, termsOfServiceMapping, termsOfServicePrimaryKeyColumns)
	termsOfServiceInsertCacheMut       sync.RWMutex
	termsOfServiceInsertCache          = make(map[string]insertCache)
	termsOfServiceUpdateCacheMut       sync.RWMutex
	termsOfServiceUpdateCache          = make(map[string]updateCache)
	termsOfServiceUpsertCacheMut       sync.RWMutex
	termsOfServiceUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// One returns a single termsOfService record from the query.
func (q termsOfServiceQuery) One(ctx context.Context, exec boil.ContextExecutor) (*TermsOfService, error) {
	o := &TermsOfService{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for terms_of_services")
	}

	return o, nil
}

// All returns all TermsOfService records from the query.
func (q termsOfServiceQuery) All(ctx context.Context, exec boil.ContextExecutor) (TermsOfServiceSlice, error) {
	var o []*TermsOfService

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to TermsOfService slice")
	}

	return o, nil
}

// Count returns the count of all TermsOfService records in the query.
func (q termsOfServiceQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count terms_of_services rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q termsOfServiceQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if terms_of_services exists")
	}

	return count > 0, nil
}

// TermsOfServices retrieves all the records using an executor.
func TermsOfServices(mods ...qm.QueryMod) termsOfServiceQuery {
	mods = append(mods, qm.From("\"terms_of_services\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"terms_of_services\".*"})
	}

	return termsOfServiceQuery{q}
}

// FindTermsOfService retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindTermsOfService(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*TermsOfService, error) {
	termsOfServiceObj := &TermsOfService{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"terms_of_services\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, termsOfServiceObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from terms_of_services")
	}

	return termsOfServiceObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *TermsOfService) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no terms_of_services provided for insertion")
	}

	var err error

	nzDefaults := queries.NonZeroDefaultSet(termsOfServiceColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	termsOfServiceInsertCacheMut.RLock()
	cache, cached := termsOfServiceInsertCache[key]
	termsOfServiceInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			termsOfServiceAllColumns,
			termsOfServiceColumnsWithDefault,
			termsOfServiceColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(termsOfServiceType, termsOfServiceMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(termsOfServiceType, termsOfServiceMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"terms_of_services\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"terms_of_services\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into terms_of_services")
	}

	if !cached {
		termsOfServiceInsertCacheMut.Lock()
		termsOfServiceInsertCache[key] = cache
		termsOfServiceInsertCacheMut.Unlock()
	}

	return nil
}

// Update uses an executor to update the TermsOfService.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *TermsOfService) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	key := makeCacheKey(columns, nil)
	termsOfServiceUpdateCacheMut.RLock()
	cache, cached := termsOfServiceUpdateCache[key]
	termsOfServiceUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			termsOfServiceAllColumns,
			termsOfServicePrimaryKeyColumns,
		)
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update terms_of_services, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"terms_of_services\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, termsOfServicePrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(termsOfServiceType, termsOfServiceMapping, append(wl, termsOfServicePrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update terms_of_services row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for terms_of_services")
	}

	if !cached {
		termsOfServiceUpdateCacheMut.Lock()
		termsOfServiceUpdateCache[key] = cache
		termsOfServiceUpdateCacheMut.Unlock()
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values.
func (q termsOfServiceQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for terms_of_services")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for terms_of_services")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o TermsOfServiceSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), termsOfServicePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"terms_of_services\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, termsOfServicePrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in termsOfService slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all termsOfService")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *TermsOfService) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no terms_of_services provided for upsert")
	}

	nzDefaults := queries.NonZeroDefaultSet(termsOfServiceColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	termsOfServiceUpsertCacheMut.RLock()
	cache, cached := termsOfServiceUpsertCache[key]
	termsOfServiceUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			termsOfServiceAllColumns,
			termsOfServiceColumnsWithDefault,
			termsOfServiceColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			termsOfServiceAllColumns,
			termsOfServicePrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("models: unable to upsert terms_of_services, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(termsOfServicePrimaryKeyColumns))
			copy(conflict, termsOfServicePrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"terms_of_services\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(termsOfServiceType, termsOfServiceMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(termsOfServiceType, termsOfServiceMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert terms_of_services")
	}

	if !cached {
		termsOfServiceUpsertCacheMut.Lock()
		termsOfServiceUpsertCache[key] = cache
		termsOfServiceUpsertCacheMut.Unlock()
	}

	return nil
}

// Delete deletes a single TermsOfService record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *TermsOfService) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no TermsOfService provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), termsOfServicePrimaryKeyMapping)
	sql := "DELETE FROM \"terms_of_services\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from terms_of_services")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for terms_of_services")
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q termsOfServiceQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no termsOfServiceQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from terms_of_services")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for terms_of_services")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o TermsOfServiceSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), termsOfServicePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"terms_of_services\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, termsOfServicePrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from termsOfService slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for terms_of_services")
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *TermsOfService) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindTermsOfService(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *TermsOfServiceSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := TermsOfServiceSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), termsOfServicePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"terms_of_services\".* FROM \"terms_of_services\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, termsOfServicePrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in TermsOfServiceSlice")
	}

	*o = slice

	return nil
}

// TermsOfServiceExists checks if the TermsOfService row exists.
func TermsOfServiceExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"terms_of_services\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if terms_of_services exists")
	}

	return exists, nil
}

// Exists checks if the TermsOfService row exists.
func (o *TermsOfService) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return TermsOfServiceExists(ctx, exec, o.ID)
}
