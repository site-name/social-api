// Code generated by SQLBoiler 4.15.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// SaleProduct is an object representing the database table.
type SaleProduct struct {
	ID        string      `boil:"id" json:"id" toml:"id" yaml:"id"`
	Saleid    null.String `boil:"saleid" json:"saleid,omitempty" toml:"saleid" yaml:"saleid,omitempty"`
	Productid null.String `boil:"productid" json:"productid,omitempty" toml:"productid" yaml:"productid,omitempty"`
	Createat  null.Int64  `boil:"createat" json:"createat,omitempty" toml:"createat" yaml:"createat,omitempty"`

	R *saleProductR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L saleProductL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var SaleProductColumns = struct {
	ID        string
	Saleid    string
	Productid string
	Createat  string
}{
	ID:        "id",
	Saleid:    "saleid",
	Productid: "productid",
	Createat:  "createat",
}

var SaleProductTableColumns = struct {
	ID        string
	Saleid    string
	Productid string
	Createat  string
}{
	ID:        "sale_products.id",
	Saleid:    "sale_products.saleid",
	Productid: "sale_products.productid",
	Createat:  "sale_products.createat",
}

// Generated where

var SaleProductWhere = struct {
	ID        whereHelperstring
	Saleid    whereHelpernull_String
	Productid whereHelpernull_String
	Createat  whereHelpernull_Int64
}{
	ID:        whereHelperstring{field: "\"sale_products\".\"id\""},
	Saleid:    whereHelpernull_String{field: "\"sale_products\".\"saleid\""},
	Productid: whereHelpernull_String{field: "\"sale_products\".\"productid\""},
	Createat:  whereHelpernull_Int64{field: "\"sale_products\".\"createat\""},
}

// SaleProductRels is where relationship names are stored.
var SaleProductRels = struct {
	ProductidProduct string
	SaleidSale       string
}{
	ProductidProduct: "ProductidProduct",
	SaleidSale:       "SaleidSale",
}

// saleProductR is where relationships are stored.
type saleProductR struct {
	ProductidProduct *Product `boil:"ProductidProduct" json:"ProductidProduct" toml:"ProductidProduct" yaml:"ProductidProduct"`
	SaleidSale       *Sale    `boil:"SaleidSale" json:"SaleidSale" toml:"SaleidSale" yaml:"SaleidSale"`
}

// NewStruct creates a new relationship struct
func (*saleProductR) NewStruct() *saleProductR {
	return &saleProductR{}
}

func (r *saleProductR) GetProductidProduct() *Product {
	if r == nil {
		return nil
	}
	return r.ProductidProduct
}

func (r *saleProductR) GetSaleidSale() *Sale {
	if r == nil {
		return nil
	}
	return r.SaleidSale
}

// saleProductL is where Load methods for each relationship are stored.
type saleProductL struct{}

var (
	saleProductAllColumns            = []string{"id", "saleid", "productid", "createat"}
	saleProductColumnsWithoutDefault = []string{"id"}
	saleProductColumnsWithDefault    = []string{"saleid", "productid", "createat"}
	saleProductPrimaryKeyColumns     = []string{"id"}
	saleProductGeneratedColumns      = []string{}
)

type (
	// SaleProductSlice is an alias for a slice of pointers to SaleProduct.
	// This should almost always be used instead of []SaleProduct.
	SaleProductSlice []*SaleProduct
	// SaleProductHook is the signature for custom SaleProduct hook methods
	SaleProductHook func(context.Context, boil.ContextExecutor, *SaleProduct) error

	saleProductQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	saleProductType                 = reflect.TypeOf(&SaleProduct{})
	saleProductMapping              = queries.MakeStructMapping(saleProductType)
	saleProductPrimaryKeyMapping, _ = queries.BindMapping(saleProductType, saleProductMapping, saleProductPrimaryKeyColumns)
	saleProductInsertCacheMut       sync.RWMutex
	saleProductInsertCache          = make(map[string]insertCache)
	saleProductUpdateCacheMut       sync.RWMutex
	saleProductUpdateCache          = make(map[string]updateCache)
	saleProductUpsertCacheMut       sync.RWMutex
	saleProductUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var saleProductAfterSelectHooks []SaleProductHook

var saleProductBeforeInsertHooks []SaleProductHook
var saleProductAfterInsertHooks []SaleProductHook

var saleProductBeforeUpdateHooks []SaleProductHook
var saleProductAfterUpdateHooks []SaleProductHook

var saleProductBeforeDeleteHooks []SaleProductHook
var saleProductAfterDeleteHooks []SaleProductHook

var saleProductBeforeUpsertHooks []SaleProductHook
var saleProductAfterUpsertHooks []SaleProductHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *SaleProduct) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range saleProductAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *SaleProduct) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range saleProductBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *SaleProduct) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range saleProductAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *SaleProduct) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range saleProductBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *SaleProduct) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range saleProductAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *SaleProduct) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range saleProductBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *SaleProduct) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range saleProductAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *SaleProduct) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range saleProductBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *SaleProduct) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range saleProductAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddSaleProductHook registers your hook function for all future operations.
func AddSaleProductHook(hookPoint boil.HookPoint, saleProductHook SaleProductHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		saleProductAfterSelectHooks = append(saleProductAfterSelectHooks, saleProductHook)
	case boil.BeforeInsertHook:
		saleProductBeforeInsertHooks = append(saleProductBeforeInsertHooks, saleProductHook)
	case boil.AfterInsertHook:
		saleProductAfterInsertHooks = append(saleProductAfterInsertHooks, saleProductHook)
	case boil.BeforeUpdateHook:
		saleProductBeforeUpdateHooks = append(saleProductBeforeUpdateHooks, saleProductHook)
	case boil.AfterUpdateHook:
		saleProductAfterUpdateHooks = append(saleProductAfterUpdateHooks, saleProductHook)
	case boil.BeforeDeleteHook:
		saleProductBeforeDeleteHooks = append(saleProductBeforeDeleteHooks, saleProductHook)
	case boil.AfterDeleteHook:
		saleProductAfterDeleteHooks = append(saleProductAfterDeleteHooks, saleProductHook)
	case boil.BeforeUpsertHook:
		saleProductBeforeUpsertHooks = append(saleProductBeforeUpsertHooks, saleProductHook)
	case boil.AfterUpsertHook:
		saleProductAfterUpsertHooks = append(saleProductAfterUpsertHooks, saleProductHook)
	}
}

// One returns a single saleProduct record from the query.
func (q saleProductQuery) One(ctx context.Context, exec boil.ContextExecutor) (*SaleProduct, error) {
	o := &SaleProduct{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for sale_products")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all SaleProduct records from the query.
func (q saleProductQuery) All(ctx context.Context, exec boil.ContextExecutor) (SaleProductSlice, error) {
	var o []*SaleProduct

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to SaleProduct slice")
	}

	if len(saleProductAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all SaleProduct records in the query.
func (q saleProductQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count sale_products rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q saleProductQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if sale_products exists")
	}

	return count > 0, nil
}

// ProductidProduct pointed to by the foreign key.
func (o *SaleProduct) ProductidProduct(mods ...qm.QueryMod) productQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.Productid),
	}

	queryMods = append(queryMods, mods...)

	return Products(queryMods...)
}

// SaleidSale pointed to by the foreign key.
func (o *SaleProduct) SaleidSale(mods ...qm.QueryMod) saleQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.Saleid),
	}

	queryMods = append(queryMods, mods...)

	return Sales(queryMods...)
}

// LoadProductidProduct allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (saleProductL) LoadProductidProduct(ctx context.Context, e boil.ContextExecutor, singular bool, maybeSaleProduct interface{}, mods queries.Applicator) error {
	var slice []*SaleProduct
	var object *SaleProduct

	if singular {
		var ok bool
		object, ok = maybeSaleProduct.(*SaleProduct)
		if !ok {
			object = new(SaleProduct)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeSaleProduct)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeSaleProduct))
			}
		}
	} else {
		s, ok := maybeSaleProduct.(*[]*SaleProduct)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeSaleProduct)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeSaleProduct))
			}
		}
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &saleProductR{}
		}
		if !queries.IsNil(object.Productid) {
			args = append(args, object.Productid)
		}

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &saleProductR{}
			}

			for _, a := range args {
				if queries.Equal(a, obj.Productid) {
					continue Outer
				}
			}

			if !queries.IsNil(obj.Productid) {
				args = append(args, obj.Productid)
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`products`),
		qm.WhereIn(`products.id in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Product")
	}

	var resultSlice []*Product
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Product")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for products")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for products")
	}

	if len(productAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.ProductidProduct = foreign
		if foreign.R == nil {
			foreign.R = &productR{}
		}
		foreign.R.ProductidSaleProducts = append(foreign.R.ProductidSaleProducts, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.Productid, foreign.ID) {
				local.R.ProductidProduct = foreign
				if foreign.R == nil {
					foreign.R = &productR{}
				}
				foreign.R.ProductidSaleProducts = append(foreign.R.ProductidSaleProducts, local)
				break
			}
		}
	}

	return nil
}

// LoadSaleidSale allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (saleProductL) LoadSaleidSale(ctx context.Context, e boil.ContextExecutor, singular bool, maybeSaleProduct interface{}, mods queries.Applicator) error {
	var slice []*SaleProduct
	var object *SaleProduct

	if singular {
		var ok bool
		object, ok = maybeSaleProduct.(*SaleProduct)
		if !ok {
			object = new(SaleProduct)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeSaleProduct)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeSaleProduct))
			}
		}
	} else {
		s, ok := maybeSaleProduct.(*[]*SaleProduct)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeSaleProduct)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeSaleProduct))
			}
		}
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &saleProductR{}
		}
		if !queries.IsNil(object.Saleid) {
			args = append(args, object.Saleid)
		}

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &saleProductR{}
			}

			for _, a := range args {
				if queries.Equal(a, obj.Saleid) {
					continue Outer
				}
			}

			if !queries.IsNil(obj.Saleid) {
				args = append(args, obj.Saleid)
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`sales`),
		qm.WhereIn(`sales.id in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Sale")
	}

	var resultSlice []*Sale
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Sale")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for sales")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for sales")
	}

	if len(saleAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.SaleidSale = foreign
		if foreign.R == nil {
			foreign.R = &saleR{}
		}
		foreign.R.SaleidSaleProducts = append(foreign.R.SaleidSaleProducts, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.Saleid, foreign.ID) {
				local.R.SaleidSale = foreign
				if foreign.R == nil {
					foreign.R = &saleR{}
				}
				foreign.R.SaleidSaleProducts = append(foreign.R.SaleidSaleProducts, local)
				break
			}
		}
	}

	return nil
}

// SetProductidProduct of the saleProduct to the related item.
// Sets o.R.ProductidProduct to related.
// Adds o to related.R.ProductidSaleProducts.
func (o *SaleProduct) SetProductidProduct(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Product) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"sale_products\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"productid"}),
		strmangle.WhereClause("\"", "\"", 2, saleProductPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.Productid, related.ID)
	if o.R == nil {
		o.R = &saleProductR{
			ProductidProduct: related,
		}
	} else {
		o.R.ProductidProduct = related
	}

	if related.R == nil {
		related.R = &productR{
			ProductidSaleProducts: SaleProductSlice{o},
		}
	} else {
		related.R.ProductidSaleProducts = append(related.R.ProductidSaleProducts, o)
	}

	return nil
}

// RemoveProductidProduct relationship.
// Sets o.R.ProductidProduct to nil.
// Removes o from all passed in related items' relationships struct.
func (o *SaleProduct) RemoveProductidProduct(ctx context.Context, exec boil.ContextExecutor, related *Product) error {
	var err error

	queries.SetScanner(&o.Productid, nil)
	if _, err = o.Update(ctx, exec, boil.Whitelist("productid")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	if o.R != nil {
		o.R.ProductidProduct = nil
	}
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.ProductidSaleProducts {
		if queries.Equal(o.Productid, ri.Productid) {
			continue
		}

		ln := len(related.R.ProductidSaleProducts)
		if ln > 1 && i < ln-1 {
			related.R.ProductidSaleProducts[i] = related.R.ProductidSaleProducts[ln-1]
		}
		related.R.ProductidSaleProducts = related.R.ProductidSaleProducts[:ln-1]
		break
	}
	return nil
}

// SetSaleidSale of the saleProduct to the related item.
// Sets o.R.SaleidSale to related.
// Adds o to related.R.SaleidSaleProducts.
func (o *SaleProduct) SetSaleidSale(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Sale) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"sale_products\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"saleid"}),
		strmangle.WhereClause("\"", "\"", 2, saleProductPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.Saleid, related.ID)
	if o.R == nil {
		o.R = &saleProductR{
			SaleidSale: related,
		}
	} else {
		o.R.SaleidSale = related
	}

	if related.R == nil {
		related.R = &saleR{
			SaleidSaleProducts: SaleProductSlice{o},
		}
	} else {
		related.R.SaleidSaleProducts = append(related.R.SaleidSaleProducts, o)
	}

	return nil
}

// RemoveSaleidSale relationship.
// Sets o.R.SaleidSale to nil.
// Removes o from all passed in related items' relationships struct.
func (o *SaleProduct) RemoveSaleidSale(ctx context.Context, exec boil.ContextExecutor, related *Sale) error {
	var err error

	queries.SetScanner(&o.Saleid, nil)
	if _, err = o.Update(ctx, exec, boil.Whitelist("saleid")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	if o.R != nil {
		o.R.SaleidSale = nil
	}
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.SaleidSaleProducts {
		if queries.Equal(o.Saleid, ri.Saleid) {
			continue
		}

		ln := len(related.R.SaleidSaleProducts)
		if ln > 1 && i < ln-1 {
			related.R.SaleidSaleProducts[i] = related.R.SaleidSaleProducts[ln-1]
		}
		related.R.SaleidSaleProducts = related.R.SaleidSaleProducts[:ln-1]
		break
	}
	return nil
}

// SaleProducts retrieves all the records using an executor.
func SaleProducts(mods ...qm.QueryMod) saleProductQuery {
	mods = append(mods, qm.From("\"sale_products\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"sale_products\".*"})
	}

	return saleProductQuery{q}
}

// FindSaleProduct retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindSaleProduct(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*SaleProduct, error) {
	saleProductObj := &SaleProduct{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"sale_products\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, saleProductObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from sale_products")
	}

	if err = saleProductObj.doAfterSelectHooks(ctx, exec); err != nil {
		return saleProductObj, err
	}

	return saleProductObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *SaleProduct) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no sale_products provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(saleProductColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	saleProductInsertCacheMut.RLock()
	cache, cached := saleProductInsertCache[key]
	saleProductInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			saleProductAllColumns,
			saleProductColumnsWithDefault,
			saleProductColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(saleProductType, saleProductMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(saleProductType, saleProductMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"sale_products\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"sale_products\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into sale_products")
	}

	if !cached {
		saleProductInsertCacheMut.Lock()
		saleProductInsertCache[key] = cache
		saleProductInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the SaleProduct.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *SaleProduct) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	saleProductUpdateCacheMut.RLock()
	cache, cached := saleProductUpdateCache[key]
	saleProductUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			saleProductAllColumns,
			saleProductPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update sale_products, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"sale_products\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, saleProductPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(saleProductType, saleProductMapping, append(wl, saleProductPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update sale_products row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for sale_products")
	}

	if !cached {
		saleProductUpdateCacheMut.Lock()
		saleProductUpdateCache[key] = cache
		saleProductUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q saleProductQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for sale_products")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for sale_products")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o SaleProductSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), saleProductPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"sale_products\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, saleProductPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in saleProduct slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all saleProduct")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *SaleProduct) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no sale_products provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(saleProductColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	saleProductUpsertCacheMut.RLock()
	cache, cached := saleProductUpsertCache[key]
	saleProductUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			saleProductAllColumns,
			saleProductColumnsWithDefault,
			saleProductColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			saleProductAllColumns,
			saleProductPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("models: unable to upsert sale_products, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(saleProductPrimaryKeyColumns))
			copy(conflict, saleProductPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"sale_products\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(saleProductType, saleProductMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(saleProductType, saleProductMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert sale_products")
	}

	if !cached {
		saleProductUpsertCacheMut.Lock()
		saleProductUpsertCache[key] = cache
		saleProductUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single SaleProduct record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *SaleProduct) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no SaleProduct provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), saleProductPrimaryKeyMapping)
	sql := "DELETE FROM \"sale_products\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from sale_products")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for sale_products")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q saleProductQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no saleProductQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from sale_products")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for sale_products")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o SaleProductSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(saleProductBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), saleProductPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"sale_products\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, saleProductPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from saleProduct slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for sale_products")
	}

	if len(saleProductAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *SaleProduct) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindSaleProduct(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *SaleProductSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := SaleProductSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), saleProductPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"sale_products\".* FROM \"sale_products\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, saleProductPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in SaleProductSlice")
	}

	*o = slice

	return nil
}

// SaleProductExists checks if the SaleProduct row exists.
func SaleProductExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"sale_products\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if sale_products exists")
	}

	return exists, nil
}

// Exists checks if the SaleProduct row exists.
func (o *SaleProduct) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return SaleProductExists(ctx, exec, o.ID)
}
