// Code generated by SQLBoiler 4.15.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/sqlboiler/v4/types"
	"github.com/volatiletech/strmangle"
)

// SaleChannelListing is an object representing the database table.
type SaleChannelListing struct {
	ID            string            `boil:"id" json:"id" toml:"id" yaml:"id"`
	SaleID        string            `boil:"sale_id" json:"sale_id" toml:"sale_id" yaml:"sale_id"`
	ChannelID     string            `boil:"channel_id" json:"channel_id" toml:"channel_id" yaml:"channel_id"`
	DiscountValue types.NullDecimal `boil:"discount_value" json:"discount_value,omitempty" toml:"discount_value" yaml:"discount_value,omitempty"`
	Currency      string            `boil:"currency" json:"currency" toml:"currency" yaml:"currency"`
	CreatedAt     int64             `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`

	R *saleChannelListingR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L saleChannelListingL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var SaleChannelListingColumns = struct {
	ID            string
	SaleID        string
	ChannelID     string
	DiscountValue string
	Currency      string
	CreatedAt     string
}{
	ID:            "id",
	SaleID:        "sale_id",
	ChannelID:     "channel_id",
	DiscountValue: "discount_value",
	Currency:      "currency",
	CreatedAt:     "created_at",
}

var SaleChannelListingTableColumns = struct {
	ID            string
	SaleID        string
	ChannelID     string
	DiscountValue string
	Currency      string
	CreatedAt     string
}{
	ID:            "sale_channel_listings.id",
	SaleID:        "sale_channel_listings.sale_id",
	ChannelID:     "sale_channel_listings.channel_id",
	DiscountValue: "sale_channel_listings.discount_value",
	Currency:      "sale_channel_listings.currency",
	CreatedAt:     "sale_channel_listings.created_at",
}

// Generated where

var SaleChannelListingWhere = struct {
	ID            whereHelperstring
	SaleID        whereHelperstring
	ChannelID     whereHelperstring
	DiscountValue whereHelpertypes_NullDecimal
	Currency      whereHelperstring
	CreatedAt     whereHelperint64
}{
	ID:            whereHelperstring{field: "\"sale_channel_listings\".\"id\""},
	SaleID:        whereHelperstring{field: "\"sale_channel_listings\".\"sale_id\""},
	ChannelID:     whereHelperstring{field: "\"sale_channel_listings\".\"channel_id\""},
	DiscountValue: whereHelpertypes_NullDecimal{field: "\"sale_channel_listings\".\"discount_value\""},
	Currency:      whereHelperstring{field: "\"sale_channel_listings\".\"currency\""},
	CreatedAt:     whereHelperint64{field: "\"sale_channel_listings\".\"created_at\""},
}

// SaleChannelListingRels is where relationship names are stored.
var SaleChannelListingRels = struct {
	Channel string
	Sale    string
}{
	Channel: "Channel",
	Sale:    "Sale",
}

// saleChannelListingR is where relationships are stored.
type saleChannelListingR struct {
	Channel *Channel `boil:"Channel" json:"Channel" toml:"Channel" yaml:"Channel"`
	Sale    *Sale    `boil:"Sale" json:"Sale" toml:"Sale" yaml:"Sale"`
}

// NewStruct creates a new relationship struct
func (*saleChannelListingR) NewStruct() *saleChannelListingR {
	return &saleChannelListingR{}
}

func (r *saleChannelListingR) GetChannel() *Channel {
	if r == nil {
		return nil
	}
	return r.Channel
}

func (r *saleChannelListingR) GetSale() *Sale {
	if r == nil {
		return nil
	}
	return r.Sale
}

// saleChannelListingL is where Load methods for each relationship are stored.
type saleChannelListingL struct{}

var (
	saleChannelListingAllColumns            = []string{"id", "sale_id", "channel_id", "discount_value", "currency", "created_at"}
	saleChannelListingColumnsWithoutDefault = []string{"sale_id", "channel_id", "currency", "created_at"}
	saleChannelListingColumnsWithDefault    = []string{"id", "discount_value"}
	saleChannelListingPrimaryKeyColumns     = []string{"id"}
	saleChannelListingGeneratedColumns      = []string{}
)

type (
	// SaleChannelListingSlice is an alias for a slice of pointers to SaleChannelListing.
	// This should almost always be used instead of []SaleChannelListing.
	SaleChannelListingSlice []*SaleChannelListing
	// SaleChannelListingHook is the signature for custom SaleChannelListing hook methods
	SaleChannelListingHook func(context.Context, boil.ContextExecutor, *SaleChannelListing) error

	saleChannelListingQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	saleChannelListingType                 = reflect.TypeOf(&SaleChannelListing{})
	saleChannelListingMapping              = queries.MakeStructMapping(saleChannelListingType)
	saleChannelListingPrimaryKeyMapping, _ = queries.BindMapping(saleChannelListingType, saleChannelListingMapping, saleChannelListingPrimaryKeyColumns)
	saleChannelListingInsertCacheMut       sync.RWMutex
	saleChannelListingInsertCache          = make(map[string]insertCache)
	saleChannelListingUpdateCacheMut       sync.RWMutex
	saleChannelListingUpdateCache          = make(map[string]updateCache)
	saleChannelListingUpsertCacheMut       sync.RWMutex
	saleChannelListingUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var saleChannelListingAfterSelectHooks []SaleChannelListingHook

var saleChannelListingBeforeInsertHooks []SaleChannelListingHook
var saleChannelListingAfterInsertHooks []SaleChannelListingHook

var saleChannelListingBeforeUpdateHooks []SaleChannelListingHook
var saleChannelListingAfterUpdateHooks []SaleChannelListingHook

var saleChannelListingBeforeDeleteHooks []SaleChannelListingHook
var saleChannelListingAfterDeleteHooks []SaleChannelListingHook

var saleChannelListingBeforeUpsertHooks []SaleChannelListingHook
var saleChannelListingAfterUpsertHooks []SaleChannelListingHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *SaleChannelListing) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range saleChannelListingAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *SaleChannelListing) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range saleChannelListingBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *SaleChannelListing) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range saleChannelListingAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *SaleChannelListing) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range saleChannelListingBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *SaleChannelListing) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range saleChannelListingAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *SaleChannelListing) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range saleChannelListingBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *SaleChannelListing) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range saleChannelListingAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *SaleChannelListing) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range saleChannelListingBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *SaleChannelListing) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range saleChannelListingAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddSaleChannelListingHook registers your hook function for all future operations.
func AddSaleChannelListingHook(hookPoint boil.HookPoint, saleChannelListingHook SaleChannelListingHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		saleChannelListingAfterSelectHooks = append(saleChannelListingAfterSelectHooks, saleChannelListingHook)
	case boil.BeforeInsertHook:
		saleChannelListingBeforeInsertHooks = append(saleChannelListingBeforeInsertHooks, saleChannelListingHook)
	case boil.AfterInsertHook:
		saleChannelListingAfterInsertHooks = append(saleChannelListingAfterInsertHooks, saleChannelListingHook)
	case boil.BeforeUpdateHook:
		saleChannelListingBeforeUpdateHooks = append(saleChannelListingBeforeUpdateHooks, saleChannelListingHook)
	case boil.AfterUpdateHook:
		saleChannelListingAfterUpdateHooks = append(saleChannelListingAfterUpdateHooks, saleChannelListingHook)
	case boil.BeforeDeleteHook:
		saleChannelListingBeforeDeleteHooks = append(saleChannelListingBeforeDeleteHooks, saleChannelListingHook)
	case boil.AfterDeleteHook:
		saleChannelListingAfterDeleteHooks = append(saleChannelListingAfterDeleteHooks, saleChannelListingHook)
	case boil.BeforeUpsertHook:
		saleChannelListingBeforeUpsertHooks = append(saleChannelListingBeforeUpsertHooks, saleChannelListingHook)
	case boil.AfterUpsertHook:
		saleChannelListingAfterUpsertHooks = append(saleChannelListingAfterUpsertHooks, saleChannelListingHook)
	}
}

// One returns a single saleChannelListing record from the query.
func (q saleChannelListingQuery) One(ctx context.Context, exec boil.ContextExecutor) (*SaleChannelListing, error) {
	o := &SaleChannelListing{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for sale_channel_listings")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all SaleChannelListing records from the query.
func (q saleChannelListingQuery) All(ctx context.Context, exec boil.ContextExecutor) (SaleChannelListingSlice, error) {
	var o []*SaleChannelListing

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to SaleChannelListing slice")
	}

	if len(saleChannelListingAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all SaleChannelListing records in the query.
func (q saleChannelListingQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count sale_channel_listings rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q saleChannelListingQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if sale_channel_listings exists")
	}

	return count > 0, nil
}

// Channel pointed to by the foreign key.
func (o *SaleChannelListing) Channel(mods ...qm.QueryMod) channelQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.ChannelID),
	}

	queryMods = append(queryMods, mods...)

	return Channels(queryMods...)
}

// Sale pointed to by the foreign key.
func (o *SaleChannelListing) Sale(mods ...qm.QueryMod) saleQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.SaleID),
	}

	queryMods = append(queryMods, mods...)

	return Sales(queryMods...)
}

// LoadChannel allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (saleChannelListingL) LoadChannel(ctx context.Context, e boil.ContextExecutor, singular bool, maybeSaleChannelListing interface{}, mods queries.Applicator) error {
	var slice []*SaleChannelListing
	var object *SaleChannelListing

	if singular {
		var ok bool
		object, ok = maybeSaleChannelListing.(*SaleChannelListing)
		if !ok {
			object = new(SaleChannelListing)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeSaleChannelListing)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeSaleChannelListing))
			}
		}
	} else {
		s, ok := maybeSaleChannelListing.(*[]*SaleChannelListing)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeSaleChannelListing)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeSaleChannelListing))
			}
		}
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &saleChannelListingR{}
		}
		args = append(args, object.ChannelID)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &saleChannelListingR{}
			}

			for _, a := range args {
				if a == obj.ChannelID {
					continue Outer
				}
			}

			args = append(args, obj.ChannelID)

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`channels`),
		qm.WhereIn(`channels.id in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Channel")
	}

	var resultSlice []*Channel
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Channel")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for channels")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for channels")
	}

	if len(channelAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Channel = foreign
		if foreign.R == nil {
			foreign.R = &channelR{}
		}
		foreign.R.SaleChannelListings = append(foreign.R.SaleChannelListings, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.ChannelID == foreign.ID {
				local.R.Channel = foreign
				if foreign.R == nil {
					foreign.R = &channelR{}
				}
				foreign.R.SaleChannelListings = append(foreign.R.SaleChannelListings, local)
				break
			}
		}
	}

	return nil
}

// LoadSale allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (saleChannelListingL) LoadSale(ctx context.Context, e boil.ContextExecutor, singular bool, maybeSaleChannelListing interface{}, mods queries.Applicator) error {
	var slice []*SaleChannelListing
	var object *SaleChannelListing

	if singular {
		var ok bool
		object, ok = maybeSaleChannelListing.(*SaleChannelListing)
		if !ok {
			object = new(SaleChannelListing)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeSaleChannelListing)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeSaleChannelListing))
			}
		}
	} else {
		s, ok := maybeSaleChannelListing.(*[]*SaleChannelListing)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeSaleChannelListing)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeSaleChannelListing))
			}
		}
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &saleChannelListingR{}
		}
		args = append(args, object.SaleID)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &saleChannelListingR{}
			}

			for _, a := range args {
				if a == obj.SaleID {
					continue Outer
				}
			}

			args = append(args, obj.SaleID)

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`sales`),
		qm.WhereIn(`sales.id in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Sale")
	}

	var resultSlice []*Sale
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Sale")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for sales")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for sales")
	}

	if len(saleAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Sale = foreign
		if foreign.R == nil {
			foreign.R = &saleR{}
		}
		foreign.R.SaleChannelListings = append(foreign.R.SaleChannelListings, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.SaleID == foreign.ID {
				local.R.Sale = foreign
				if foreign.R == nil {
					foreign.R = &saleR{}
				}
				foreign.R.SaleChannelListings = append(foreign.R.SaleChannelListings, local)
				break
			}
		}
	}

	return nil
}

// SetChannel of the saleChannelListing to the related item.
// Sets o.R.Channel to related.
// Adds o to related.R.SaleChannelListings.
func (o *SaleChannelListing) SetChannel(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Channel) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"sale_channel_listings\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"channel_id"}),
		strmangle.WhereClause("\"", "\"", 2, saleChannelListingPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.ChannelID = related.ID
	if o.R == nil {
		o.R = &saleChannelListingR{
			Channel: related,
		}
	} else {
		o.R.Channel = related
	}

	if related.R == nil {
		related.R = &channelR{
			SaleChannelListings: SaleChannelListingSlice{o},
		}
	} else {
		related.R.SaleChannelListings = append(related.R.SaleChannelListings, o)
	}

	return nil
}

// SetSale of the saleChannelListing to the related item.
// Sets o.R.Sale to related.
// Adds o to related.R.SaleChannelListings.
func (o *SaleChannelListing) SetSale(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Sale) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"sale_channel_listings\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"sale_id"}),
		strmangle.WhereClause("\"", "\"", 2, saleChannelListingPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.SaleID = related.ID
	if o.R == nil {
		o.R = &saleChannelListingR{
			Sale: related,
		}
	} else {
		o.R.Sale = related
	}

	if related.R == nil {
		related.R = &saleR{
			SaleChannelListings: SaleChannelListingSlice{o},
		}
	} else {
		related.R.SaleChannelListings = append(related.R.SaleChannelListings, o)
	}

	return nil
}

// SaleChannelListings retrieves all the records using an executor.
func SaleChannelListings(mods ...qm.QueryMod) saleChannelListingQuery {
	mods = append(mods, qm.From("\"sale_channel_listings\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"sale_channel_listings\".*"})
	}

	return saleChannelListingQuery{q}
}

// FindSaleChannelListing retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindSaleChannelListing(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*SaleChannelListing, error) {
	saleChannelListingObj := &SaleChannelListing{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"sale_channel_listings\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, saleChannelListingObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from sale_channel_listings")
	}

	if err = saleChannelListingObj.doAfterSelectHooks(ctx, exec); err != nil {
		return saleChannelListingObj, err
	}

	return saleChannelListingObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *SaleChannelListing) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no sale_channel_listings provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(saleChannelListingColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	saleChannelListingInsertCacheMut.RLock()
	cache, cached := saleChannelListingInsertCache[key]
	saleChannelListingInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			saleChannelListingAllColumns,
			saleChannelListingColumnsWithDefault,
			saleChannelListingColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(saleChannelListingType, saleChannelListingMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(saleChannelListingType, saleChannelListingMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"sale_channel_listings\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"sale_channel_listings\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into sale_channel_listings")
	}

	if !cached {
		saleChannelListingInsertCacheMut.Lock()
		saleChannelListingInsertCache[key] = cache
		saleChannelListingInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the SaleChannelListing.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *SaleChannelListing) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	saleChannelListingUpdateCacheMut.RLock()
	cache, cached := saleChannelListingUpdateCache[key]
	saleChannelListingUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			saleChannelListingAllColumns,
			saleChannelListingPrimaryKeyColumns,
		)
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update sale_channel_listings, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"sale_channel_listings\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, saleChannelListingPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(saleChannelListingType, saleChannelListingMapping, append(wl, saleChannelListingPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update sale_channel_listings row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for sale_channel_listings")
	}

	if !cached {
		saleChannelListingUpdateCacheMut.Lock()
		saleChannelListingUpdateCache[key] = cache
		saleChannelListingUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q saleChannelListingQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for sale_channel_listings")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for sale_channel_listings")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o SaleChannelListingSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), saleChannelListingPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"sale_channel_listings\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, saleChannelListingPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in saleChannelListing slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all saleChannelListing")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *SaleChannelListing) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no sale_channel_listings provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(saleChannelListingColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	saleChannelListingUpsertCacheMut.RLock()
	cache, cached := saleChannelListingUpsertCache[key]
	saleChannelListingUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			saleChannelListingAllColumns,
			saleChannelListingColumnsWithDefault,
			saleChannelListingColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			saleChannelListingAllColumns,
			saleChannelListingPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("models: unable to upsert sale_channel_listings, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(saleChannelListingPrimaryKeyColumns))
			copy(conflict, saleChannelListingPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"sale_channel_listings\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(saleChannelListingType, saleChannelListingMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(saleChannelListingType, saleChannelListingMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert sale_channel_listings")
	}

	if !cached {
		saleChannelListingUpsertCacheMut.Lock()
		saleChannelListingUpsertCache[key] = cache
		saleChannelListingUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single SaleChannelListing record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *SaleChannelListing) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no SaleChannelListing provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), saleChannelListingPrimaryKeyMapping)
	sql := "DELETE FROM \"sale_channel_listings\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from sale_channel_listings")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for sale_channel_listings")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q saleChannelListingQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no saleChannelListingQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from sale_channel_listings")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for sale_channel_listings")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o SaleChannelListingSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(saleChannelListingBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), saleChannelListingPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"sale_channel_listings\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, saleChannelListingPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from saleChannelListing slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for sale_channel_listings")
	}

	if len(saleChannelListingAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *SaleChannelListing) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindSaleChannelListing(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *SaleChannelListingSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := SaleChannelListingSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), saleChannelListingPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"sale_channel_listings\".* FROM \"sale_channel_listings\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, saleChannelListingPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in SaleChannelListingSlice")
	}

	*o = slice

	return nil
}

// SaleChannelListingExists checks if the SaleChannelListing row exists.
func SaleChannelListingExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"sale_channel_listings\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if sale_channel_listings exists")
	}

	return exists, nil
}

// Exists checks if the SaleChannelListing row exists.
func (o *SaleChannelListing) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return SaleChannelListingExists(ctx, exec, o.ID)
}
