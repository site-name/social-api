# Where are all the schema files located? globs are supported eg  src/**/*.graphqls
schema:
  - web/schemas/*.graphqls

# Where should the generated server code go?
exec:
  filename: web/graphql/GRAPHQL_GENERATED.go
  package: graphql

# Uncomment to enable federation
# federation:
#   filename: graph/generated/federation.go
#   package: generated

# Where should any generated models go?
model:
  filename: web/graphql/gqlmodel/GRAPHQL_MODELS_GEN.go
  package: gqlmodel

# Where should the resolver implementations go?
resolver:
  layout: follow-schema
  dir: web/graphql
  package: graphql

# Optional: turn on use `gqlgen:"fieldName"` tags in your models
# struct_tag: json

# Optional: turn on to use []Thing instead of []*Thing
omit_slice_element_pointers: true

# Optional: set to speed up generation time by not performing a final validation pass.
# skip_validation: true

# gqlgen will search for any type names in the schema in these go packages
# if they match it will use them, otherwise it will generate them.
autobind:
  - "github.com/sitename/sitename/web/graphql/gqlmodel"

# This section declares type mapping between the GraphQL and go type systems
#
# The first line in each type will be used as defaults for resolver arguments and
# modelgen, the others will be allowed when binding to fields. Configure them to
# your liking
models:
  ID:
    model:
      - github.com/99designs/gqlgen/graphql.ID
      - github.com/99designs/gqlgen/graphql.Int
      - github.com/99designs/gqlgen/graphql.Int64
      - github.com/99designs/gqlgen/graphql.Int32
  Int:
    model:
      - github.com/99designs/gqlgen/graphql.Int
      - github.com/99designs/gqlgen/graphql.Int64
      - github.com/99designs/gqlgen/graphql.Int32

  # PositiveDecimal:
  #   model:
  #     - github.com/sitename/sitename/web/scalars.PositiveDecimal

  UUID:
    model:
      - github.com/sitename/sitename/web/scalars.UUID
  Date:
    model:
      - github.com/sitename/sitename/web/scalars.Date
  DateTime:
    model:
      - github.com/sitename/sitename/web/scalars.DateTime
