// Code generated by SQLBoiler 4.17.1 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package model

import (
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// ShopTranslation is an object representing the database table.
type ShopTranslation struct {
	ID           string       `boil:"id" json:"id" toml:"id" yaml:"id"`
	LanguageCode LanguageCode `boil:"language_code" json:"language_code" toml:"language_code" yaml:"language_code"`
	Name         string       `boil:"name" json:"name" toml:"name" yaml:"name"`
	Description  string       `boil:"description" json:"description" toml:"description" yaml:"description"`
	CreatedAt    int64        `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt    int64        `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`

	R *shopTranslationR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L shopTranslationL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var ShopTranslationColumns = struct {
	ID           string
	LanguageCode string
	Name         string
	Description  string
	CreatedAt    string
	UpdatedAt    string
}{
	ID:           "id",
	LanguageCode: "language_code",
	Name:         "name",
	Description:  "description",
	CreatedAt:    "created_at",
	UpdatedAt:    "updated_at",
}

var ShopTranslationTableColumns = struct {
	ID           string
	LanguageCode string
	Name         string
	Description  string
	CreatedAt    string
	UpdatedAt    string
}{
	ID:           "shop_translations.id",
	LanguageCode: "shop_translations.language_code",
	Name:         "shop_translations.name",
	Description:  "shop_translations.description",
	CreatedAt:    "shop_translations.created_at",
	UpdatedAt:    "shop_translations.updated_at",
}

// Generated where

var ShopTranslationWhere = struct {
	ID           whereHelperstring
	LanguageCode whereHelperLanguageCode
	Name         whereHelperstring
	Description  whereHelperstring
	CreatedAt    whereHelperint64
	UpdatedAt    whereHelperint64
}{
	ID:           whereHelperstring{field: "\"shop_translations\".\"id\""},
	LanguageCode: whereHelperLanguageCode{field: "\"shop_translations\".\"language_code\""},
	Name:         whereHelperstring{field: "\"shop_translations\".\"name\""},
	Description:  whereHelperstring{field: "\"shop_translations\".\"description\""},
	CreatedAt:    whereHelperint64{field: "\"shop_translations\".\"created_at\""},
	UpdatedAt:    whereHelperint64{field: "\"shop_translations\".\"updated_at\""},
}

// ShopTranslationRels is where relationship names are stored.
var ShopTranslationRels = struct {
}{}

// shopTranslationR is where relationships are stored.
type shopTranslationR struct {
}

// NewStruct creates a new relationship struct
func (*shopTranslationR) NewStruct() *shopTranslationR {
	return &shopTranslationR{}
}

// shopTranslationL is where Load methods for each relationship are stored.
type shopTranslationL struct{}

var (
	shopTranslationAllColumns            = []string{"id", "language_code", "name", "description", "created_at", "updated_at"}
	shopTranslationColumnsWithoutDefault = []string{"id", "language_code", "name", "description", "created_at", "updated_at"}
	shopTranslationColumnsWithDefault    = []string{}
	shopTranslationPrimaryKeyColumns     = []string{"id"}
	shopTranslationGeneratedColumns      = []string{}
)

type (
	// ShopTranslationSlice is an alias for a slice of pointers to ShopTranslation.
	// This should almost always be used instead of []ShopTranslation.
	ShopTranslationSlice []*ShopTranslation

	shopTranslationQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	shopTranslationType                 = reflect.TypeOf(&ShopTranslation{})
	shopTranslationMapping              = queries.MakeStructMapping(shopTranslationType)
	shopTranslationPrimaryKeyMapping, _ = queries.BindMapping(shopTranslationType, shopTranslationMapping, shopTranslationPrimaryKeyColumns)
	shopTranslationInsertCacheMut       sync.RWMutex
	shopTranslationInsertCache          = make(map[string]insertCache)
	shopTranslationUpdateCacheMut       sync.RWMutex
	shopTranslationUpdateCache          = make(map[string]updateCache)
	shopTranslationUpsertCacheMut       sync.RWMutex
	shopTranslationUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// One returns a single shopTranslation record from the query.
func (q shopTranslationQuery) One(exec boil.Executor) (*ShopTranslation, error) {
	o := &ShopTranslation{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(nil, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "model: failed to execute a one query for shop_translations")
	}

	return o, nil
}

// All returns all ShopTranslation records from the query.
func (q shopTranslationQuery) All(exec boil.Executor) (ShopTranslationSlice, error) {
	var o []*ShopTranslation

	err := q.Bind(nil, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "model: failed to assign all query results to ShopTranslation slice")
	}

	return o, nil
}

// Count returns the count of all ShopTranslation records in the query.
func (q shopTranslationQuery) Count(exec boil.Executor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to count shop_translations rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q shopTranslationQuery) Exists(exec boil.Executor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "model: failed to check if shop_translations exists")
	}

	return count > 0, nil
}

// ShopTranslations retrieves all the records using an executor.
func ShopTranslations(mods ...qm.QueryMod) shopTranslationQuery {
	mods = append(mods, qm.From("\"shop_translations\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"shop_translations\".*"})
	}

	return shopTranslationQuery{q}
}

// FindShopTranslation retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindShopTranslation(exec boil.Executor, iD string, selectCols ...string) (*ShopTranslation, error) {
	shopTranslationObj := &ShopTranslation{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"shop_translations\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(nil, exec, shopTranslationObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "model: unable to select from shop_translations")
	}

	return shopTranslationObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *ShopTranslation) Insert(exec boil.Executor, columns boil.Columns) error {
	if o == nil {
		return errors.New("model: no shop_translations provided for insertion")
	}

	var err error

	nzDefaults := queries.NonZeroDefaultSet(shopTranslationColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	shopTranslationInsertCacheMut.RLock()
	cache, cached := shopTranslationInsertCache[key]
	shopTranslationInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			shopTranslationAllColumns,
			shopTranslationColumnsWithDefault,
			shopTranslationColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(shopTranslationType, shopTranslationMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(shopTranslationType, shopTranslationMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"shop_translations\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"shop_translations\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "model: unable to insert into shop_translations")
	}

	if !cached {
		shopTranslationInsertCacheMut.Lock()
		shopTranslationInsertCache[key] = cache
		shopTranslationInsertCacheMut.Unlock()
	}

	return nil
}

// Update uses an executor to update the ShopTranslation.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *ShopTranslation) Update(exec boil.Executor, columns boil.Columns) (int64, error) {
	var err error
	key := makeCacheKey(columns, nil)
	shopTranslationUpdateCacheMut.RLock()
	cache, cached := shopTranslationUpdateCache[key]
	shopTranslationUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			shopTranslationAllColumns,
			shopTranslationPrimaryKeyColumns,
		)
		if len(wl) == 0 {
			return 0, errors.New("model: unable to update shop_translations, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"shop_translations\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, shopTranslationPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(shopTranslationType, shopTranslationMapping, append(wl, shopTranslationPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	var result sql.Result
	result, err = exec.Exec(cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to update shop_translations row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by update for shop_translations")
	}

	if !cached {
		shopTranslationUpdateCacheMut.Lock()
		shopTranslationUpdateCache[key] = cache
		shopTranslationUpdateCacheMut.Unlock()
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values.
func (q shopTranslationQuery) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to update all for shop_translations")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to retrieve rows affected for shop_translations")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o ShopTranslationSlice) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("model: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), shopTranslationPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"shop_translations\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, shopTranslationPrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to update all in shopTranslation slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to retrieve rows affected all in update all shopTranslation")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *ShopTranslation) Upsert(exec boil.Executor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("model: no shop_translations provided for upsert")
	}

	nzDefaults := queries.NonZeroDefaultSet(shopTranslationColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	shopTranslationUpsertCacheMut.RLock()
	cache, cached := shopTranslationUpsertCache[key]
	shopTranslationUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			shopTranslationAllColumns,
			shopTranslationColumnsWithDefault,
			shopTranslationColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			shopTranslationAllColumns,
			shopTranslationPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("model: unable to upsert shop_translations, could not build update column list")
		}

		ret := strmangle.SetComplement(shopTranslationAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(shopTranslationPrimaryKeyColumns) == 0 {
				return errors.New("model: unable to upsert shop_translations, could not build conflict column list")
			}

			conflict = make([]string, len(shopTranslationPrimaryKeyColumns))
			copy(conflict, shopTranslationPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"shop_translations\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(shopTranslationType, shopTranslationMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(shopTranslationType, shopTranslationMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "model: unable to upsert shop_translations")
	}

	if !cached {
		shopTranslationUpsertCacheMut.Lock()
		shopTranslationUpsertCache[key] = cache
		shopTranslationUpsertCacheMut.Unlock()
	}

	return nil
}

// Delete deletes a single ShopTranslation record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *ShopTranslation) Delete(exec boil.Executor) (int64, error) {
	if o == nil {
		return 0, errors.New("model: no ShopTranslation provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), shopTranslationPrimaryKeyMapping)
	sql := "DELETE FROM \"shop_translations\" WHERE \"id\"=$1"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to delete from shop_translations")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by delete for shop_translations")
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q shopTranslationQuery) DeleteAll(exec boil.Executor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("model: no shopTranslationQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to delete all from shop_translations")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by deleteall for shop_translations")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o ShopTranslationSlice) DeleteAll(exec boil.Executor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), shopTranslationPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"shop_translations\" WHERE " +
		strmangle.WhereInClause(string(dialect.LQ), string(dialect.RQ), 1, shopTranslationPrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to delete all from shopTranslation slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by deleteall for shop_translations")
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *ShopTranslation) Reload(exec boil.Executor) error {
	ret, err := FindShopTranslation(exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *ShopTranslationSlice) ReloadAll(exec boil.Executor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := ShopTranslationSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), shopTranslationPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"shop_translations\".* FROM \"shop_translations\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, shopTranslationPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(nil, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "model: unable to reload all in ShopTranslationSlice")
	}

	*o = slice

	return nil
}

// ShopTranslationExists checks if the ShopTranslation row exists.
func ShopTranslationExists(exec boil.Executor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"shop_translations\" where \"id\"=$1 limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, iD)
	}
	row := exec.QueryRow(sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "model: unable to check if shop_translations exists")
	}

	return exists, nil
}

// Exists checks if the ShopTranslation row exists.
func (o *ShopTranslation) Exists(exec boil.Executor) (bool, error) {
	return ShopTranslationExists(exec, o.ID)
}
