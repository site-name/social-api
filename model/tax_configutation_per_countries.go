// Code generated by SQLBoiler 4.17.1 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package model

import (
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/sitename/sitename/modules/model_types"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// TaxConfigutationPerCountry is an object representing the database table.
type TaxConfigutationPerCountry struct {
	ID                     string                     `boil:"id" json:"id" toml:"id" yaml:"id"`
	TaxConfigurationID     string                     `boil:"tax_configuration_id" json:"tax_configuration_id" toml:"tax_configuration_id" yaml:"tax_configuration_id"`
	Country                CountryCode                `boil:"country" json:"country" toml:"country" yaml:"country"`
	ChargeTaxes            bool                       `boil:"charge_taxes" json:"charge_taxes" toml:"charge_taxes" yaml:"charge_taxes"`
	TaxCalculationStrategy NullTaxCalculationStrategy `boil:"tax_calculation_strategy" json:"tax_calculation_strategy,omitempty" toml:"tax_calculation_strategy" yaml:"tax_calculation_strategy,omitempty"`
	DisplayGrossPrice      bool                       `boil:"display_gross_price" json:"display_gross_price" toml:"display_gross_price" yaml:"display_gross_price"`
	TaxAppID               model_types.NullString     `boil:"tax_app_id" json:"tax_app_id,omitempty" toml:"tax_app_id" yaml:"tax_app_id,omitempty"`

	R *taxConfigutationPerCountryR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L taxConfigutationPerCountryL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var TaxConfigutationPerCountryColumns = struct {
	ID                     string
	TaxConfigurationID     string
	Country                string
	ChargeTaxes            string
	TaxCalculationStrategy string
	DisplayGrossPrice      string
	TaxAppID               string
}{
	ID:                     "id",
	TaxConfigurationID:     "tax_configuration_id",
	Country:                "country",
	ChargeTaxes:            "charge_taxes",
	TaxCalculationStrategy: "tax_calculation_strategy",
	DisplayGrossPrice:      "display_gross_price",
	TaxAppID:               "tax_app_id",
}

var TaxConfigutationPerCountryTableColumns = struct {
	ID                     string
	TaxConfigurationID     string
	Country                string
	ChargeTaxes            string
	TaxCalculationStrategy string
	DisplayGrossPrice      string
	TaxAppID               string
}{
	ID:                     "tax_configutation_per_countries.id",
	TaxConfigurationID:     "tax_configutation_per_countries.tax_configuration_id",
	Country:                "tax_configutation_per_countries.country",
	ChargeTaxes:            "tax_configutation_per_countries.charge_taxes",
	TaxCalculationStrategy: "tax_configutation_per_countries.tax_calculation_strategy",
	DisplayGrossPrice:      "tax_configutation_per_countries.display_gross_price",
	TaxAppID:               "tax_configutation_per_countries.tax_app_id",
}

// Generated where

var TaxConfigutationPerCountryWhere = struct {
	ID                     whereHelperstring
	TaxConfigurationID     whereHelperstring
	Country                whereHelperCountryCode
	ChargeTaxes            whereHelperbool
	TaxCalculationStrategy whereHelperNullTaxCalculationStrategy
	DisplayGrossPrice      whereHelperbool
	TaxAppID               whereHelpermodel_types_NullString
}{
	ID:                     whereHelperstring{field: "\"tax_configutation_per_countries\".\"id\""},
	TaxConfigurationID:     whereHelperstring{field: "\"tax_configutation_per_countries\".\"tax_configuration_id\""},
	Country:                whereHelperCountryCode{field: "\"tax_configutation_per_countries\".\"country\""},
	ChargeTaxes:            whereHelperbool{field: "\"tax_configutation_per_countries\".\"charge_taxes\""},
	TaxCalculationStrategy: whereHelperNullTaxCalculationStrategy{field: "\"tax_configutation_per_countries\".\"tax_calculation_strategy\""},
	DisplayGrossPrice:      whereHelperbool{field: "\"tax_configutation_per_countries\".\"display_gross_price\""},
	TaxAppID:               whereHelpermodel_types_NullString{field: "\"tax_configutation_per_countries\".\"tax_app_id\""},
}

// TaxConfigutationPerCountryRels is where relationship names are stored.
var TaxConfigutationPerCountryRels = struct {
	TaxConfiguration string
}{
	TaxConfiguration: "TaxConfiguration",
}

// taxConfigutationPerCountryR is where relationships are stored.
type taxConfigutationPerCountryR struct {
	TaxConfiguration *TaxConfiguration `boil:"TaxConfiguration" json:"TaxConfiguration" toml:"TaxConfiguration" yaml:"TaxConfiguration"`
}

// NewStruct creates a new relationship struct
func (*taxConfigutationPerCountryR) NewStruct() *taxConfigutationPerCountryR {
	return &taxConfigutationPerCountryR{}
}

func (r *taxConfigutationPerCountryR) GetTaxConfiguration() *TaxConfiguration {
	if r == nil {
		return nil
	}
	return r.TaxConfiguration
}

// taxConfigutationPerCountryL is where Load methods for each relationship are stored.
type taxConfigutationPerCountryL struct{}

var (
	taxConfigutationPerCountryAllColumns            = []string{"id", "tax_configuration_id", "country", "charge_taxes", "tax_calculation_strategy", "display_gross_price", "tax_app_id"}
	taxConfigutationPerCountryColumnsWithoutDefault = []string{"id", "tax_configuration_id", "country"}
	taxConfigutationPerCountryColumnsWithDefault    = []string{"charge_taxes", "tax_calculation_strategy", "display_gross_price", "tax_app_id"}
	taxConfigutationPerCountryPrimaryKeyColumns     = []string{"id"}
	taxConfigutationPerCountryGeneratedColumns      = []string{}
)

type (
	// TaxConfigutationPerCountrySlice is an alias for a slice of pointers to TaxConfigutationPerCountry.
	// This should almost always be used instead of []TaxConfigutationPerCountry.
	TaxConfigutationPerCountrySlice []*TaxConfigutationPerCountry

	taxConfigutationPerCountryQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	taxConfigutationPerCountryType                 = reflect.TypeOf(&TaxConfigutationPerCountry{})
	taxConfigutationPerCountryMapping              = queries.MakeStructMapping(taxConfigutationPerCountryType)
	taxConfigutationPerCountryPrimaryKeyMapping, _ = queries.BindMapping(taxConfigutationPerCountryType, taxConfigutationPerCountryMapping, taxConfigutationPerCountryPrimaryKeyColumns)
	taxConfigutationPerCountryInsertCacheMut       sync.RWMutex
	taxConfigutationPerCountryInsertCache          = make(map[string]insertCache)
	taxConfigutationPerCountryUpdateCacheMut       sync.RWMutex
	taxConfigutationPerCountryUpdateCache          = make(map[string]updateCache)
	taxConfigutationPerCountryUpsertCacheMut       sync.RWMutex
	taxConfigutationPerCountryUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// One returns a single taxConfigutationPerCountry record from the query.
func (q taxConfigutationPerCountryQuery) One(exec boil.Executor) (*TaxConfigutationPerCountry, error) {
	o := &TaxConfigutationPerCountry{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(nil, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "model: failed to execute a one query for tax_configutation_per_countries")
	}

	return o, nil
}

// All returns all TaxConfigutationPerCountry records from the query.
func (q taxConfigutationPerCountryQuery) All(exec boil.Executor) (TaxConfigutationPerCountrySlice, error) {
	var o []*TaxConfigutationPerCountry

	err := q.Bind(nil, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "model: failed to assign all query results to TaxConfigutationPerCountry slice")
	}

	return o, nil
}

// Count returns the count of all TaxConfigutationPerCountry records in the query.
func (q taxConfigutationPerCountryQuery) Count(exec boil.Executor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to count tax_configutation_per_countries rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q taxConfigutationPerCountryQuery) Exists(exec boil.Executor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "model: failed to check if tax_configutation_per_countries exists")
	}

	return count > 0, nil
}

// TaxConfiguration pointed to by the foreign key.
func (o *TaxConfigutationPerCountry) TaxConfiguration(mods ...qm.QueryMod) taxConfigurationQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.TaxConfigurationID),
	}

	queryMods = append(queryMods, mods...)

	return TaxConfigurations(queryMods...)
}

// LoadTaxConfiguration allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (taxConfigutationPerCountryL) LoadTaxConfiguration(e boil.Executor, singular bool, maybeTaxConfigutationPerCountry interface{}, mods queries.Applicator) error {
	var slice []*TaxConfigutationPerCountry
	var object *TaxConfigutationPerCountry

	if singular {
		var ok bool
		object, ok = maybeTaxConfigutationPerCountry.(*TaxConfigutationPerCountry)
		if !ok {
			object = new(TaxConfigutationPerCountry)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeTaxConfigutationPerCountry)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeTaxConfigutationPerCountry))
			}
		}
	} else {
		s, ok := maybeTaxConfigutationPerCountry.(*[]*TaxConfigutationPerCountry)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeTaxConfigutationPerCountry)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeTaxConfigutationPerCountry))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &taxConfigutationPerCountryR{}
		}
		args[object.TaxConfigurationID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &taxConfigutationPerCountryR{}
			}

			args[obj.TaxConfigurationID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`tax_configurations`),
		qm.WhereIn(`tax_configurations.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.Query(e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load TaxConfiguration")
	}

	var resultSlice []*TaxConfiguration
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice TaxConfiguration")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for tax_configurations")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for tax_configurations")
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.TaxConfiguration = foreign
		if foreign.R == nil {
			foreign.R = &taxConfigurationR{}
		}
		foreign.R.TaxConfigutationPerCountries = append(foreign.R.TaxConfigutationPerCountries, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.TaxConfigurationID == foreign.ID {
				local.R.TaxConfiguration = foreign
				if foreign.R == nil {
					foreign.R = &taxConfigurationR{}
				}
				foreign.R.TaxConfigutationPerCountries = append(foreign.R.TaxConfigutationPerCountries, local)
				break
			}
		}
	}

	return nil
}

// SetTaxConfiguration of the taxConfigutationPerCountry to the related item.
// Sets o.R.TaxConfiguration to related.
// Adds o to related.R.TaxConfigutationPerCountries.
func (o *TaxConfigutationPerCountry) SetTaxConfiguration(exec boil.Executor, insert bool, related *TaxConfiguration) error {
	var err error
	if insert {
		if err = related.Insert(exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"tax_configutation_per_countries\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"tax_configuration_id"}),
		strmangle.WhereClause("\"", "\"", 2, taxConfigutationPerCountryPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	if _, err = exec.Exec(updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.TaxConfigurationID = related.ID
	if o.R == nil {
		o.R = &taxConfigutationPerCountryR{
			TaxConfiguration: related,
		}
	} else {
		o.R.TaxConfiguration = related
	}

	if related.R == nil {
		related.R = &taxConfigurationR{
			TaxConfigutationPerCountries: TaxConfigutationPerCountrySlice{o},
		}
	} else {
		related.R.TaxConfigutationPerCountries = append(related.R.TaxConfigutationPerCountries, o)
	}

	return nil
}

// TaxConfigutationPerCountries retrieves all the records using an executor.
func TaxConfigutationPerCountries(mods ...qm.QueryMod) taxConfigutationPerCountryQuery {
	mods = append(mods, qm.From("\"tax_configutation_per_countries\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"tax_configutation_per_countries\".*"})
	}

	return taxConfigutationPerCountryQuery{q}
}

// FindTaxConfigutationPerCountry retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindTaxConfigutationPerCountry(exec boil.Executor, iD string, selectCols ...string) (*TaxConfigutationPerCountry, error) {
	taxConfigutationPerCountryObj := &TaxConfigutationPerCountry{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"tax_configutation_per_countries\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(nil, exec, taxConfigutationPerCountryObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "model: unable to select from tax_configutation_per_countries")
	}

	return taxConfigutationPerCountryObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *TaxConfigutationPerCountry) Insert(exec boil.Executor, columns boil.Columns) error {
	if o == nil {
		return errors.New("model: no tax_configutation_per_countries provided for insertion")
	}

	var err error

	nzDefaults := queries.NonZeroDefaultSet(taxConfigutationPerCountryColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	taxConfigutationPerCountryInsertCacheMut.RLock()
	cache, cached := taxConfigutationPerCountryInsertCache[key]
	taxConfigutationPerCountryInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			taxConfigutationPerCountryAllColumns,
			taxConfigutationPerCountryColumnsWithDefault,
			taxConfigutationPerCountryColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(taxConfigutationPerCountryType, taxConfigutationPerCountryMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(taxConfigutationPerCountryType, taxConfigutationPerCountryMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"tax_configutation_per_countries\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"tax_configutation_per_countries\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "model: unable to insert into tax_configutation_per_countries")
	}

	if !cached {
		taxConfigutationPerCountryInsertCacheMut.Lock()
		taxConfigutationPerCountryInsertCache[key] = cache
		taxConfigutationPerCountryInsertCacheMut.Unlock()
	}

	return nil
}

// Update uses an executor to update the TaxConfigutationPerCountry.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *TaxConfigutationPerCountry) Update(exec boil.Executor, columns boil.Columns) (int64, error) {
	var err error
	key := makeCacheKey(columns, nil)
	taxConfigutationPerCountryUpdateCacheMut.RLock()
	cache, cached := taxConfigutationPerCountryUpdateCache[key]
	taxConfigutationPerCountryUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			taxConfigutationPerCountryAllColumns,
			taxConfigutationPerCountryPrimaryKeyColumns,
		)
		if len(wl) == 0 {
			return 0, errors.New("model: unable to update tax_configutation_per_countries, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"tax_configutation_per_countries\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, taxConfigutationPerCountryPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(taxConfigutationPerCountryType, taxConfigutationPerCountryMapping, append(wl, taxConfigutationPerCountryPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	var result sql.Result
	result, err = exec.Exec(cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to update tax_configutation_per_countries row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by update for tax_configutation_per_countries")
	}

	if !cached {
		taxConfigutationPerCountryUpdateCacheMut.Lock()
		taxConfigutationPerCountryUpdateCache[key] = cache
		taxConfigutationPerCountryUpdateCacheMut.Unlock()
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values.
func (q taxConfigutationPerCountryQuery) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to update all for tax_configutation_per_countries")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to retrieve rows affected for tax_configutation_per_countries")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o TaxConfigutationPerCountrySlice) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("model: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), taxConfigutationPerCountryPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"tax_configutation_per_countries\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, taxConfigutationPerCountryPrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to update all in taxConfigutationPerCountry slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to retrieve rows affected all in update all taxConfigutationPerCountry")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *TaxConfigutationPerCountry) Upsert(exec boil.Executor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("model: no tax_configutation_per_countries provided for upsert")
	}

	nzDefaults := queries.NonZeroDefaultSet(taxConfigutationPerCountryColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	taxConfigutationPerCountryUpsertCacheMut.RLock()
	cache, cached := taxConfigutationPerCountryUpsertCache[key]
	taxConfigutationPerCountryUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			taxConfigutationPerCountryAllColumns,
			taxConfigutationPerCountryColumnsWithDefault,
			taxConfigutationPerCountryColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			taxConfigutationPerCountryAllColumns,
			taxConfigutationPerCountryPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("model: unable to upsert tax_configutation_per_countries, could not build update column list")
		}

		ret := strmangle.SetComplement(taxConfigutationPerCountryAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(taxConfigutationPerCountryPrimaryKeyColumns) == 0 {
				return errors.New("model: unable to upsert tax_configutation_per_countries, could not build conflict column list")
			}

			conflict = make([]string, len(taxConfigutationPerCountryPrimaryKeyColumns))
			copy(conflict, taxConfigutationPerCountryPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"tax_configutation_per_countries\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(taxConfigutationPerCountryType, taxConfigutationPerCountryMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(taxConfigutationPerCountryType, taxConfigutationPerCountryMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "model: unable to upsert tax_configutation_per_countries")
	}

	if !cached {
		taxConfigutationPerCountryUpsertCacheMut.Lock()
		taxConfigutationPerCountryUpsertCache[key] = cache
		taxConfigutationPerCountryUpsertCacheMut.Unlock()
	}

	return nil
}

// Delete deletes a single TaxConfigutationPerCountry record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *TaxConfigutationPerCountry) Delete(exec boil.Executor) (int64, error) {
	if o == nil {
		return 0, errors.New("model: no TaxConfigutationPerCountry provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), taxConfigutationPerCountryPrimaryKeyMapping)
	sql := "DELETE FROM \"tax_configutation_per_countries\" WHERE \"id\"=$1"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to delete from tax_configutation_per_countries")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by delete for tax_configutation_per_countries")
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q taxConfigutationPerCountryQuery) DeleteAll(exec boil.Executor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("model: no taxConfigutationPerCountryQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to delete all from tax_configutation_per_countries")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by deleteall for tax_configutation_per_countries")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o TaxConfigutationPerCountrySlice) DeleteAll(exec boil.Executor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), taxConfigutationPerCountryPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"tax_configutation_per_countries\" WHERE " +
		strmangle.WhereInClause(string(dialect.LQ), string(dialect.RQ), 1, taxConfigutationPerCountryPrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to delete all from taxConfigutationPerCountry slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by deleteall for tax_configutation_per_countries")
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *TaxConfigutationPerCountry) Reload(exec boil.Executor) error {
	ret, err := FindTaxConfigutationPerCountry(exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *TaxConfigutationPerCountrySlice) ReloadAll(exec boil.Executor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := TaxConfigutationPerCountrySlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), taxConfigutationPerCountryPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"tax_configutation_per_countries\".* FROM \"tax_configutation_per_countries\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, taxConfigutationPerCountryPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(nil, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "model: unable to reload all in TaxConfigutationPerCountrySlice")
	}

	*o = slice

	return nil
}

// TaxConfigutationPerCountryExists checks if the TaxConfigutationPerCountry row exists.
func TaxConfigutationPerCountryExists(exec boil.Executor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"tax_configutation_per_countries\" where \"id\"=$1 limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, iD)
	}
	row := exec.QueryRow(sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "model: unable to check if tax_configutation_per_countries exists")
	}

	return exists, nil
}

// Exists checks if the TaxConfigutationPerCountry row exists.
func (o *TaxConfigutationPerCountry) Exists(exec boil.Executor) (bool, error) {
	return TaxConfigutationPerCountryExists(exec, o.ID)
}
