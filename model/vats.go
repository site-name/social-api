// Code generated by SQLBoiler 4.16.2 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package model

import (
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/sitename/sitename/modules/model_types"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// Vat is an object representing the database table.
type Vat struct {
	ID          string                 `boil:"id" json:"id" toml:"id" yaml:"id"`
	CountryCode CountryCode            `boil:"country_code" json:"country_code" toml:"country_code" yaml:"country_code"`
	Data        model_types.JSONString `boil:"data" json:"data,omitempty" toml:"data" yaml:"data,omitempty"`

	R *vatR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L vatL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var VatColumns = struct {
	ID          string
	CountryCode string
	Data        string
}{
	ID:          "id",
	CountryCode: "country_code",
	Data:        "data",
}

var VatTableColumns = struct {
	ID          string
	CountryCode string
	Data        string
}{
	ID:          "vats.id",
	CountryCode: "vats.country_code",
	Data:        "vats.data",
}

// Generated where

var VatWhere = struct {
	ID          whereHelperstring
	CountryCode whereHelperCountryCode
	Data        whereHelpermodel_types_JSONString
}{
	ID:          whereHelperstring{field: "\"vats\".\"id\""},
	CountryCode: whereHelperCountryCode{field: "\"vats\".\"country_code\""},
	Data:        whereHelpermodel_types_JSONString{field: "\"vats\".\"data\""},
}

// VatRels is where relationship names are stored.
var VatRels = struct {
}{}

// vatR is where relationships are stored.
type vatR struct {
}

// NewStruct creates a new relationship struct
func (*vatR) NewStruct() *vatR {
	return &vatR{}
}

// vatL is where Load methods for each relationship are stored.
type vatL struct{}

var (
	vatAllColumns            = []string{"id", "country_code", "data"}
	vatColumnsWithoutDefault = []string{"id", "country_code"}
	vatColumnsWithDefault    = []string{"data"}
	vatPrimaryKeyColumns     = []string{"id"}
	vatGeneratedColumns      = []string{}
)

type (
	// VatSlice is an alias for a slice of pointers to Vat.
	// This should almost always be used instead of []Vat.
	VatSlice []*Vat

	vatQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	vatType                 = reflect.TypeOf(&Vat{})
	vatMapping              = queries.MakeStructMapping(vatType)
	vatPrimaryKeyMapping, _ = queries.BindMapping(vatType, vatMapping, vatPrimaryKeyColumns)
	vatInsertCacheMut       sync.RWMutex
	vatInsertCache          = make(map[string]insertCache)
	vatUpdateCacheMut       sync.RWMutex
	vatUpdateCache          = make(map[string]updateCache)
	vatUpsertCacheMut       sync.RWMutex
	vatUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// One returns a single vat record from the query.
func (q vatQuery) One(exec boil.Executor) (*Vat, error) {
	o := &Vat{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(nil, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "model: failed to execute a one query for vats")
	}

	return o, nil
}

// All returns all Vat records from the query.
func (q vatQuery) All(exec boil.Executor) (VatSlice, error) {
	var o []*Vat

	err := q.Bind(nil, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "model: failed to assign all query results to Vat slice")
	}

	return o, nil
}

// Count returns the count of all Vat records in the query.
func (q vatQuery) Count(exec boil.Executor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to count vats rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q vatQuery) Exists(exec boil.Executor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "model: failed to check if vats exists")
	}

	return count > 0, nil
}

// Vats retrieves all the records using an executor.
func Vats(mods ...qm.QueryMod) vatQuery {
	mods = append(mods, qm.From("\"vats\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"vats\".*"})
	}

	return vatQuery{q}
}

// FindVat retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindVat(exec boil.Executor, iD string, selectCols ...string) (*Vat, error) {
	vatObj := &Vat{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"vats\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(nil, exec, vatObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "model: unable to select from vats")
	}

	return vatObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *Vat) Insert(exec boil.Executor, columns boil.Columns) error {
	if o == nil {
		return errors.New("model: no vats provided for insertion")
	}

	var err error

	nzDefaults := queries.NonZeroDefaultSet(vatColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	vatInsertCacheMut.RLock()
	cache, cached := vatInsertCache[key]
	vatInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			vatAllColumns,
			vatColumnsWithDefault,
			vatColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(vatType, vatMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(vatType, vatMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"vats\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"vats\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "model: unable to insert into vats")
	}

	if !cached {
		vatInsertCacheMut.Lock()
		vatInsertCache[key] = cache
		vatInsertCacheMut.Unlock()
	}

	return nil
}

// Update uses an executor to update the Vat.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *Vat) Update(exec boil.Executor, columns boil.Columns) (int64, error) {
	var err error
	key := makeCacheKey(columns, nil)
	vatUpdateCacheMut.RLock()
	cache, cached := vatUpdateCache[key]
	vatUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			vatAllColumns,
			vatPrimaryKeyColumns,
		)
		if len(wl) == 0 {
			return 0, errors.New("model: unable to update vats, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"vats\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, vatPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(vatType, vatMapping, append(wl, vatPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	var result sql.Result
	result, err = exec.Exec(cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to update vats row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by update for vats")
	}

	if !cached {
		vatUpdateCacheMut.Lock()
		vatUpdateCache[key] = cache
		vatUpdateCacheMut.Unlock()
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values.
func (q vatQuery) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to update all for vats")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to retrieve rows affected for vats")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o VatSlice) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("model: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]any, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), vatPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"vats\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, vatPrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to update all in vat slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to retrieve rows affected all in update all vat")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *Vat) Upsert(exec boil.Executor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("model: no vats provided for upsert")
	}

	nzDefaults := queries.NonZeroDefaultSet(vatColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	vatUpsertCacheMut.RLock()
	cache, cached := vatUpsertCache[key]
	vatUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			vatAllColumns,
			vatColumnsWithDefault,
			vatColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			vatAllColumns,
			vatPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("model: unable to upsert vats, could not build update column list")
		}

		ret := strmangle.SetComplement(vatAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(vatPrimaryKeyColumns) == 0 {
				return errors.New("model: unable to upsert vats, could not build conflict column list")
			}

			conflict = make([]string, len(vatPrimaryKeyColumns))
			copy(conflict, vatPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"vats\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(vatType, vatMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(vatType, vatMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []any
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "model: unable to upsert vats")
	}

	if !cached {
		vatUpsertCacheMut.Lock()
		vatUpsertCache[key] = cache
		vatUpsertCacheMut.Unlock()
	}

	return nil
}

// Delete deletes a single Vat record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *Vat) Delete(exec boil.Executor) (int64, error) {
	if o == nil {
		return 0, errors.New("model: no Vat provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), vatPrimaryKeyMapping)
	sql := "DELETE FROM \"vats\" WHERE \"id\"=$1"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to delete from vats")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by delete for vats")
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q vatQuery) DeleteAll(exec boil.Executor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("model: no vatQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to delete all from vats")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by deleteall for vats")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o VatSlice) DeleteAll(exec boil.Executor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var args []any
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), vatPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"vats\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, vatPrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to delete all from vat slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by deleteall for vats")
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *Vat) Reload(exec boil.Executor) error {
	ret, err := FindVat(exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *VatSlice) ReloadAll(exec boil.Executor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := VatSlice{}
	var args []any
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), vatPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"vats\".* FROM \"vats\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, vatPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(nil, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "model: unable to reload all in VatSlice")
	}

	*o = slice

	return nil
}

// VatExists checks if the Vat row exists.
func VatExists(exec boil.Executor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"vats\" where \"id\"=$1 limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, iD)
	}
	row := exec.QueryRow(sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "model: unable to check if vats exists")
	}

	return exists, nil
}

// Exists checks if the Vat row exists.
func (o *Vat) Exists(exec boil.Executor) (bool, error) {
	return VatExists(exec, o.ID)
}
