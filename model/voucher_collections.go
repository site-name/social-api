// Code generated by SQLBoiler 4.16.2 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package model

import (
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// VoucherCollection is an object representing the database table.
type VoucherCollection struct {
	ID           string `boil:"id" json:"id" toml:"id" yaml:"id"`
	VoucherID    string `boil:"voucher_id" json:"voucher_id" toml:"voucher_id" yaml:"voucher_id"`
	CollectionID string `boil:"collection_id" json:"collection_id" toml:"collection_id" yaml:"collection_id"`

	R *voucherCollectionR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L voucherCollectionL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var VoucherCollectionColumns = struct {
	ID           string
	VoucherID    string
	CollectionID string
}{
	ID:           "id",
	VoucherID:    "voucher_id",
	CollectionID: "collection_id",
}

var VoucherCollectionTableColumns = struct {
	ID           string
	VoucherID    string
	CollectionID string
}{
	ID:           "voucher_collections.id",
	VoucherID:    "voucher_collections.voucher_id",
	CollectionID: "voucher_collections.collection_id",
}

// Generated where

var VoucherCollectionWhere = struct {
	ID           whereHelperstring
	VoucherID    whereHelperstring
	CollectionID whereHelperstring
}{
	ID:           whereHelperstring{field: "\"voucher_collections\".\"id\""},
	VoucherID:    whereHelperstring{field: "\"voucher_collections\".\"voucher_id\""},
	CollectionID: whereHelperstring{field: "\"voucher_collections\".\"collection_id\""},
}

// VoucherCollectionRels is where relationship names are stored.
var VoucherCollectionRels = struct {
	Collection string
	Voucher    string
}{
	Collection: "Collection",
	Voucher:    "Voucher",
}

// voucherCollectionR is where relationships are stored.
type voucherCollectionR struct {
	Collection *Collection `boil:"Collection" json:"Collection" toml:"Collection" yaml:"Collection"`
	Voucher    *Voucher    `boil:"Voucher" json:"Voucher" toml:"Voucher" yaml:"Voucher"`
}

// NewStruct creates a new relationship struct
func (*voucherCollectionR) NewStruct() *voucherCollectionR {
	return &voucherCollectionR{}
}

func (r *voucherCollectionR) GetCollection() *Collection {
	if r == nil {
		return nil
	}
	return r.Collection
}

func (r *voucherCollectionR) GetVoucher() *Voucher {
	if r == nil {
		return nil
	}
	return r.Voucher
}

// voucherCollectionL is where Load methods for each relationship are stored.
type voucherCollectionL struct{}

var (
	voucherCollectionAllColumns            = []string{"id", "voucher_id", "collection_id"}
	voucherCollectionColumnsWithoutDefault = []string{"id", "voucher_id", "collection_id"}
	voucherCollectionColumnsWithDefault    = []string{}
	voucherCollectionPrimaryKeyColumns     = []string{"id"}
	voucherCollectionGeneratedColumns      = []string{}
)

type (
	// VoucherCollectionSlice is an alias for a slice of pointers to VoucherCollection.
	// This should almost always be used instead of []VoucherCollection.
	VoucherCollectionSlice []*VoucherCollection

	voucherCollectionQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	voucherCollectionType                 = reflect.TypeOf(&VoucherCollection{})
	voucherCollectionMapping              = queries.MakeStructMapping(voucherCollectionType)
	voucherCollectionPrimaryKeyMapping, _ = queries.BindMapping(voucherCollectionType, voucherCollectionMapping, voucherCollectionPrimaryKeyColumns)
	voucherCollectionInsertCacheMut       sync.RWMutex
	voucherCollectionInsertCache          = make(map[string]insertCache)
	voucherCollectionUpdateCacheMut       sync.RWMutex
	voucherCollectionUpdateCache          = make(map[string]updateCache)
	voucherCollectionUpsertCacheMut       sync.RWMutex
	voucherCollectionUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// One returns a single voucherCollection record from the query.
func (q voucherCollectionQuery) One(exec boil.Executor) (*VoucherCollection, error) {
	o := &VoucherCollection{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(nil, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "model: failed to execute a one query for voucher_collections")
	}

	return o, nil
}

// All returns all VoucherCollection records from the query.
func (q voucherCollectionQuery) All(exec boil.Executor) (VoucherCollectionSlice, error) {
	var o []*VoucherCollection

	err := q.Bind(nil, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "model: failed to assign all query results to VoucherCollection slice")
	}

	return o, nil
}

// Count returns the count of all VoucherCollection records in the query.
func (q voucherCollectionQuery) Count(exec boil.Executor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to count voucher_collections rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q voucherCollectionQuery) Exists(exec boil.Executor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "model: failed to check if voucher_collections exists")
	}

	return count > 0, nil
}

// Collection pointed to by the foreign key.
func (o *VoucherCollection) Collection(mods ...qm.QueryMod) collectionQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.CollectionID),
	}

	queryMods = append(queryMods, mods...)

	return Collections(queryMods...)
}

// Voucher pointed to by the foreign key.
func (o *VoucherCollection) Voucher(mods ...qm.QueryMod) voucherQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.VoucherID),
	}

	queryMods = append(queryMods, mods...)

	return Vouchers(queryMods...)
}

// LoadCollection allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (voucherCollectionL) LoadCollection(e boil.Executor, singular bool, maybeVoucherCollection any, mods queries.Applicator) error {
	var slice []*VoucherCollection
	var object *VoucherCollection

	if singular {
		var ok bool
		object, ok = maybeVoucherCollection.(*VoucherCollection)
		if !ok {
			object = new(VoucherCollection)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeVoucherCollection)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeVoucherCollection))
			}
		}
	} else {
		s, ok := maybeVoucherCollection.(*[]*VoucherCollection)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeVoucherCollection)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeVoucherCollection))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &voucherCollectionR{}
		}
		args[object.CollectionID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &voucherCollectionR{}
			}

			args[obj.CollectionID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`collections`),
		qm.WhereIn(`collections.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.Query(e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Collection")
	}

	var resultSlice []*Collection
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Collection")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for collections")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for collections")
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Collection = foreign
		if foreign.R == nil {
			foreign.R = &collectionR{}
		}
		foreign.R.VoucherCollections = append(foreign.R.VoucherCollections, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.CollectionID == foreign.ID {
				local.R.Collection = foreign
				if foreign.R == nil {
					foreign.R = &collectionR{}
				}
				foreign.R.VoucherCollections = append(foreign.R.VoucherCollections, local)
				break
			}
		}
	}

	return nil
}

// LoadVoucher allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (voucherCollectionL) LoadVoucher(e boil.Executor, singular bool, maybeVoucherCollection any, mods queries.Applicator) error {
	var slice []*VoucherCollection
	var object *VoucherCollection

	if singular {
		var ok bool
		object, ok = maybeVoucherCollection.(*VoucherCollection)
		if !ok {
			object = new(VoucherCollection)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeVoucherCollection)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeVoucherCollection))
			}
		}
	} else {
		s, ok := maybeVoucherCollection.(*[]*VoucherCollection)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeVoucherCollection)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeVoucherCollection))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &voucherCollectionR{}
		}
		args[object.VoucherID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &voucherCollectionR{}
			}

			args[obj.VoucherID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`vouchers`),
		qm.WhereIn(`vouchers.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.Query(e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Voucher")
	}

	var resultSlice []*Voucher
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Voucher")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for vouchers")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for vouchers")
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Voucher = foreign
		if foreign.R == nil {
			foreign.R = &voucherR{}
		}
		foreign.R.VoucherCollections = append(foreign.R.VoucherCollections, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.VoucherID == foreign.ID {
				local.R.Voucher = foreign
				if foreign.R == nil {
					foreign.R = &voucherR{}
				}
				foreign.R.VoucherCollections = append(foreign.R.VoucherCollections, local)
				break
			}
		}
	}

	return nil
}

// SetCollection of the voucherCollection to the related item.
// Sets o.R.Collection to related.
// Adds o to related.R.VoucherCollections.
func (o *VoucherCollection) SetCollection(exec boil.Executor, insert bool, related *Collection) error {
	var err error
	if insert {
		if err = related.Insert(exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"voucher_collections\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"collection_id"}),
		strmangle.WhereClause("\"", "\"", 2, voucherCollectionPrimaryKeyColumns),
	)
	values := []any{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	if _, err = exec.Exec(updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.CollectionID = related.ID
	if o.R == nil {
		o.R = &voucherCollectionR{
			Collection: related,
		}
	} else {
		o.R.Collection = related
	}

	if related.R == nil {
		related.R = &collectionR{
			VoucherCollections: VoucherCollectionSlice{o},
		}
	} else {
		related.R.VoucherCollections = append(related.R.VoucherCollections, o)
	}

	return nil
}

// SetVoucher of the voucherCollection to the related item.
// Sets o.R.Voucher to related.
// Adds o to related.R.VoucherCollections.
func (o *VoucherCollection) SetVoucher(exec boil.Executor, insert bool, related *Voucher) error {
	var err error
	if insert {
		if err = related.Insert(exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"voucher_collections\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"voucher_id"}),
		strmangle.WhereClause("\"", "\"", 2, voucherCollectionPrimaryKeyColumns),
	)
	values := []any{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	if _, err = exec.Exec(updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.VoucherID = related.ID
	if o.R == nil {
		o.R = &voucherCollectionR{
			Voucher: related,
		}
	} else {
		o.R.Voucher = related
	}

	if related.R == nil {
		related.R = &voucherR{
			VoucherCollections: VoucherCollectionSlice{o},
		}
	} else {
		related.R.VoucherCollections = append(related.R.VoucherCollections, o)
	}

	return nil
}

// VoucherCollections retrieves all the records using an executor.
func VoucherCollections(mods ...qm.QueryMod) voucherCollectionQuery {
	mods = append(mods, qm.From("\"voucher_collections\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"voucher_collections\".*"})
	}

	return voucherCollectionQuery{q}
}

// FindVoucherCollection retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindVoucherCollection(exec boil.Executor, iD string, selectCols ...string) (*VoucherCollection, error) {
	voucherCollectionObj := &VoucherCollection{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"voucher_collections\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(nil, exec, voucherCollectionObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "model: unable to select from voucher_collections")
	}

	return voucherCollectionObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *VoucherCollection) Insert(exec boil.Executor, columns boil.Columns) error {
	if o == nil {
		return errors.New("model: no voucher_collections provided for insertion")
	}

	var err error

	nzDefaults := queries.NonZeroDefaultSet(voucherCollectionColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	voucherCollectionInsertCacheMut.RLock()
	cache, cached := voucherCollectionInsertCache[key]
	voucherCollectionInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			voucherCollectionAllColumns,
			voucherCollectionColumnsWithDefault,
			voucherCollectionColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(voucherCollectionType, voucherCollectionMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(voucherCollectionType, voucherCollectionMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"voucher_collections\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"voucher_collections\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "model: unable to insert into voucher_collections")
	}

	if !cached {
		voucherCollectionInsertCacheMut.Lock()
		voucherCollectionInsertCache[key] = cache
		voucherCollectionInsertCacheMut.Unlock()
	}

	return nil
}

// Update uses an executor to update the VoucherCollection.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *VoucherCollection) Update(exec boil.Executor, columns boil.Columns) (int64, error) {
	var err error
	key := makeCacheKey(columns, nil)
	voucherCollectionUpdateCacheMut.RLock()
	cache, cached := voucherCollectionUpdateCache[key]
	voucherCollectionUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			voucherCollectionAllColumns,
			voucherCollectionPrimaryKeyColumns,
		)
		if len(wl) == 0 {
			return 0, errors.New("model: unable to update voucher_collections, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"voucher_collections\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, voucherCollectionPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(voucherCollectionType, voucherCollectionMapping, append(wl, voucherCollectionPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	var result sql.Result
	result, err = exec.Exec(cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to update voucher_collections row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by update for voucher_collections")
	}

	if !cached {
		voucherCollectionUpdateCacheMut.Lock()
		voucherCollectionUpdateCache[key] = cache
		voucherCollectionUpdateCacheMut.Unlock()
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values.
func (q voucherCollectionQuery) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to update all for voucher_collections")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to retrieve rows affected for voucher_collections")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o VoucherCollectionSlice) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("model: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]any, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), voucherCollectionPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"voucher_collections\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, voucherCollectionPrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to update all in voucherCollection slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to retrieve rows affected all in update all voucherCollection")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *VoucherCollection) Upsert(exec boil.Executor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("model: no voucher_collections provided for upsert")
	}

	nzDefaults := queries.NonZeroDefaultSet(voucherCollectionColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	voucherCollectionUpsertCacheMut.RLock()
	cache, cached := voucherCollectionUpsertCache[key]
	voucherCollectionUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			voucherCollectionAllColumns,
			voucherCollectionColumnsWithDefault,
			voucherCollectionColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			voucherCollectionAllColumns,
			voucherCollectionPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("model: unable to upsert voucher_collections, could not build update column list")
		}

		ret := strmangle.SetComplement(voucherCollectionAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(voucherCollectionPrimaryKeyColumns) == 0 {
				return errors.New("model: unable to upsert voucher_collections, could not build conflict column list")
			}

			conflict = make([]string, len(voucherCollectionPrimaryKeyColumns))
			copy(conflict, voucherCollectionPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"voucher_collections\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(voucherCollectionType, voucherCollectionMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(voucherCollectionType, voucherCollectionMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []any
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "model: unable to upsert voucher_collections")
	}

	if !cached {
		voucherCollectionUpsertCacheMut.Lock()
		voucherCollectionUpsertCache[key] = cache
		voucherCollectionUpsertCacheMut.Unlock()
	}

	return nil
}

// Delete deletes a single VoucherCollection record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *VoucherCollection) Delete(exec boil.Executor) (int64, error) {
	if o == nil {
		return 0, errors.New("model: no VoucherCollection provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), voucherCollectionPrimaryKeyMapping)
	sql := "DELETE FROM \"voucher_collections\" WHERE \"id\"=$1"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to delete from voucher_collections")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by delete for voucher_collections")
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q voucherCollectionQuery) DeleteAll(exec boil.Executor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("model: no voucherCollectionQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to delete all from voucher_collections")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by deleteall for voucher_collections")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o VoucherCollectionSlice) DeleteAll(exec boil.Executor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var args []any
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), voucherCollectionPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"voucher_collections\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, voucherCollectionPrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to delete all from voucherCollection slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by deleteall for voucher_collections")
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *VoucherCollection) Reload(exec boil.Executor) error {
	ret, err := FindVoucherCollection(exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *VoucherCollectionSlice) ReloadAll(exec boil.Executor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := VoucherCollectionSlice{}
	var args []any
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), voucherCollectionPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"voucher_collections\".* FROM \"voucher_collections\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, voucherCollectionPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(nil, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "model: unable to reload all in VoucherCollectionSlice")
	}

	*o = slice

	return nil
}

// VoucherCollectionExists checks if the VoucherCollection row exists.
func VoucherCollectionExists(exec boil.Executor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"voucher_collections\" where \"id\"=$1 limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, iD)
	}
	row := exec.QueryRow(sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "model: unable to check if voucher_collections exists")
	}

	return exists, nil
}

// Exists checks if the VoucherCollection row exists.
func (o *VoucherCollection) Exists(exec boil.Executor) (bool, error) {
	return VoucherCollectionExists(exec, o.ID)
}
