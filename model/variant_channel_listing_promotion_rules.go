// Code generated by SQLBoiler 4.17.1 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package model

import (
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/site-name/decimal"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// VariantChannelListingPromotionRule is an object representing the database table.
type VariantChannelListingPromotionRule struct {
	ID                      string          `boil:"id" json:"id" toml:"id" yaml:"id"`
	VariantChannelListingID string          `boil:"variant_channel_listing_id" json:"variant_channel_listing_id" toml:"variant_channel_listing_id" yaml:"variant_channel_listing_id"`
	PromotionRuleID         string          `boil:"promotion_rule_id" json:"promotion_rule_id" toml:"promotion_rule_id" yaml:"promotion_rule_id"`
	DiscountAmount          decimal.Decimal `boil:"discount_amount" json:"discount_amount" toml:"discount_amount" yaml:"discount_amount"`
	Currency                Currency        `boil:"currency" json:"currency" toml:"currency" yaml:"currency"`

	R *variantChannelListingPromotionRuleR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L variantChannelListingPromotionRuleL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var VariantChannelListingPromotionRuleColumns = struct {
	ID                      string
	VariantChannelListingID string
	PromotionRuleID         string
	DiscountAmount          string
	Currency                string
}{
	ID:                      "id",
	VariantChannelListingID: "variant_channel_listing_id",
	PromotionRuleID:         "promotion_rule_id",
	DiscountAmount:          "discount_amount",
	Currency:                "currency",
}

var VariantChannelListingPromotionRuleTableColumns = struct {
	ID                      string
	VariantChannelListingID string
	PromotionRuleID         string
	DiscountAmount          string
	Currency                string
}{
	ID:                      "variant_channel_listing_promotion_rules.id",
	VariantChannelListingID: "variant_channel_listing_promotion_rules.variant_channel_listing_id",
	PromotionRuleID:         "variant_channel_listing_promotion_rules.promotion_rule_id",
	DiscountAmount:          "variant_channel_listing_promotion_rules.discount_amount",
	Currency:                "variant_channel_listing_promotion_rules.currency",
}

// Generated where

var VariantChannelListingPromotionRuleWhere = struct {
	ID                      whereHelperstring
	VariantChannelListingID whereHelperstring
	PromotionRuleID         whereHelperstring
	DiscountAmount          whereHelperdecimal_Decimal
	Currency                whereHelperCurrency
}{
	ID:                      whereHelperstring{field: "\"variant_channel_listing_promotion_rules\".\"id\""},
	VariantChannelListingID: whereHelperstring{field: "\"variant_channel_listing_promotion_rules\".\"variant_channel_listing_id\""},
	PromotionRuleID:         whereHelperstring{field: "\"variant_channel_listing_promotion_rules\".\"promotion_rule_id\""},
	DiscountAmount:          whereHelperdecimal_Decimal{field: "\"variant_channel_listing_promotion_rules\".\"discount_amount\""},
	Currency:                whereHelperCurrency{field: "\"variant_channel_listing_promotion_rules\".\"currency\""},
}

// VariantChannelListingPromotionRuleRels is where relationship names are stored.
var VariantChannelListingPromotionRuleRels = struct {
	PromotionRule         string
	VariantChannelListing string
}{
	PromotionRule:         "PromotionRule",
	VariantChannelListing: "VariantChannelListing",
}

// variantChannelListingPromotionRuleR is where relationships are stored.
type variantChannelListingPromotionRuleR struct {
	PromotionRule         *PromotionRule                `boil:"PromotionRule" json:"PromotionRule" toml:"PromotionRule" yaml:"PromotionRule"`
	VariantChannelListing *ProductVariantChannelListing `boil:"VariantChannelListing" json:"VariantChannelListing" toml:"VariantChannelListing" yaml:"VariantChannelListing"`
}

// NewStruct creates a new relationship struct
func (*variantChannelListingPromotionRuleR) NewStruct() *variantChannelListingPromotionRuleR {
	return &variantChannelListingPromotionRuleR{}
}

func (r *variantChannelListingPromotionRuleR) GetPromotionRule() *PromotionRule {
	if r == nil {
		return nil
	}
	return r.PromotionRule
}

func (r *variantChannelListingPromotionRuleR) GetVariantChannelListing() *ProductVariantChannelListing {
	if r == nil {
		return nil
	}
	return r.VariantChannelListing
}

// variantChannelListingPromotionRuleL is where Load methods for each relationship are stored.
type variantChannelListingPromotionRuleL struct{}

var (
	variantChannelListingPromotionRuleAllColumns            = []string{"id", "variant_channel_listing_id", "promotion_rule_id", "discount_amount", "currency"}
	variantChannelListingPromotionRuleColumnsWithoutDefault = []string{"id", "variant_channel_listing_id", "promotion_rule_id", "currency"}
	variantChannelListingPromotionRuleColumnsWithDefault    = []string{"discount_amount"}
	variantChannelListingPromotionRulePrimaryKeyColumns     = []string{"id"}
	variantChannelListingPromotionRuleGeneratedColumns      = []string{}
)

type (
	// VariantChannelListingPromotionRuleSlice is an alias for a slice of pointers to VariantChannelListingPromotionRule.
	// This should almost always be used instead of []VariantChannelListingPromotionRule.
	VariantChannelListingPromotionRuleSlice []*VariantChannelListingPromotionRule

	variantChannelListingPromotionRuleQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	variantChannelListingPromotionRuleType                 = reflect.TypeOf(&VariantChannelListingPromotionRule{})
	variantChannelListingPromotionRuleMapping              = queries.MakeStructMapping(variantChannelListingPromotionRuleType)
	variantChannelListingPromotionRulePrimaryKeyMapping, _ = queries.BindMapping(variantChannelListingPromotionRuleType, variantChannelListingPromotionRuleMapping, variantChannelListingPromotionRulePrimaryKeyColumns)
	variantChannelListingPromotionRuleInsertCacheMut       sync.RWMutex
	variantChannelListingPromotionRuleInsertCache          = make(map[string]insertCache)
	variantChannelListingPromotionRuleUpdateCacheMut       sync.RWMutex
	variantChannelListingPromotionRuleUpdateCache          = make(map[string]updateCache)
	variantChannelListingPromotionRuleUpsertCacheMut       sync.RWMutex
	variantChannelListingPromotionRuleUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// One returns a single variantChannelListingPromotionRule record from the query.
func (q variantChannelListingPromotionRuleQuery) One(exec boil.Executor) (*VariantChannelListingPromotionRule, error) {
	o := &VariantChannelListingPromotionRule{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(nil, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "model: failed to execute a one query for variant_channel_listing_promotion_rules")
	}

	return o, nil
}

// All returns all VariantChannelListingPromotionRule records from the query.
func (q variantChannelListingPromotionRuleQuery) All(exec boil.Executor) (VariantChannelListingPromotionRuleSlice, error) {
	var o []*VariantChannelListingPromotionRule

	err := q.Bind(nil, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "model: failed to assign all query results to VariantChannelListingPromotionRule slice")
	}

	return o, nil
}

// Count returns the count of all VariantChannelListingPromotionRule records in the query.
func (q variantChannelListingPromotionRuleQuery) Count(exec boil.Executor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to count variant_channel_listing_promotion_rules rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q variantChannelListingPromotionRuleQuery) Exists(exec boil.Executor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "model: failed to check if variant_channel_listing_promotion_rules exists")
	}

	return count > 0, nil
}

// PromotionRule pointed to by the foreign key.
func (o *VariantChannelListingPromotionRule) PromotionRule(mods ...qm.QueryMod) promotionRuleQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.PromotionRuleID),
	}

	queryMods = append(queryMods, mods...)

	return PromotionRules(queryMods...)
}

// VariantChannelListing pointed to by the foreign key.
func (o *VariantChannelListingPromotionRule) VariantChannelListing(mods ...qm.QueryMod) productVariantChannelListingQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.VariantChannelListingID),
	}

	queryMods = append(queryMods, mods...)

	return ProductVariantChannelListings(queryMods...)
}

// LoadPromotionRule allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (variantChannelListingPromotionRuleL) LoadPromotionRule(e boil.Executor, singular bool, maybeVariantChannelListingPromotionRule interface{}, mods queries.Applicator) error {
	var slice []*VariantChannelListingPromotionRule
	var object *VariantChannelListingPromotionRule

	if singular {
		var ok bool
		object, ok = maybeVariantChannelListingPromotionRule.(*VariantChannelListingPromotionRule)
		if !ok {
			object = new(VariantChannelListingPromotionRule)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeVariantChannelListingPromotionRule)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeVariantChannelListingPromotionRule))
			}
		}
	} else {
		s, ok := maybeVariantChannelListingPromotionRule.(*[]*VariantChannelListingPromotionRule)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeVariantChannelListingPromotionRule)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeVariantChannelListingPromotionRule))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &variantChannelListingPromotionRuleR{}
		}
		args[object.PromotionRuleID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &variantChannelListingPromotionRuleR{}
			}

			args[obj.PromotionRuleID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`promotion_rules`),
		qm.WhereIn(`promotion_rules.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.Query(e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load PromotionRule")
	}

	var resultSlice []*PromotionRule
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice PromotionRule")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for promotion_rules")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for promotion_rules")
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.PromotionRule = foreign
		if foreign.R == nil {
			foreign.R = &promotionRuleR{}
		}
		foreign.R.VariantChannelListingPromotionRules = append(foreign.R.VariantChannelListingPromotionRules, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.PromotionRuleID == foreign.ID {
				local.R.PromotionRule = foreign
				if foreign.R == nil {
					foreign.R = &promotionRuleR{}
				}
				foreign.R.VariantChannelListingPromotionRules = append(foreign.R.VariantChannelListingPromotionRules, local)
				break
			}
		}
	}

	return nil
}

// LoadVariantChannelListing allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (variantChannelListingPromotionRuleL) LoadVariantChannelListing(e boil.Executor, singular bool, maybeVariantChannelListingPromotionRule interface{}, mods queries.Applicator) error {
	var slice []*VariantChannelListingPromotionRule
	var object *VariantChannelListingPromotionRule

	if singular {
		var ok bool
		object, ok = maybeVariantChannelListingPromotionRule.(*VariantChannelListingPromotionRule)
		if !ok {
			object = new(VariantChannelListingPromotionRule)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeVariantChannelListingPromotionRule)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeVariantChannelListingPromotionRule))
			}
		}
	} else {
		s, ok := maybeVariantChannelListingPromotionRule.(*[]*VariantChannelListingPromotionRule)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeVariantChannelListingPromotionRule)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeVariantChannelListingPromotionRule))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &variantChannelListingPromotionRuleR{}
		}
		args[object.VariantChannelListingID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &variantChannelListingPromotionRuleR{}
			}

			args[obj.VariantChannelListingID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`product_variant_channel_listings`),
		qm.WhereIn(`product_variant_channel_listings.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.Query(e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load ProductVariantChannelListing")
	}

	var resultSlice []*ProductVariantChannelListing
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice ProductVariantChannelListing")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for product_variant_channel_listings")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for product_variant_channel_listings")
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.VariantChannelListing = foreign
		if foreign.R == nil {
			foreign.R = &productVariantChannelListingR{}
		}
		foreign.R.VariantChannelListingVariantChannelListingPromotionRules = append(foreign.R.VariantChannelListingVariantChannelListingPromotionRules, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.VariantChannelListingID == foreign.ID {
				local.R.VariantChannelListing = foreign
				if foreign.R == nil {
					foreign.R = &productVariantChannelListingR{}
				}
				foreign.R.VariantChannelListingVariantChannelListingPromotionRules = append(foreign.R.VariantChannelListingVariantChannelListingPromotionRules, local)
				break
			}
		}
	}

	return nil
}

// SetPromotionRule of the variantChannelListingPromotionRule to the related item.
// Sets o.R.PromotionRule to related.
// Adds o to related.R.VariantChannelListingPromotionRules.
func (o *VariantChannelListingPromotionRule) SetPromotionRule(exec boil.Executor, insert bool, related *PromotionRule) error {
	var err error
	if insert {
		if err = related.Insert(exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"variant_channel_listing_promotion_rules\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"promotion_rule_id"}),
		strmangle.WhereClause("\"", "\"", 2, variantChannelListingPromotionRulePrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	if _, err = exec.Exec(updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.PromotionRuleID = related.ID
	if o.R == nil {
		o.R = &variantChannelListingPromotionRuleR{
			PromotionRule: related,
		}
	} else {
		o.R.PromotionRule = related
	}

	if related.R == nil {
		related.R = &promotionRuleR{
			VariantChannelListingPromotionRules: VariantChannelListingPromotionRuleSlice{o},
		}
	} else {
		related.R.VariantChannelListingPromotionRules = append(related.R.VariantChannelListingPromotionRules, o)
	}

	return nil
}

// SetVariantChannelListing of the variantChannelListingPromotionRule to the related item.
// Sets o.R.VariantChannelListing to related.
// Adds o to related.R.VariantChannelListingVariantChannelListingPromotionRules.
func (o *VariantChannelListingPromotionRule) SetVariantChannelListing(exec boil.Executor, insert bool, related *ProductVariantChannelListing) error {
	var err error
	if insert {
		if err = related.Insert(exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"variant_channel_listing_promotion_rules\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"variant_channel_listing_id"}),
		strmangle.WhereClause("\"", "\"", 2, variantChannelListingPromotionRulePrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	if _, err = exec.Exec(updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.VariantChannelListingID = related.ID
	if o.R == nil {
		o.R = &variantChannelListingPromotionRuleR{
			VariantChannelListing: related,
		}
	} else {
		o.R.VariantChannelListing = related
	}

	if related.R == nil {
		related.R = &productVariantChannelListingR{
			VariantChannelListingVariantChannelListingPromotionRules: VariantChannelListingPromotionRuleSlice{o},
		}
	} else {
		related.R.VariantChannelListingVariantChannelListingPromotionRules = append(related.R.VariantChannelListingVariantChannelListingPromotionRules, o)
	}

	return nil
}

// VariantChannelListingPromotionRules retrieves all the records using an executor.
func VariantChannelListingPromotionRules(mods ...qm.QueryMod) variantChannelListingPromotionRuleQuery {
	mods = append(mods, qm.From("\"variant_channel_listing_promotion_rules\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"variant_channel_listing_promotion_rules\".*"})
	}

	return variantChannelListingPromotionRuleQuery{q}
}

// FindVariantChannelListingPromotionRule retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindVariantChannelListingPromotionRule(exec boil.Executor, iD string, selectCols ...string) (*VariantChannelListingPromotionRule, error) {
	variantChannelListingPromotionRuleObj := &VariantChannelListingPromotionRule{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"variant_channel_listing_promotion_rules\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(nil, exec, variantChannelListingPromotionRuleObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "model: unable to select from variant_channel_listing_promotion_rules")
	}

	return variantChannelListingPromotionRuleObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *VariantChannelListingPromotionRule) Insert(exec boil.Executor, columns boil.Columns) error {
	if o == nil {
		return errors.New("model: no variant_channel_listing_promotion_rules provided for insertion")
	}

	var err error

	nzDefaults := queries.NonZeroDefaultSet(variantChannelListingPromotionRuleColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	variantChannelListingPromotionRuleInsertCacheMut.RLock()
	cache, cached := variantChannelListingPromotionRuleInsertCache[key]
	variantChannelListingPromotionRuleInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			variantChannelListingPromotionRuleAllColumns,
			variantChannelListingPromotionRuleColumnsWithDefault,
			variantChannelListingPromotionRuleColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(variantChannelListingPromotionRuleType, variantChannelListingPromotionRuleMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(variantChannelListingPromotionRuleType, variantChannelListingPromotionRuleMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"variant_channel_listing_promotion_rules\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"variant_channel_listing_promotion_rules\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "model: unable to insert into variant_channel_listing_promotion_rules")
	}

	if !cached {
		variantChannelListingPromotionRuleInsertCacheMut.Lock()
		variantChannelListingPromotionRuleInsertCache[key] = cache
		variantChannelListingPromotionRuleInsertCacheMut.Unlock()
	}

	return nil
}

// Update uses an executor to update the VariantChannelListingPromotionRule.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *VariantChannelListingPromotionRule) Update(exec boil.Executor, columns boil.Columns) (int64, error) {
	var err error
	key := makeCacheKey(columns, nil)
	variantChannelListingPromotionRuleUpdateCacheMut.RLock()
	cache, cached := variantChannelListingPromotionRuleUpdateCache[key]
	variantChannelListingPromotionRuleUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			variantChannelListingPromotionRuleAllColumns,
			variantChannelListingPromotionRulePrimaryKeyColumns,
		)
		if len(wl) == 0 {
			return 0, errors.New("model: unable to update variant_channel_listing_promotion_rules, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"variant_channel_listing_promotion_rules\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, variantChannelListingPromotionRulePrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(variantChannelListingPromotionRuleType, variantChannelListingPromotionRuleMapping, append(wl, variantChannelListingPromotionRulePrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	var result sql.Result
	result, err = exec.Exec(cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to update variant_channel_listing_promotion_rules row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by update for variant_channel_listing_promotion_rules")
	}

	if !cached {
		variantChannelListingPromotionRuleUpdateCacheMut.Lock()
		variantChannelListingPromotionRuleUpdateCache[key] = cache
		variantChannelListingPromotionRuleUpdateCacheMut.Unlock()
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values.
func (q variantChannelListingPromotionRuleQuery) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to update all for variant_channel_listing_promotion_rules")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to retrieve rows affected for variant_channel_listing_promotion_rules")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o VariantChannelListingPromotionRuleSlice) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("model: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), variantChannelListingPromotionRulePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"variant_channel_listing_promotion_rules\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, variantChannelListingPromotionRulePrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to update all in variantChannelListingPromotionRule slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to retrieve rows affected all in update all variantChannelListingPromotionRule")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *VariantChannelListingPromotionRule) Upsert(exec boil.Executor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("model: no variant_channel_listing_promotion_rules provided for upsert")
	}

	nzDefaults := queries.NonZeroDefaultSet(variantChannelListingPromotionRuleColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	variantChannelListingPromotionRuleUpsertCacheMut.RLock()
	cache, cached := variantChannelListingPromotionRuleUpsertCache[key]
	variantChannelListingPromotionRuleUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			variantChannelListingPromotionRuleAllColumns,
			variantChannelListingPromotionRuleColumnsWithDefault,
			variantChannelListingPromotionRuleColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			variantChannelListingPromotionRuleAllColumns,
			variantChannelListingPromotionRulePrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("model: unable to upsert variant_channel_listing_promotion_rules, could not build update column list")
		}

		ret := strmangle.SetComplement(variantChannelListingPromotionRuleAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(variantChannelListingPromotionRulePrimaryKeyColumns) == 0 {
				return errors.New("model: unable to upsert variant_channel_listing_promotion_rules, could not build conflict column list")
			}

			conflict = make([]string, len(variantChannelListingPromotionRulePrimaryKeyColumns))
			copy(conflict, variantChannelListingPromotionRulePrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"variant_channel_listing_promotion_rules\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(variantChannelListingPromotionRuleType, variantChannelListingPromotionRuleMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(variantChannelListingPromotionRuleType, variantChannelListingPromotionRuleMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "model: unable to upsert variant_channel_listing_promotion_rules")
	}

	if !cached {
		variantChannelListingPromotionRuleUpsertCacheMut.Lock()
		variantChannelListingPromotionRuleUpsertCache[key] = cache
		variantChannelListingPromotionRuleUpsertCacheMut.Unlock()
	}

	return nil
}

// Delete deletes a single VariantChannelListingPromotionRule record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *VariantChannelListingPromotionRule) Delete(exec boil.Executor) (int64, error) {
	if o == nil {
		return 0, errors.New("model: no VariantChannelListingPromotionRule provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), variantChannelListingPromotionRulePrimaryKeyMapping)
	sql := "DELETE FROM \"variant_channel_listing_promotion_rules\" WHERE \"id\"=$1"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to delete from variant_channel_listing_promotion_rules")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by delete for variant_channel_listing_promotion_rules")
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q variantChannelListingPromotionRuleQuery) DeleteAll(exec boil.Executor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("model: no variantChannelListingPromotionRuleQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to delete all from variant_channel_listing_promotion_rules")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by deleteall for variant_channel_listing_promotion_rules")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o VariantChannelListingPromotionRuleSlice) DeleteAll(exec boil.Executor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), variantChannelListingPromotionRulePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"variant_channel_listing_promotion_rules\" WHERE " +
		strmangle.WhereInClause(string(dialect.LQ), string(dialect.RQ), 1, variantChannelListingPromotionRulePrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to delete all from variantChannelListingPromotionRule slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by deleteall for variant_channel_listing_promotion_rules")
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *VariantChannelListingPromotionRule) Reload(exec boil.Executor) error {
	ret, err := FindVariantChannelListingPromotionRule(exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *VariantChannelListingPromotionRuleSlice) ReloadAll(exec boil.Executor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := VariantChannelListingPromotionRuleSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), variantChannelListingPromotionRulePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"variant_channel_listing_promotion_rules\".* FROM \"variant_channel_listing_promotion_rules\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, variantChannelListingPromotionRulePrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(nil, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "model: unable to reload all in VariantChannelListingPromotionRuleSlice")
	}

	*o = slice

	return nil
}

// VariantChannelListingPromotionRuleExists checks if the VariantChannelListingPromotionRule row exists.
func VariantChannelListingPromotionRuleExists(exec boil.Executor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"variant_channel_listing_promotion_rules\" where \"id\"=$1 limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, iD)
	}
	row := exec.QueryRow(sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "model: unable to check if variant_channel_listing_promotion_rules exists")
	}

	return exists, nil
}

// Exists checks if the VariantChannelListingPromotionRule row exists.
func (o *VariantChannelListingPromotionRule) Exists(exec boil.Executor) (bool, error) {
	return VariantChannelListingPromotionRuleExists(exec, o.ID)
}
