// Code generated by SQLBoiler 4.15.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package model

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// WishlistItem is an object representing the database table.
type WishlistItem struct {
	ID         string `boil:"id" json:"id" toml:"id" yaml:"id"`
	WishlistID string `boil:"wishlist_id" json:"wishlist_id" toml:"wishlist_id" yaml:"wishlist_id"`
	ProductID  string `boil:"product_id" json:"product_id" toml:"product_id" yaml:"product_id"`
	CreatedAt  int64  `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`

	R *wishlistItemR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L wishlistItemL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var WishlistItemColumns = struct {
	ID         string
	WishlistID string
	ProductID  string
	CreatedAt  string
}{
	ID:         "id",
	WishlistID: "wishlist_id",
	ProductID:  "product_id",
	CreatedAt:  "created_at",
}

var WishlistItemTableColumns = struct {
	ID         string
	WishlistID string
	ProductID  string
	CreatedAt  string
}{
	ID:         "wishlist_items.id",
	WishlistID: "wishlist_items.wishlist_id",
	ProductID:  "wishlist_items.product_id",
	CreatedAt:  "wishlist_items.created_at",
}

// Generated where

var WishlistItemWhere = struct {
	ID         whereHelperstring
	WishlistID whereHelperstring
	ProductID  whereHelperstring
	CreatedAt  whereHelperint64
}{
	ID:         whereHelperstring{field: "\"wishlist_items\".\"id\""},
	WishlistID: whereHelperstring{field: "\"wishlist_items\".\"wishlist_id\""},
	ProductID:  whereHelperstring{field: "\"wishlist_items\".\"product_id\""},
	CreatedAt:  whereHelperint64{field: "\"wishlist_items\".\"created_at\""},
}

// WishlistItemRels is where relationship names are stored.
var WishlistItemRels = struct {
	Product                     string
	Wishlist                    string
	WishlistItemProductVariants string
}{
	Product:                     "Product",
	Wishlist:                    "Wishlist",
	WishlistItemProductVariants: "WishlistItemProductVariants",
}

// wishlistItemR is where relationships are stored.
type wishlistItemR struct {
	Product                     *ProductVariant                 `boil:"Product" json:"Product" toml:"Product" yaml:"Product"`
	Wishlist                    *Wishlist                       `boil:"Wishlist" json:"Wishlist" toml:"Wishlist" yaml:"Wishlist"`
	WishlistItemProductVariants WishlistItemProductVariantSlice `boil:"WishlistItemProductVariants" json:"WishlistItemProductVariants" toml:"WishlistItemProductVariants" yaml:"WishlistItemProductVariants"`
}

// NewStruct creates a new relationship struct
func (*wishlistItemR) NewStruct() *wishlistItemR {
	return &wishlistItemR{}
}

func (r *wishlistItemR) GetProduct() *ProductVariant {
	if r == nil {
		return nil
	}
	return r.Product
}

func (r *wishlistItemR) GetWishlist() *Wishlist {
	if r == nil {
		return nil
	}
	return r.Wishlist
}

func (r *wishlistItemR) GetWishlistItemProductVariants() WishlistItemProductVariantSlice {
	if r == nil {
		return nil
	}
	return r.WishlistItemProductVariants
}

// wishlistItemL is where Load methods for each relationship are stored.
type wishlistItemL struct{}

var (
	wishlistItemAllColumns            = []string{"id", "wishlist_id", "product_id", "created_at"}
	wishlistItemColumnsWithoutDefault = []string{"wishlist_id", "product_id", "created_at"}
	wishlistItemColumnsWithDefault    = []string{"id"}
	wishlistItemPrimaryKeyColumns     = []string{"id"}
	wishlistItemGeneratedColumns      = []string{}
)

type (
	// WishlistItemSlice is an alias for a slice of pointers to WishlistItem.
	// This should almost always be used instead of []WishlistItem.
	WishlistItemSlice []*WishlistItem

	wishlistItemQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	wishlistItemType                 = reflect.TypeOf(&WishlistItem{})
	wishlistItemMapping              = queries.MakeStructMapping(wishlistItemType)
	wishlistItemPrimaryKeyMapping, _ = queries.BindMapping(wishlistItemType, wishlistItemMapping, wishlistItemPrimaryKeyColumns)
	wishlistItemInsertCacheMut       sync.RWMutex
	wishlistItemInsertCache          = make(map[string]insertCache)
	wishlistItemUpdateCacheMut       sync.RWMutex
	wishlistItemUpdateCache          = make(map[string]updateCache)
	wishlistItemUpsertCacheMut       sync.RWMutex
	wishlistItemUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// One returns a single wishlistItem record from the query.
func (q wishlistItemQuery) One(ctx context.Context, exec boil.ContextExecutor) (*WishlistItem, error) {
	o := &WishlistItem{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "model: failed to execute a one query for wishlist_items")
	}

	return o, nil
}

// All returns all WishlistItem records from the query.
func (q wishlistItemQuery) All(ctx context.Context, exec boil.ContextExecutor) (WishlistItemSlice, error) {
	var o []*WishlistItem

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "model: failed to assign all query results to WishlistItem slice")
	}

	return o, nil
}

// Count returns the count of all WishlistItem records in the query.
func (q wishlistItemQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to count wishlist_items rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q wishlistItemQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "model: failed to check if wishlist_items exists")
	}

	return count > 0, nil
}

// Product pointed to by the foreign key.
func (o *WishlistItem) Product(mods ...qm.QueryMod) productVariantQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.ProductID),
	}

	queryMods = append(queryMods, mods...)

	return ProductVariants(queryMods...)
}

// Wishlist pointed to by the foreign key.
func (o *WishlistItem) Wishlist(mods ...qm.QueryMod) wishlistQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.WishlistID),
	}

	queryMods = append(queryMods, mods...)

	return Wishlists(queryMods...)
}

// WishlistItemProductVariants retrieves all the wishlist_item_product_variant's WishlistItemProductVariants with an executor.
func (o *WishlistItem) WishlistItemProductVariants(mods ...qm.QueryMod) wishlistItemProductVariantQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"wishlist_item_product_variants\".\"wishlist_item_id\"=?", o.ID),
	)

	return WishlistItemProductVariants(queryMods...)
}

// LoadProduct allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (wishlistItemL) LoadProduct(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWishlistItem interface{}, mods queries.Applicator) error {
	var slice []*WishlistItem
	var object *WishlistItem

	if singular {
		var ok bool
		object, ok = maybeWishlistItem.(*WishlistItem)
		if !ok {
			object = new(WishlistItem)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWishlistItem)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWishlistItem))
			}
		}
	} else {
		s, ok := maybeWishlistItem.(*[]*WishlistItem)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWishlistItem)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWishlistItem))
			}
		}
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &wishlistItemR{}
		}
		args = append(args, object.ProductID)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &wishlistItemR{}
			}

			for _, a := range args {
				if a == obj.ProductID {
					continue Outer
				}
			}

			args = append(args, obj.ProductID)

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`product_variants`),
		qm.WhereIn(`product_variants.id in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load ProductVariant")
	}

	var resultSlice []*ProductVariant
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice ProductVariant")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for product_variants")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for product_variants")
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Product = foreign
		if foreign.R == nil {
			foreign.R = &productVariantR{}
		}
		foreign.R.ProductWishlistItems = append(foreign.R.ProductWishlistItems, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.ProductID == foreign.ID {
				local.R.Product = foreign
				if foreign.R == nil {
					foreign.R = &productVariantR{}
				}
				foreign.R.ProductWishlistItems = append(foreign.R.ProductWishlistItems, local)
				break
			}
		}
	}

	return nil
}

// LoadWishlist allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (wishlistItemL) LoadWishlist(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWishlistItem interface{}, mods queries.Applicator) error {
	var slice []*WishlistItem
	var object *WishlistItem

	if singular {
		var ok bool
		object, ok = maybeWishlistItem.(*WishlistItem)
		if !ok {
			object = new(WishlistItem)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWishlistItem)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWishlistItem))
			}
		}
	} else {
		s, ok := maybeWishlistItem.(*[]*WishlistItem)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWishlistItem)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWishlistItem))
			}
		}
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &wishlistItemR{}
		}
		args = append(args, object.WishlistID)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &wishlistItemR{}
			}

			for _, a := range args {
				if a == obj.WishlistID {
					continue Outer
				}
			}

			args = append(args, obj.WishlistID)

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`wishlists`),
		qm.WhereIn(`wishlists.id in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Wishlist")
	}

	var resultSlice []*Wishlist
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Wishlist")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for wishlists")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for wishlists")
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Wishlist = foreign
		if foreign.R == nil {
			foreign.R = &wishlistR{}
		}
		foreign.R.WishlistItems = append(foreign.R.WishlistItems, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.WishlistID == foreign.ID {
				local.R.Wishlist = foreign
				if foreign.R == nil {
					foreign.R = &wishlistR{}
				}
				foreign.R.WishlistItems = append(foreign.R.WishlistItems, local)
				break
			}
		}
	}

	return nil
}

// LoadWishlistItemProductVariants allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (wishlistItemL) LoadWishlistItemProductVariants(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWishlistItem interface{}, mods queries.Applicator) error {
	var slice []*WishlistItem
	var object *WishlistItem

	if singular {
		var ok bool
		object, ok = maybeWishlistItem.(*WishlistItem)
		if !ok {
			object = new(WishlistItem)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWishlistItem)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWishlistItem))
			}
		}
	} else {
		s, ok := maybeWishlistItem.(*[]*WishlistItem)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWishlistItem)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWishlistItem))
			}
		}
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &wishlistItemR{}
		}
		args = append(args, object.ID)
	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &wishlistItemR{}
			}

			for _, a := range args {
				if a == obj.ID {
					continue Outer
				}
			}

			args = append(args, obj.ID)
		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`wishlist_item_product_variants`),
		qm.WhereIn(`wishlist_item_product_variants.wishlist_item_id in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load wishlist_item_product_variants")
	}

	var resultSlice []*WishlistItemProductVariant
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice wishlist_item_product_variants")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on wishlist_item_product_variants")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for wishlist_item_product_variants")
	}

	if singular {
		object.R.WishlistItemProductVariants = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &wishlistItemProductVariantR{}
			}
			foreign.R.WishlistItem = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.WishlistItemID {
				local.R.WishlistItemProductVariants = append(local.R.WishlistItemProductVariants, foreign)
				if foreign.R == nil {
					foreign.R = &wishlistItemProductVariantR{}
				}
				foreign.R.WishlistItem = local
				break
			}
		}
	}

	return nil
}

// SetProduct of the wishlistItem to the related item.
// Sets o.R.Product to related.
// Adds o to related.R.ProductWishlistItems.
func (o *WishlistItem) SetProduct(ctx context.Context, exec boil.ContextExecutor, insert bool, related *ProductVariant) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"wishlist_items\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"product_id"}),
		strmangle.WhereClause("\"", "\"", 2, wishlistItemPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.ProductID = related.ID
	if o.R == nil {
		o.R = &wishlistItemR{
			Product: related,
		}
	} else {
		o.R.Product = related
	}

	if related.R == nil {
		related.R = &productVariantR{
			ProductWishlistItems: WishlistItemSlice{o},
		}
	} else {
		related.R.ProductWishlistItems = append(related.R.ProductWishlistItems, o)
	}

	return nil
}

// SetWishlist of the wishlistItem to the related item.
// Sets o.R.Wishlist to related.
// Adds o to related.R.WishlistItems.
func (o *WishlistItem) SetWishlist(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Wishlist) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"wishlist_items\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"wishlist_id"}),
		strmangle.WhereClause("\"", "\"", 2, wishlistItemPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.WishlistID = related.ID
	if o.R == nil {
		o.R = &wishlistItemR{
			Wishlist: related,
		}
	} else {
		o.R.Wishlist = related
	}

	if related.R == nil {
		related.R = &wishlistR{
			WishlistItems: WishlistItemSlice{o},
		}
	} else {
		related.R.WishlistItems = append(related.R.WishlistItems, o)
	}

	return nil
}

// AddWishlistItemProductVariants adds the given related objects to the existing relationships
// of the wishlist_item, optionally inserting them as new records.
// Appends related to o.R.WishlistItemProductVariants.
// Sets related.R.WishlistItem appropriately.
func (o *WishlistItem) AddWishlistItemProductVariants(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*WishlistItemProductVariant) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.WishlistItemID = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"wishlist_item_product_variants\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"wishlist_item_id"}),
				strmangle.WhereClause("\"", "\"", 2, wishlistItemProductVariantPrimaryKeyColumns),
			)
			values := []interface{}{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.WishlistItemID = o.ID
		}
	}

	if o.R == nil {
		o.R = &wishlistItemR{
			WishlistItemProductVariants: related,
		}
	} else {
		o.R.WishlistItemProductVariants = append(o.R.WishlistItemProductVariants, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &wishlistItemProductVariantR{
				WishlistItem: o,
			}
		} else {
			rel.R.WishlistItem = o
		}
	}
	return nil
}

// WishlistItems retrieves all the records using an executor.
func WishlistItems(mods ...qm.QueryMod) wishlistItemQuery {
	mods = append(mods, qm.From("\"wishlist_items\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"wishlist_items\".*"})
	}

	return wishlistItemQuery{q}
}

// FindWishlistItem retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindWishlistItem(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*WishlistItem, error) {
	wishlistItemObj := &WishlistItem{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"wishlist_items\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, wishlistItemObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "model: unable to select from wishlist_items")
	}

	return wishlistItemObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *WishlistItem) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("model: no wishlist_items provided for insertion")
	}

	var err error

	nzDefaults := queries.NonZeroDefaultSet(wishlistItemColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	wishlistItemInsertCacheMut.RLock()
	cache, cached := wishlistItemInsertCache[key]
	wishlistItemInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			wishlistItemAllColumns,
			wishlistItemColumnsWithDefault,
			wishlistItemColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(wishlistItemType, wishlistItemMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(wishlistItemType, wishlistItemMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"wishlist_items\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"wishlist_items\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "model: unable to insert into wishlist_items")
	}

	if !cached {
		wishlistItemInsertCacheMut.Lock()
		wishlistItemInsertCache[key] = cache
		wishlistItemInsertCacheMut.Unlock()
	}

	return nil
}

// Update uses an executor to update the WishlistItem.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *WishlistItem) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	key := makeCacheKey(columns, nil)
	wishlistItemUpdateCacheMut.RLock()
	cache, cached := wishlistItemUpdateCache[key]
	wishlistItemUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			wishlistItemAllColumns,
			wishlistItemPrimaryKeyColumns,
		)
		if len(wl) == 0 {
			return 0, errors.New("model: unable to update wishlist_items, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"wishlist_items\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, wishlistItemPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(wishlistItemType, wishlistItemMapping, append(wl, wishlistItemPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to update wishlist_items row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by update for wishlist_items")
	}

	if !cached {
		wishlistItemUpdateCacheMut.Lock()
		wishlistItemUpdateCache[key] = cache
		wishlistItemUpdateCacheMut.Unlock()
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values.
func (q wishlistItemQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to update all for wishlist_items")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to retrieve rows affected for wishlist_items")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o WishlistItemSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("model: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), wishlistItemPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"wishlist_items\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, wishlistItemPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to update all in wishlistItem slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to retrieve rows affected all in update all wishlistItem")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *WishlistItem) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("model: no wishlist_items provided for upsert")
	}

	nzDefaults := queries.NonZeroDefaultSet(wishlistItemColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	wishlistItemUpsertCacheMut.RLock()
	cache, cached := wishlistItemUpsertCache[key]
	wishlistItemUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			wishlistItemAllColumns,
			wishlistItemColumnsWithDefault,
			wishlistItemColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			wishlistItemAllColumns,
			wishlistItemPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("model: unable to upsert wishlist_items, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(wishlistItemPrimaryKeyColumns))
			copy(conflict, wishlistItemPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"wishlist_items\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(wishlistItemType, wishlistItemMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(wishlistItemType, wishlistItemMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "model: unable to upsert wishlist_items")
	}

	if !cached {
		wishlistItemUpsertCacheMut.Lock()
		wishlistItemUpsertCache[key] = cache
		wishlistItemUpsertCacheMut.Unlock()
	}

	return nil
}

// Delete deletes a single WishlistItem record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *WishlistItem) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("model: no WishlistItem provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), wishlistItemPrimaryKeyMapping)
	sql := "DELETE FROM \"wishlist_items\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to delete from wishlist_items")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by delete for wishlist_items")
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q wishlistItemQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("model: no wishlistItemQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to delete all from wishlist_items")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by deleteall for wishlist_items")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o WishlistItemSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), wishlistItemPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"wishlist_items\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, wishlistItemPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to delete all from wishlistItem slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by deleteall for wishlist_items")
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *WishlistItem) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindWishlistItem(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *WishlistItemSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := WishlistItemSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), wishlistItemPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"wishlist_items\".* FROM \"wishlist_items\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, wishlistItemPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "model: unable to reload all in WishlistItemSlice")
	}

	*o = slice

	return nil
}

// WishlistItemExists checks if the WishlistItem row exists.
func WishlistItemExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"wishlist_items\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "model: unable to check if wishlist_items exists")
	}

	return exists, nil
}

// Exists checks if the WishlistItem row exists.
func (o *WishlistItem) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return WishlistItemExists(ctx, exec, o.ID)
}
