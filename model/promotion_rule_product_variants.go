// Code generated by SQLBoiler 4.17.1 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package model

import (
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// PromotionRuleProductVariant is an object representing the database table.
type PromotionRuleProductVariant struct {
	ID               string `boil:"id" json:"id" toml:"id" yaml:"id"`
	PromotionRuleID  string `boil:"promotion_rule_id" json:"promotion_rule_id" toml:"promotion_rule_id" yaml:"promotion_rule_id"`
	ProductVariantID string `boil:"product_variant_id" json:"product_variant_id" toml:"product_variant_id" yaml:"product_variant_id"`

	R *promotionRuleProductVariantR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L promotionRuleProductVariantL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var PromotionRuleProductVariantColumns = struct {
	ID               string
	PromotionRuleID  string
	ProductVariantID string
}{
	ID:               "id",
	PromotionRuleID:  "promotion_rule_id",
	ProductVariantID: "product_variant_id",
}

var PromotionRuleProductVariantTableColumns = struct {
	ID               string
	PromotionRuleID  string
	ProductVariantID string
}{
	ID:               "promotion_rule_product_variants.id",
	PromotionRuleID:  "promotion_rule_product_variants.promotion_rule_id",
	ProductVariantID: "promotion_rule_product_variants.product_variant_id",
}

// Generated where

var PromotionRuleProductVariantWhere = struct {
	ID               whereHelperstring
	PromotionRuleID  whereHelperstring
	ProductVariantID whereHelperstring
}{
	ID:               whereHelperstring{field: "\"promotion_rule_product_variants\".\"id\""},
	PromotionRuleID:  whereHelperstring{field: "\"promotion_rule_product_variants\".\"promotion_rule_id\""},
	ProductVariantID: whereHelperstring{field: "\"promotion_rule_product_variants\".\"product_variant_id\""},
}

// PromotionRuleProductVariantRels is where relationship names are stored.
var PromotionRuleProductVariantRels = struct {
	ProductVariant string
	PromotionRule  string
}{
	ProductVariant: "ProductVariant",
	PromotionRule:  "PromotionRule",
}

// promotionRuleProductVariantR is where relationships are stored.
type promotionRuleProductVariantR struct {
	ProductVariant *ProductVariant `boil:"ProductVariant" json:"ProductVariant" toml:"ProductVariant" yaml:"ProductVariant"`
	PromotionRule  *PromotionRule  `boil:"PromotionRule" json:"PromotionRule" toml:"PromotionRule" yaml:"PromotionRule"`
}

// NewStruct creates a new relationship struct
func (*promotionRuleProductVariantR) NewStruct() *promotionRuleProductVariantR {
	return &promotionRuleProductVariantR{}
}

func (r *promotionRuleProductVariantR) GetProductVariant() *ProductVariant {
	if r == nil {
		return nil
	}
	return r.ProductVariant
}

func (r *promotionRuleProductVariantR) GetPromotionRule() *PromotionRule {
	if r == nil {
		return nil
	}
	return r.PromotionRule
}

// promotionRuleProductVariantL is where Load methods for each relationship are stored.
type promotionRuleProductVariantL struct{}

var (
	promotionRuleProductVariantAllColumns            = []string{"id", "promotion_rule_id", "product_variant_id"}
	promotionRuleProductVariantColumnsWithoutDefault = []string{"id", "promotion_rule_id", "product_variant_id"}
	promotionRuleProductVariantColumnsWithDefault    = []string{}
	promotionRuleProductVariantPrimaryKeyColumns     = []string{"id"}
	promotionRuleProductVariantGeneratedColumns      = []string{}
)

type (
	// PromotionRuleProductVariantSlice is an alias for a slice of pointers to PromotionRuleProductVariant.
	// This should almost always be used instead of []PromotionRuleProductVariant.
	PromotionRuleProductVariantSlice []*PromotionRuleProductVariant

	promotionRuleProductVariantQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	promotionRuleProductVariantType                 = reflect.TypeOf(&PromotionRuleProductVariant{})
	promotionRuleProductVariantMapping              = queries.MakeStructMapping(promotionRuleProductVariantType)
	promotionRuleProductVariantPrimaryKeyMapping, _ = queries.BindMapping(promotionRuleProductVariantType, promotionRuleProductVariantMapping, promotionRuleProductVariantPrimaryKeyColumns)
	promotionRuleProductVariantInsertCacheMut       sync.RWMutex
	promotionRuleProductVariantInsertCache          = make(map[string]insertCache)
	promotionRuleProductVariantUpdateCacheMut       sync.RWMutex
	promotionRuleProductVariantUpdateCache          = make(map[string]updateCache)
	promotionRuleProductVariantUpsertCacheMut       sync.RWMutex
	promotionRuleProductVariantUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// One returns a single promotionRuleProductVariant record from the query.
func (q promotionRuleProductVariantQuery) One(exec boil.Executor) (*PromotionRuleProductVariant, error) {
	o := &PromotionRuleProductVariant{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(nil, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "model: failed to execute a one query for promotion_rule_product_variants")
	}

	return o, nil
}

// All returns all PromotionRuleProductVariant records from the query.
func (q promotionRuleProductVariantQuery) All(exec boil.Executor) (PromotionRuleProductVariantSlice, error) {
	var o []*PromotionRuleProductVariant

	err := q.Bind(nil, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "model: failed to assign all query results to PromotionRuleProductVariant slice")
	}

	return o, nil
}

// Count returns the count of all PromotionRuleProductVariant records in the query.
func (q promotionRuleProductVariantQuery) Count(exec boil.Executor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to count promotion_rule_product_variants rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q promotionRuleProductVariantQuery) Exists(exec boil.Executor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "model: failed to check if promotion_rule_product_variants exists")
	}

	return count > 0, nil
}

// ProductVariant pointed to by the foreign key.
func (o *PromotionRuleProductVariant) ProductVariant(mods ...qm.QueryMod) productVariantQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.ProductVariantID),
	}

	queryMods = append(queryMods, mods...)

	return ProductVariants(queryMods...)
}

// PromotionRule pointed to by the foreign key.
func (o *PromotionRuleProductVariant) PromotionRule(mods ...qm.QueryMod) promotionRuleQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.PromotionRuleID),
	}

	queryMods = append(queryMods, mods...)

	return PromotionRules(queryMods...)
}

// LoadProductVariant allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (promotionRuleProductVariantL) LoadProductVariant(e boil.Executor, singular bool, maybePromotionRuleProductVariant interface{}, mods queries.Applicator) error {
	var slice []*PromotionRuleProductVariant
	var object *PromotionRuleProductVariant

	if singular {
		var ok bool
		object, ok = maybePromotionRuleProductVariant.(*PromotionRuleProductVariant)
		if !ok {
			object = new(PromotionRuleProductVariant)
			ok = queries.SetFromEmbeddedStruct(&object, &maybePromotionRuleProductVariant)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybePromotionRuleProductVariant))
			}
		}
	} else {
		s, ok := maybePromotionRuleProductVariant.(*[]*PromotionRuleProductVariant)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybePromotionRuleProductVariant)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybePromotionRuleProductVariant))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &promotionRuleProductVariantR{}
		}
		args[object.ProductVariantID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &promotionRuleProductVariantR{}
			}

			args[obj.ProductVariantID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`product_variants`),
		qm.WhereIn(`product_variants.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.Query(e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load ProductVariant")
	}

	var resultSlice []*ProductVariant
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice ProductVariant")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for product_variants")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for product_variants")
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.ProductVariant = foreign
		if foreign.R == nil {
			foreign.R = &productVariantR{}
		}
		foreign.R.PromotionRuleProductVariants = append(foreign.R.PromotionRuleProductVariants, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.ProductVariantID == foreign.ID {
				local.R.ProductVariant = foreign
				if foreign.R == nil {
					foreign.R = &productVariantR{}
				}
				foreign.R.PromotionRuleProductVariants = append(foreign.R.PromotionRuleProductVariants, local)
				break
			}
		}
	}

	return nil
}

// LoadPromotionRule allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (promotionRuleProductVariantL) LoadPromotionRule(e boil.Executor, singular bool, maybePromotionRuleProductVariant interface{}, mods queries.Applicator) error {
	var slice []*PromotionRuleProductVariant
	var object *PromotionRuleProductVariant

	if singular {
		var ok bool
		object, ok = maybePromotionRuleProductVariant.(*PromotionRuleProductVariant)
		if !ok {
			object = new(PromotionRuleProductVariant)
			ok = queries.SetFromEmbeddedStruct(&object, &maybePromotionRuleProductVariant)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybePromotionRuleProductVariant))
			}
		}
	} else {
		s, ok := maybePromotionRuleProductVariant.(*[]*PromotionRuleProductVariant)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybePromotionRuleProductVariant)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybePromotionRuleProductVariant))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &promotionRuleProductVariantR{}
		}
		args[object.PromotionRuleID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &promotionRuleProductVariantR{}
			}

			args[obj.PromotionRuleID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`promotion_rules`),
		qm.WhereIn(`promotion_rules.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.Query(e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load PromotionRule")
	}

	var resultSlice []*PromotionRule
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice PromotionRule")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for promotion_rules")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for promotion_rules")
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.PromotionRule = foreign
		if foreign.R == nil {
			foreign.R = &promotionRuleR{}
		}
		foreign.R.PromotionRuleProductVariants = append(foreign.R.PromotionRuleProductVariants, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.PromotionRuleID == foreign.ID {
				local.R.PromotionRule = foreign
				if foreign.R == nil {
					foreign.R = &promotionRuleR{}
				}
				foreign.R.PromotionRuleProductVariants = append(foreign.R.PromotionRuleProductVariants, local)
				break
			}
		}
	}

	return nil
}

// SetProductVariant of the promotionRuleProductVariant to the related item.
// Sets o.R.ProductVariant to related.
// Adds o to related.R.PromotionRuleProductVariants.
func (o *PromotionRuleProductVariant) SetProductVariant(exec boil.Executor, insert bool, related *ProductVariant) error {
	var err error
	if insert {
		if err = related.Insert(exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"promotion_rule_product_variants\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"product_variant_id"}),
		strmangle.WhereClause("\"", "\"", 2, promotionRuleProductVariantPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	if _, err = exec.Exec(updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.ProductVariantID = related.ID
	if o.R == nil {
		o.R = &promotionRuleProductVariantR{
			ProductVariant: related,
		}
	} else {
		o.R.ProductVariant = related
	}

	if related.R == nil {
		related.R = &productVariantR{
			PromotionRuleProductVariants: PromotionRuleProductVariantSlice{o},
		}
	} else {
		related.R.PromotionRuleProductVariants = append(related.R.PromotionRuleProductVariants, o)
	}

	return nil
}

// SetPromotionRule of the promotionRuleProductVariant to the related item.
// Sets o.R.PromotionRule to related.
// Adds o to related.R.PromotionRuleProductVariants.
func (o *PromotionRuleProductVariant) SetPromotionRule(exec boil.Executor, insert bool, related *PromotionRule) error {
	var err error
	if insert {
		if err = related.Insert(exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"promotion_rule_product_variants\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"promotion_rule_id"}),
		strmangle.WhereClause("\"", "\"", 2, promotionRuleProductVariantPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	if _, err = exec.Exec(updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.PromotionRuleID = related.ID
	if o.R == nil {
		o.R = &promotionRuleProductVariantR{
			PromotionRule: related,
		}
	} else {
		o.R.PromotionRule = related
	}

	if related.R == nil {
		related.R = &promotionRuleR{
			PromotionRuleProductVariants: PromotionRuleProductVariantSlice{o},
		}
	} else {
		related.R.PromotionRuleProductVariants = append(related.R.PromotionRuleProductVariants, o)
	}

	return nil
}

// PromotionRuleProductVariants retrieves all the records using an executor.
func PromotionRuleProductVariants(mods ...qm.QueryMod) promotionRuleProductVariantQuery {
	mods = append(mods, qm.From("\"promotion_rule_product_variants\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"promotion_rule_product_variants\".*"})
	}

	return promotionRuleProductVariantQuery{q}
}

// FindPromotionRuleProductVariant retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindPromotionRuleProductVariant(exec boil.Executor, iD string, selectCols ...string) (*PromotionRuleProductVariant, error) {
	promotionRuleProductVariantObj := &PromotionRuleProductVariant{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"promotion_rule_product_variants\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(nil, exec, promotionRuleProductVariantObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "model: unable to select from promotion_rule_product_variants")
	}

	return promotionRuleProductVariantObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *PromotionRuleProductVariant) Insert(exec boil.Executor, columns boil.Columns) error {
	if o == nil {
		return errors.New("model: no promotion_rule_product_variants provided for insertion")
	}

	var err error

	nzDefaults := queries.NonZeroDefaultSet(promotionRuleProductVariantColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	promotionRuleProductVariantInsertCacheMut.RLock()
	cache, cached := promotionRuleProductVariantInsertCache[key]
	promotionRuleProductVariantInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			promotionRuleProductVariantAllColumns,
			promotionRuleProductVariantColumnsWithDefault,
			promotionRuleProductVariantColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(promotionRuleProductVariantType, promotionRuleProductVariantMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(promotionRuleProductVariantType, promotionRuleProductVariantMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"promotion_rule_product_variants\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"promotion_rule_product_variants\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "model: unable to insert into promotion_rule_product_variants")
	}

	if !cached {
		promotionRuleProductVariantInsertCacheMut.Lock()
		promotionRuleProductVariantInsertCache[key] = cache
		promotionRuleProductVariantInsertCacheMut.Unlock()
	}

	return nil
}

// Update uses an executor to update the PromotionRuleProductVariant.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *PromotionRuleProductVariant) Update(exec boil.Executor, columns boil.Columns) (int64, error) {
	var err error
	key := makeCacheKey(columns, nil)
	promotionRuleProductVariantUpdateCacheMut.RLock()
	cache, cached := promotionRuleProductVariantUpdateCache[key]
	promotionRuleProductVariantUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			promotionRuleProductVariantAllColumns,
			promotionRuleProductVariantPrimaryKeyColumns,
		)
		if len(wl) == 0 {
			return 0, errors.New("model: unable to update promotion_rule_product_variants, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"promotion_rule_product_variants\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, promotionRuleProductVariantPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(promotionRuleProductVariantType, promotionRuleProductVariantMapping, append(wl, promotionRuleProductVariantPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	var result sql.Result
	result, err = exec.Exec(cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to update promotion_rule_product_variants row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by update for promotion_rule_product_variants")
	}

	if !cached {
		promotionRuleProductVariantUpdateCacheMut.Lock()
		promotionRuleProductVariantUpdateCache[key] = cache
		promotionRuleProductVariantUpdateCacheMut.Unlock()
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values.
func (q promotionRuleProductVariantQuery) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to update all for promotion_rule_product_variants")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to retrieve rows affected for promotion_rule_product_variants")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o PromotionRuleProductVariantSlice) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("model: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), promotionRuleProductVariantPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"promotion_rule_product_variants\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, promotionRuleProductVariantPrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to update all in promotionRuleProductVariant slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to retrieve rows affected all in update all promotionRuleProductVariant")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *PromotionRuleProductVariant) Upsert(exec boil.Executor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("model: no promotion_rule_product_variants provided for upsert")
	}

	nzDefaults := queries.NonZeroDefaultSet(promotionRuleProductVariantColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	promotionRuleProductVariantUpsertCacheMut.RLock()
	cache, cached := promotionRuleProductVariantUpsertCache[key]
	promotionRuleProductVariantUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			promotionRuleProductVariantAllColumns,
			promotionRuleProductVariantColumnsWithDefault,
			promotionRuleProductVariantColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			promotionRuleProductVariantAllColumns,
			promotionRuleProductVariantPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("model: unable to upsert promotion_rule_product_variants, could not build update column list")
		}

		ret := strmangle.SetComplement(promotionRuleProductVariantAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(promotionRuleProductVariantPrimaryKeyColumns) == 0 {
				return errors.New("model: unable to upsert promotion_rule_product_variants, could not build conflict column list")
			}

			conflict = make([]string, len(promotionRuleProductVariantPrimaryKeyColumns))
			copy(conflict, promotionRuleProductVariantPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"promotion_rule_product_variants\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(promotionRuleProductVariantType, promotionRuleProductVariantMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(promotionRuleProductVariantType, promotionRuleProductVariantMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "model: unable to upsert promotion_rule_product_variants")
	}

	if !cached {
		promotionRuleProductVariantUpsertCacheMut.Lock()
		promotionRuleProductVariantUpsertCache[key] = cache
		promotionRuleProductVariantUpsertCacheMut.Unlock()
	}

	return nil
}

// Delete deletes a single PromotionRuleProductVariant record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *PromotionRuleProductVariant) Delete(exec boil.Executor) (int64, error) {
	if o == nil {
		return 0, errors.New("model: no PromotionRuleProductVariant provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), promotionRuleProductVariantPrimaryKeyMapping)
	sql := "DELETE FROM \"promotion_rule_product_variants\" WHERE \"id\"=$1"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to delete from promotion_rule_product_variants")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by delete for promotion_rule_product_variants")
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q promotionRuleProductVariantQuery) DeleteAll(exec boil.Executor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("model: no promotionRuleProductVariantQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to delete all from promotion_rule_product_variants")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by deleteall for promotion_rule_product_variants")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o PromotionRuleProductVariantSlice) DeleteAll(exec boil.Executor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), promotionRuleProductVariantPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"promotion_rule_product_variants\" WHERE " +
		strmangle.WhereInClause(string(dialect.LQ), string(dialect.RQ), 1, promotionRuleProductVariantPrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to delete all from promotionRuleProductVariant slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by deleteall for promotion_rule_product_variants")
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *PromotionRuleProductVariant) Reload(exec boil.Executor) error {
	ret, err := FindPromotionRuleProductVariant(exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *PromotionRuleProductVariantSlice) ReloadAll(exec boil.Executor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := PromotionRuleProductVariantSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), promotionRuleProductVariantPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"promotion_rule_product_variants\".* FROM \"promotion_rule_product_variants\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, promotionRuleProductVariantPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(nil, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "model: unable to reload all in PromotionRuleProductVariantSlice")
	}

	*o = slice

	return nil
}

// PromotionRuleProductVariantExists checks if the PromotionRuleProductVariant row exists.
func PromotionRuleProductVariantExists(exec boil.Executor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"promotion_rule_product_variants\" where \"id\"=$1 limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, iD)
	}
	row := exec.QueryRow(sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "model: unable to check if promotion_rule_product_variants exists")
	}

	return exists, nil
}

// Exists checks if the PromotionRuleProductVariant row exists.
func (o *PromotionRuleProductVariant) Exists(exec boil.Executor) (bool, error) {
	return PromotionRuleProductVariantExists(exec, o.ID)
}
