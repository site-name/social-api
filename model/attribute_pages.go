// Code generated by SQLBoiler 4.16.1 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package model

import (
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/sitename/sitename/modules/model_types"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// AttributePage is an object representing the database table.
type AttributePage struct {
	ID          string              `boil:"id" json:"id" toml:"id" yaml:"id"`
	AttributeID string              `boil:"attribute_id" json:"attribute_id" toml:"attribute_id" yaml:"attribute_id"`
	PageTypeID  string              `boil:"page_type_id" json:"page_type_id" toml:"page_type_id" yaml:"page_type_id"`
	SortOrder   model_types.NullInt `boil:"sort_order" json:"sort_order,omitempty" toml:"sort_order" yaml:"sort_order,omitempty"`

	R *attributePageR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L attributePageL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var AttributePageColumns = struct {
	ID          string
	AttributeID string
	PageTypeID  string
	SortOrder   string
}{
	ID:          "id",
	AttributeID: "attribute_id",
	PageTypeID:  "page_type_id",
	SortOrder:   "sort_order",
}

var AttributePageTableColumns = struct {
	ID          string
	AttributeID string
	PageTypeID  string
	SortOrder   string
}{
	ID:          "attribute_pages.id",
	AttributeID: "attribute_pages.attribute_id",
	PageTypeID:  "attribute_pages.page_type_id",
	SortOrder:   "attribute_pages.sort_order",
}

// Generated where

var AttributePageWhere = struct {
	ID          whereHelperstring
	AttributeID whereHelperstring
	PageTypeID  whereHelperstring
	SortOrder   whereHelpermodel_types_NullInt
}{
	ID:          whereHelperstring{field: "\"attribute_pages\".\"id\""},
	AttributeID: whereHelperstring{field: "\"attribute_pages\".\"attribute_id\""},
	PageTypeID:  whereHelperstring{field: "\"attribute_pages\".\"page_type_id\""},
	SortOrder:   whereHelpermodel_types_NullInt{field: "\"attribute_pages\".\"sort_order\""},
}

// AttributePageRels is where relationship names are stored.
var AttributePageRels = struct {
	AssignmentAssignedPageAttributes string
}{
	AssignmentAssignedPageAttributes: "AssignmentAssignedPageAttributes",
}

// attributePageR is where relationships are stored.
type attributePageR struct {
	AssignmentAssignedPageAttributes AssignedPageAttributeSlice `boil:"AssignmentAssignedPageAttributes" json:"AssignmentAssignedPageAttributes" toml:"AssignmentAssignedPageAttributes" yaml:"AssignmentAssignedPageAttributes"`
}

// NewStruct creates a new relationship struct
func (*attributePageR) NewStruct() *attributePageR {
	return &attributePageR{}
}

func (r *attributePageR) GetAssignmentAssignedPageAttributes() AssignedPageAttributeSlice {
	if r == nil {
		return nil
	}
	return r.AssignmentAssignedPageAttributes
}

// attributePageL is where Load methods for each relationship are stored.
type attributePageL struct{}

var (
	attributePageAllColumns            = []string{"id", "attribute_id", "page_type_id", "sort_order"}
	attributePageColumnsWithoutDefault = []string{"attribute_id", "page_type_id"}
	attributePageColumnsWithDefault    = []string{"id", "sort_order"}
	attributePagePrimaryKeyColumns     = []string{"id"}
	attributePageGeneratedColumns      = []string{}
)

type (
	// AttributePageSlice is an alias for a slice of pointers to AttributePage.
	// This should almost always be used instead of []AttributePage.
	AttributePageSlice []*AttributePage

	attributePageQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	attributePageType                 = reflect.TypeOf(&AttributePage{})
	attributePageMapping              = queries.MakeStructMapping(attributePageType)
	attributePagePrimaryKeyMapping, _ = queries.BindMapping(attributePageType, attributePageMapping, attributePagePrimaryKeyColumns)
	attributePageInsertCacheMut       sync.RWMutex
	attributePageInsertCache          = make(map[string]insertCache)
	attributePageUpdateCacheMut       sync.RWMutex
	attributePageUpdateCache          = make(map[string]updateCache)
	attributePageUpsertCacheMut       sync.RWMutex
	attributePageUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// One returns a single attributePage record from the query.
func (q attributePageQuery) One(exec boil.Executor) (*AttributePage, error) {
	o := &AttributePage{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(nil, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "model: failed to execute a one query for attribute_pages")
	}

	return o, nil
}

// All returns all AttributePage records from the query.
func (q attributePageQuery) All(exec boil.Executor) (AttributePageSlice, error) {
	var o []*AttributePage

	err := q.Bind(nil, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "model: failed to assign all query results to AttributePage slice")
	}

	return o, nil
}

// Count returns the count of all AttributePage records in the query.
func (q attributePageQuery) Count(exec boil.Executor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to count attribute_pages rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q attributePageQuery) Exists(exec boil.Executor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "model: failed to check if attribute_pages exists")
	}

	return count > 0, nil
}

// AssignmentAssignedPageAttributes retrieves all the assigned_page_attribute's AssignedPageAttributes with an executor via assignment_id column.
func (o *AttributePage) AssignmentAssignedPageAttributes(mods ...qm.QueryMod) assignedPageAttributeQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"assigned_page_attributes\".\"assignment_id\"=?", o.ID),
	)

	return AssignedPageAttributes(queryMods...)
}

// LoadAssignmentAssignedPageAttributes allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (attributePageL) LoadAssignmentAssignedPageAttributes(e boil.Executor, singular bool, maybeAttributePage interface{}, mods queries.Applicator) error {
	var slice []*AttributePage
	var object *AttributePage

	if singular {
		var ok bool
		object, ok = maybeAttributePage.(*AttributePage)
		if !ok {
			object = new(AttributePage)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeAttributePage)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeAttributePage))
			}
		}
	} else {
		s, ok := maybeAttributePage.(*[]*AttributePage)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeAttributePage)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeAttributePage))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &attributePageR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &attributePageR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`assigned_page_attributes`),
		qm.WhereIn(`assigned_page_attributes.assignment_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.Query(e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load assigned_page_attributes")
	}

	var resultSlice []*AssignedPageAttribute
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice assigned_page_attributes")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on assigned_page_attributes")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for assigned_page_attributes")
	}

	if singular {
		object.R.AssignmentAssignedPageAttributes = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &assignedPageAttributeR{}
			}
			foreign.R.Assignment = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.AssignmentID {
				local.R.AssignmentAssignedPageAttributes = append(local.R.AssignmentAssignedPageAttributes, foreign)
				if foreign.R == nil {
					foreign.R = &assignedPageAttributeR{}
				}
				foreign.R.Assignment = local
				break
			}
		}
	}

	return nil
}

// AddAssignmentAssignedPageAttributes adds the given related objects to the existing relationships
// of the attribute_page, optionally inserting them as new records.
// Appends related to o.R.AssignmentAssignedPageAttributes.
// Sets related.R.Assignment appropriately.
func (o *AttributePage) AddAssignmentAssignedPageAttributes(exec boil.Executor, insert bool, related ...*AssignedPageAttribute) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.AssignmentID = o.ID
			if err = rel.Insert(exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"assigned_page_attributes\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"assignment_id"}),
				strmangle.WhereClause("\"", "\"", 2, assignedPageAttributePrimaryKeyColumns),
			)
			values := []interface{}{o.ID, rel.ID}

			if boil.DebugMode {
				fmt.Fprintln(boil.DebugWriter, updateQuery)
				fmt.Fprintln(boil.DebugWriter, values)
			}
			if _, err = exec.Exec(updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.AssignmentID = o.ID
		}
	}

	if o.R == nil {
		o.R = &attributePageR{
			AssignmentAssignedPageAttributes: related,
		}
	} else {
		o.R.AssignmentAssignedPageAttributes = append(o.R.AssignmentAssignedPageAttributes, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &assignedPageAttributeR{
				Assignment: o,
			}
		} else {
			rel.R.Assignment = o
		}
	}
	return nil
}

// AttributePages retrieves all the records using an executor.
func AttributePages(mods ...qm.QueryMod) attributePageQuery {
	mods = append(mods, qm.From("\"attribute_pages\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"attribute_pages\".*"})
	}

	return attributePageQuery{q}
}

// FindAttributePage retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindAttributePage(exec boil.Executor, iD string, selectCols ...string) (*AttributePage, error) {
	attributePageObj := &AttributePage{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"attribute_pages\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(nil, exec, attributePageObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "model: unable to select from attribute_pages")
	}

	return attributePageObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *AttributePage) Insert(exec boil.Executor, columns boil.Columns) error {
	if o == nil {
		return errors.New("model: no attribute_pages provided for insertion")
	}

	var err error

	nzDefaults := queries.NonZeroDefaultSet(attributePageColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	attributePageInsertCacheMut.RLock()
	cache, cached := attributePageInsertCache[key]
	attributePageInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			attributePageAllColumns,
			attributePageColumnsWithDefault,
			attributePageColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(attributePageType, attributePageMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(attributePageType, attributePageMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"attribute_pages\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"attribute_pages\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "model: unable to insert into attribute_pages")
	}

	if !cached {
		attributePageInsertCacheMut.Lock()
		attributePageInsertCache[key] = cache
		attributePageInsertCacheMut.Unlock()
	}

	return nil
}

// Update uses an executor to update the AttributePage.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *AttributePage) Update(exec boil.Executor, columns boil.Columns) (int64, error) {
	var err error
	key := makeCacheKey(columns, nil)
	attributePageUpdateCacheMut.RLock()
	cache, cached := attributePageUpdateCache[key]
	attributePageUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			attributePageAllColumns,
			attributePagePrimaryKeyColumns,
		)
		if len(wl) == 0 {
			return 0, errors.New("model: unable to update attribute_pages, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"attribute_pages\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, attributePagePrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(attributePageType, attributePageMapping, append(wl, attributePagePrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	var result sql.Result
	result, err = exec.Exec(cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to update attribute_pages row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by update for attribute_pages")
	}

	if !cached {
		attributePageUpdateCacheMut.Lock()
		attributePageUpdateCache[key] = cache
		attributePageUpdateCacheMut.Unlock()
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values.
func (q attributePageQuery) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to update all for attribute_pages")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to retrieve rows affected for attribute_pages")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o AttributePageSlice) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("model: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), attributePagePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"attribute_pages\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, attributePagePrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to update all in attributePage slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to retrieve rows affected all in update all attributePage")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *AttributePage) Upsert(exec boil.Executor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("model: no attribute_pages provided for upsert")
	}

	nzDefaults := queries.NonZeroDefaultSet(attributePageColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	attributePageUpsertCacheMut.RLock()
	cache, cached := attributePageUpsertCache[key]
	attributePageUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			attributePageAllColumns,
			attributePageColumnsWithDefault,
			attributePageColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			attributePageAllColumns,
			attributePagePrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("model: unable to upsert attribute_pages, could not build update column list")
		}

		ret := strmangle.SetComplement(attributePageAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(attributePagePrimaryKeyColumns) == 0 {
				return errors.New("model: unable to upsert attribute_pages, could not build conflict column list")
			}

			conflict = make([]string, len(attributePagePrimaryKeyColumns))
			copy(conflict, attributePagePrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"attribute_pages\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(attributePageType, attributePageMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(attributePageType, attributePageMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "model: unable to upsert attribute_pages")
	}

	if !cached {
		attributePageUpsertCacheMut.Lock()
		attributePageUpsertCache[key] = cache
		attributePageUpsertCacheMut.Unlock()
	}

	return nil
}

// Delete deletes a single AttributePage record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *AttributePage) Delete(exec boil.Executor) (int64, error) {
	if o == nil {
		return 0, errors.New("model: no AttributePage provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), attributePagePrimaryKeyMapping)
	sql := "DELETE FROM \"attribute_pages\" WHERE \"id\"=$1"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to delete from attribute_pages")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by delete for attribute_pages")
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q attributePageQuery) DeleteAll(exec boil.Executor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("model: no attributePageQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to delete all from attribute_pages")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by deleteall for attribute_pages")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o AttributePageSlice) DeleteAll(exec boil.Executor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), attributePagePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"attribute_pages\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, attributePagePrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to delete all from attributePage slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by deleteall for attribute_pages")
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *AttributePage) Reload(exec boil.Executor) error {
	ret, err := FindAttributePage(exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *AttributePageSlice) ReloadAll(exec boil.Executor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := AttributePageSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), attributePagePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"attribute_pages\".* FROM \"attribute_pages\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, attributePagePrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(nil, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "model: unable to reload all in AttributePageSlice")
	}

	*o = slice

	return nil
}

// AttributePageExists checks if the AttributePage row exists.
func AttributePageExists(exec boil.Executor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"attribute_pages\" where \"id\"=$1 limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, iD)
	}
	row := exec.QueryRow(sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "model: unable to check if attribute_pages exists")
	}

	return exists, nil
}

// Exists checks if the AttributePage row exists.
func (o *AttributePage) Exists(exec boil.Executor) (bool, error) {
	return AttributePageExists(exec, o.ID)
}
