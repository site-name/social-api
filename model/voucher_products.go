// Code generated by SQLBoiler 4.15.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package model

import (
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// VoucherProduct is an object representing the database table.
type VoucherProduct struct {
	ID        string `boil:"id" json:"id" toml:"id" yaml:"id"`
	VoucherID string `boil:"voucher_id" json:"voucher_id" toml:"voucher_id" yaml:"voucher_id"`
	ProductID string `boil:"product_id" json:"product_id" toml:"product_id" yaml:"product_id"`

	R *voucherProductR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L voucherProductL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var VoucherProductColumns = struct {
	ID        string
	VoucherID string
	ProductID string
}{
	ID:        "id",
	VoucherID: "voucher_id",
	ProductID: "product_id",
}

var VoucherProductTableColumns = struct {
	ID        string
	VoucherID string
	ProductID string
}{
	ID:        "voucher_products.id",
	VoucherID: "voucher_products.voucher_id",
	ProductID: "voucher_products.product_id",
}

// Generated where

var VoucherProductWhere = struct {
	ID        whereHelperstring
	VoucherID whereHelperstring
	ProductID whereHelperstring
}{
	ID:        whereHelperstring{field: "\"voucher_products\".\"id\""},
	VoucherID: whereHelperstring{field: "\"voucher_products\".\"voucher_id\""},
	ProductID: whereHelperstring{field: "\"voucher_products\".\"product_id\""},
}

// VoucherProductRels is where relationship names are stored.
var VoucherProductRels = struct {
	Product string
	Voucher string
}{
	Product: "Product",
	Voucher: "Voucher",
}

// voucherProductR is where relationships are stored.
type voucherProductR struct {
	Product *Product `boil:"Product" json:"Product" toml:"Product" yaml:"Product"`
	Voucher *Voucher `boil:"Voucher" json:"Voucher" toml:"Voucher" yaml:"Voucher"`
}

// NewStruct creates a new relationship struct
func (*voucherProductR) NewStruct() *voucherProductR {
	return &voucherProductR{}
}

func (r *voucherProductR) GetProduct() *Product {
	if r == nil {
		return nil
	}
	return r.Product
}

func (r *voucherProductR) GetVoucher() *Voucher {
	if r == nil {
		return nil
	}
	return r.Voucher
}

// voucherProductL is where Load methods for each relationship are stored.
type voucherProductL struct{}

var (
	voucherProductAllColumns            = []string{"id", "voucher_id", "product_id"}
	voucherProductColumnsWithoutDefault = []string{"voucher_id", "product_id"}
	voucherProductColumnsWithDefault    = []string{"id"}
	voucherProductPrimaryKeyColumns     = []string{"id"}
	voucherProductGeneratedColumns      = []string{}
)

type (
	// VoucherProductSlice is an alias for a slice of pointers to VoucherProduct.
	// This should almost always be used instead of []VoucherProduct.
	VoucherProductSlice []*VoucherProduct

	voucherProductQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	voucherProductType                 = reflect.TypeOf(&VoucherProduct{})
	voucherProductMapping              = queries.MakeStructMapping(voucherProductType)
	voucherProductPrimaryKeyMapping, _ = queries.BindMapping(voucherProductType, voucherProductMapping, voucherProductPrimaryKeyColumns)
	voucherProductInsertCacheMut       sync.RWMutex
	voucherProductInsertCache          = make(map[string]insertCache)
	voucherProductUpdateCacheMut       sync.RWMutex
	voucherProductUpdateCache          = make(map[string]updateCache)
	voucherProductUpsertCacheMut       sync.RWMutex
	voucherProductUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// One returns a single voucherProduct record from the query.
func (q voucherProductQuery) One(exec boil.Executor) (*VoucherProduct, error) {
	o := &VoucherProduct{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(nil, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "model: failed to execute a one query for voucher_products")
	}

	return o, nil
}

// All returns all VoucherProduct records from the query.
func (q voucherProductQuery) All(exec boil.Executor) (VoucherProductSlice, error) {
	var o []*VoucherProduct

	err := q.Bind(nil, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "model: failed to assign all query results to VoucherProduct slice")
	}

	return o, nil
}

// Count returns the count of all VoucherProduct records in the query.
func (q voucherProductQuery) Count(exec boil.Executor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to count voucher_products rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q voucherProductQuery) Exists(exec boil.Executor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "model: failed to check if voucher_products exists")
	}

	return count > 0, nil
}

// Product pointed to by the foreign key.
func (o *VoucherProduct) Product(mods ...qm.QueryMod) productQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.ProductID),
	}

	queryMods = append(queryMods, mods...)

	return Products(queryMods...)
}

// Voucher pointed to by the foreign key.
func (o *VoucherProduct) Voucher(mods ...qm.QueryMod) voucherQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.VoucherID),
	}

	queryMods = append(queryMods, mods...)

	return Vouchers(queryMods...)
}

// LoadProduct allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (voucherProductL) LoadProduct(e boil.Executor, singular bool, maybeVoucherProduct interface{}, mods queries.Applicator) error {
	var slice []*VoucherProduct
	var object *VoucherProduct

	if singular {
		var ok bool
		object, ok = maybeVoucherProduct.(*VoucherProduct)
		if !ok {
			object = new(VoucherProduct)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeVoucherProduct)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeVoucherProduct))
			}
		}
	} else {
		s, ok := maybeVoucherProduct.(*[]*VoucherProduct)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeVoucherProduct)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeVoucherProduct))
			}
		}
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &voucherProductR{}
		}
		args = append(args, object.ProductID)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &voucherProductR{}
			}

			for _, a := range args {
				if a == obj.ProductID {
					continue Outer
				}
			}

			args = append(args, obj.ProductID)

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`products`),
		qm.WhereIn(`products.id in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.Query(e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Product")
	}

	var resultSlice []*Product
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Product")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for products")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for products")
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Product = foreign
		if foreign.R == nil {
			foreign.R = &productR{}
		}
		foreign.R.VoucherProducts = append(foreign.R.VoucherProducts, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.ProductID == foreign.ID {
				local.R.Product = foreign
				if foreign.R == nil {
					foreign.R = &productR{}
				}
				foreign.R.VoucherProducts = append(foreign.R.VoucherProducts, local)
				break
			}
		}
	}

	return nil
}

// LoadVoucher allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (voucherProductL) LoadVoucher(e boil.Executor, singular bool, maybeVoucherProduct interface{}, mods queries.Applicator) error {
	var slice []*VoucherProduct
	var object *VoucherProduct

	if singular {
		var ok bool
		object, ok = maybeVoucherProduct.(*VoucherProduct)
		if !ok {
			object = new(VoucherProduct)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeVoucherProduct)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeVoucherProduct))
			}
		}
	} else {
		s, ok := maybeVoucherProduct.(*[]*VoucherProduct)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeVoucherProduct)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeVoucherProduct))
			}
		}
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &voucherProductR{}
		}
		args = append(args, object.VoucherID)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &voucherProductR{}
			}

			for _, a := range args {
				if a == obj.VoucherID {
					continue Outer
				}
			}

			args = append(args, obj.VoucherID)

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`vouchers`),
		qm.WhereIn(`vouchers.id in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.Query(e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Voucher")
	}

	var resultSlice []*Voucher
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Voucher")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for vouchers")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for vouchers")
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Voucher = foreign
		if foreign.R == nil {
			foreign.R = &voucherR{}
		}
		foreign.R.VoucherProducts = append(foreign.R.VoucherProducts, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.VoucherID == foreign.ID {
				local.R.Voucher = foreign
				if foreign.R == nil {
					foreign.R = &voucherR{}
				}
				foreign.R.VoucherProducts = append(foreign.R.VoucherProducts, local)
				break
			}
		}
	}

	return nil
}

// SetProduct of the voucherProduct to the related item.
// Sets o.R.Product to related.
// Adds o to related.R.VoucherProducts.
func (o *VoucherProduct) SetProduct(exec boil.Executor, insert bool, related *Product) error {
	var err error
	if insert {
		if err = related.Insert(exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"voucher_products\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"product_id"}),
		strmangle.WhereClause("\"", "\"", 2, voucherProductPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	if _, err = exec.Exec(updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.ProductID = related.ID
	if o.R == nil {
		o.R = &voucherProductR{
			Product: related,
		}
	} else {
		o.R.Product = related
	}

	if related.R == nil {
		related.R = &productR{
			VoucherProducts: VoucherProductSlice{o},
		}
	} else {
		related.R.VoucherProducts = append(related.R.VoucherProducts, o)
	}

	return nil
}

// SetVoucher of the voucherProduct to the related item.
// Sets o.R.Voucher to related.
// Adds o to related.R.VoucherProducts.
func (o *VoucherProduct) SetVoucher(exec boil.Executor, insert bool, related *Voucher) error {
	var err error
	if insert {
		if err = related.Insert(exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"voucher_products\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"voucher_id"}),
		strmangle.WhereClause("\"", "\"", 2, voucherProductPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	if _, err = exec.Exec(updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.VoucherID = related.ID
	if o.R == nil {
		o.R = &voucherProductR{
			Voucher: related,
		}
	} else {
		o.R.Voucher = related
	}

	if related.R == nil {
		related.R = &voucherR{
			VoucherProducts: VoucherProductSlice{o},
		}
	} else {
		related.R.VoucherProducts = append(related.R.VoucherProducts, o)
	}

	return nil
}

// VoucherProducts retrieves all the records using an executor.
func VoucherProducts(mods ...qm.QueryMod) voucherProductQuery {
	mods = append(mods, qm.From("\"voucher_products\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"voucher_products\".*"})
	}

	return voucherProductQuery{q}
}

// FindVoucherProduct retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindVoucherProduct(exec boil.Executor, iD string, selectCols ...string) (*VoucherProduct, error) {
	voucherProductObj := &VoucherProduct{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"voucher_products\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(nil, exec, voucherProductObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "model: unable to select from voucher_products")
	}

	return voucherProductObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *VoucherProduct) Insert(exec boil.Executor, columns boil.Columns) error {
	if o == nil {
		return errors.New("model: no voucher_products provided for insertion")
	}

	var err error

	nzDefaults := queries.NonZeroDefaultSet(voucherProductColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	voucherProductInsertCacheMut.RLock()
	cache, cached := voucherProductInsertCache[key]
	voucherProductInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			voucherProductAllColumns,
			voucherProductColumnsWithDefault,
			voucherProductColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(voucherProductType, voucherProductMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(voucherProductType, voucherProductMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"voucher_products\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"voucher_products\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "model: unable to insert into voucher_products")
	}

	if !cached {
		voucherProductInsertCacheMut.Lock()
		voucherProductInsertCache[key] = cache
		voucherProductInsertCacheMut.Unlock()
	}

	return nil
}

// Update uses an executor to update the VoucherProduct.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *VoucherProduct) Update(exec boil.Executor, columns boil.Columns) (int64, error) {
	var err error
	key := makeCacheKey(columns, nil)
	voucherProductUpdateCacheMut.RLock()
	cache, cached := voucherProductUpdateCache[key]
	voucherProductUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			voucherProductAllColumns,
			voucherProductPrimaryKeyColumns,
		)
		if len(wl) == 0 {
			return 0, errors.New("model: unable to update voucher_products, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"voucher_products\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, voucherProductPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(voucherProductType, voucherProductMapping, append(wl, voucherProductPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	var result sql.Result
	result, err = exec.Exec(cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to update voucher_products row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by update for voucher_products")
	}

	if !cached {
		voucherProductUpdateCacheMut.Lock()
		voucherProductUpdateCache[key] = cache
		voucherProductUpdateCacheMut.Unlock()
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values.
func (q voucherProductQuery) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to update all for voucher_products")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to retrieve rows affected for voucher_products")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o VoucherProductSlice) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("model: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), voucherProductPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"voucher_products\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, voucherProductPrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to update all in voucherProduct slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to retrieve rows affected all in update all voucherProduct")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *VoucherProduct) Upsert(exec boil.Executor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("model: no voucher_products provided for upsert")
	}

	nzDefaults := queries.NonZeroDefaultSet(voucherProductColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	voucherProductUpsertCacheMut.RLock()
	cache, cached := voucherProductUpsertCache[key]
	voucherProductUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			voucherProductAllColumns,
			voucherProductColumnsWithDefault,
			voucherProductColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			voucherProductAllColumns,
			voucherProductPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("model: unable to upsert voucher_products, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(voucherProductPrimaryKeyColumns))
			copy(conflict, voucherProductPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"voucher_products\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(voucherProductType, voucherProductMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(voucherProductType, voucherProductMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "model: unable to upsert voucher_products")
	}

	if !cached {
		voucherProductUpsertCacheMut.Lock()
		voucherProductUpsertCache[key] = cache
		voucherProductUpsertCacheMut.Unlock()
	}

	return nil
}

// Delete deletes a single VoucherProduct record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *VoucherProduct) Delete(exec boil.Executor) (int64, error) {
	if o == nil {
		return 0, errors.New("model: no VoucherProduct provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), voucherProductPrimaryKeyMapping)
	sql := "DELETE FROM \"voucher_products\" WHERE \"id\"=$1"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to delete from voucher_products")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by delete for voucher_products")
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q voucherProductQuery) DeleteAll(exec boil.Executor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("model: no voucherProductQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to delete all from voucher_products")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by deleteall for voucher_products")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o VoucherProductSlice) DeleteAll(exec boil.Executor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), voucherProductPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"voucher_products\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, voucherProductPrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to delete all from voucherProduct slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by deleteall for voucher_products")
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *VoucherProduct) Reload(exec boil.Executor) error {
	ret, err := FindVoucherProduct(exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *VoucherProductSlice) ReloadAll(exec boil.Executor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := VoucherProductSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), voucherProductPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"voucher_products\".* FROM \"voucher_products\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, voucherProductPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(nil, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "model: unable to reload all in VoucherProductSlice")
	}

	*o = slice

	return nil
}

// VoucherProductExists checks if the VoucherProduct row exists.
func VoucherProductExists(exec boil.Executor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"voucher_products\" where \"id\"=$1 limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, iD)
	}
	row := exec.QueryRow(sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "model: unable to check if voucher_products exists")
	}

	return exists, nil
}

// Exists checks if the VoucherProduct row exists.
func (o *VoucherProduct) Exists(exec boil.Executor) (bool, error) {
	return VoucherProductExists(exec, o.ID)
}
