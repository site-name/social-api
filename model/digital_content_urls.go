// Code generated by SQLBoiler 4.16.1 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package model

import (
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/sitename/sitename/modules/model_types"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// DigitalContentURL is an object representing the database table.
type DigitalContentURL struct {
	ID          string                 `boil:"id" json:"id" toml:"id" yaml:"id"`
	Token       string                 `boil:"token" json:"token" toml:"token" yaml:"token"`
	ContentID   string                 `boil:"content_id" json:"content_id" toml:"content_id" yaml:"content_id"`
	CreatedAt   int64                  `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	DownloadNum int                    `boil:"download_num" json:"download_num" toml:"download_num" yaml:"download_num"`
	LineID      model_types.NullString `boil:"line_id" json:"line_id,omitempty" toml:"line_id" yaml:"line_id,omitempty"`

	R *digitalContentURLR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L digitalContentURLL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var DigitalContentURLColumns = struct {
	ID          string
	Token       string
	ContentID   string
	CreatedAt   string
	DownloadNum string
	LineID      string
}{
	ID:          "id",
	Token:       "token",
	ContentID:   "content_id",
	CreatedAt:   "created_at",
	DownloadNum: "download_num",
	LineID:      "line_id",
}

var DigitalContentURLTableColumns = struct {
	ID          string
	Token       string
	ContentID   string
	CreatedAt   string
	DownloadNum string
	LineID      string
}{
	ID:          "digital_content_urls.id",
	Token:       "digital_content_urls.token",
	ContentID:   "digital_content_urls.content_id",
	CreatedAt:   "digital_content_urls.created_at",
	DownloadNum: "digital_content_urls.download_num",
	LineID:      "digital_content_urls.line_id",
}

// Generated where

var DigitalContentURLWhere = struct {
	ID          whereHelperstring
	Token       whereHelperstring
	ContentID   whereHelperstring
	CreatedAt   whereHelperint64
	DownloadNum whereHelperint
	LineID      whereHelpermodel_types_NullString
}{
	ID:          whereHelperstring{field: "\"digital_content_urls\".\"id\""},
	Token:       whereHelperstring{field: "\"digital_content_urls\".\"token\""},
	ContentID:   whereHelperstring{field: "\"digital_content_urls\".\"content_id\""},
	CreatedAt:   whereHelperint64{field: "\"digital_content_urls\".\"created_at\""},
	DownloadNum: whereHelperint{field: "\"digital_content_urls\".\"download_num\""},
	LineID:      whereHelpermodel_types_NullString{field: "\"digital_content_urls\".\"line_id\""},
}

// DigitalContentURLRels is where relationship names are stored.
var DigitalContentURLRels = struct {
}{}

// digitalContentURLR is where relationships are stored.
type digitalContentURLR struct {
}

// NewStruct creates a new relationship struct
func (*digitalContentURLR) NewStruct() *digitalContentURLR {
	return &digitalContentURLR{}
}

// digitalContentURLL is where Load methods for each relationship are stored.
type digitalContentURLL struct{}

var (
	digitalContentURLAllColumns            = []string{"id", "token", "content_id", "created_at", "download_num", "line_id"}
	digitalContentURLColumnsWithoutDefault = []string{"token", "content_id", "created_at", "download_num"}
	digitalContentURLColumnsWithDefault    = []string{"id", "line_id"}
	digitalContentURLPrimaryKeyColumns     = []string{"id"}
	digitalContentURLGeneratedColumns      = []string{}
)

type (
	// DigitalContentURLSlice is an alias for a slice of pointers to DigitalContentURL.
	// This should almost always be used instead of []DigitalContentURL.
	DigitalContentURLSlice []*DigitalContentURL

	digitalContentURLQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	digitalContentURLType                 = reflect.TypeOf(&DigitalContentURL{})
	digitalContentURLMapping              = queries.MakeStructMapping(digitalContentURLType)
	digitalContentURLPrimaryKeyMapping, _ = queries.BindMapping(digitalContentURLType, digitalContentURLMapping, digitalContentURLPrimaryKeyColumns)
	digitalContentURLInsertCacheMut       sync.RWMutex
	digitalContentURLInsertCache          = make(map[string]insertCache)
	digitalContentURLUpdateCacheMut       sync.RWMutex
	digitalContentURLUpdateCache          = make(map[string]updateCache)
	digitalContentURLUpsertCacheMut       sync.RWMutex
	digitalContentURLUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// One returns a single digitalContentURL record from the query.
func (q digitalContentURLQuery) One(exec boil.Executor) (*DigitalContentURL, error) {
	o := &DigitalContentURL{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(nil, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "model: failed to execute a one query for digital_content_urls")
	}

	return o, nil
}

// All returns all DigitalContentURL records from the query.
func (q digitalContentURLQuery) All(exec boil.Executor) (DigitalContentURLSlice, error) {
	var o []*DigitalContentURL

	err := q.Bind(nil, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "model: failed to assign all query results to DigitalContentURL slice")
	}

	return o, nil
}

// Count returns the count of all DigitalContentURL records in the query.
func (q digitalContentURLQuery) Count(exec boil.Executor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to count digital_content_urls rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q digitalContentURLQuery) Exists(exec boil.Executor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "model: failed to check if digital_content_urls exists")
	}

	return count > 0, nil
}

// DigitalContentUrls retrieves all the records using an executor.
func DigitalContentUrls(mods ...qm.QueryMod) digitalContentURLQuery {
	mods = append(mods, qm.From("\"digital_content_urls\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"digital_content_urls\".*"})
	}

	return digitalContentURLQuery{q}
}

// FindDigitalContentURL retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindDigitalContentURL(exec boil.Executor, iD string, selectCols ...string) (*DigitalContentURL, error) {
	digitalContentURLObj := &DigitalContentURL{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"digital_content_urls\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(nil, exec, digitalContentURLObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "model: unable to select from digital_content_urls")
	}

	return digitalContentURLObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *DigitalContentURL) Insert(exec boil.Executor, columns boil.Columns) error {
	if o == nil {
		return errors.New("model: no digital_content_urls provided for insertion")
	}

	var err error

	nzDefaults := queries.NonZeroDefaultSet(digitalContentURLColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	digitalContentURLInsertCacheMut.RLock()
	cache, cached := digitalContentURLInsertCache[key]
	digitalContentURLInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			digitalContentURLAllColumns,
			digitalContentURLColumnsWithDefault,
			digitalContentURLColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(digitalContentURLType, digitalContentURLMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(digitalContentURLType, digitalContentURLMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"digital_content_urls\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"digital_content_urls\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "model: unable to insert into digital_content_urls")
	}

	if !cached {
		digitalContentURLInsertCacheMut.Lock()
		digitalContentURLInsertCache[key] = cache
		digitalContentURLInsertCacheMut.Unlock()
	}

	return nil
}

// Update uses an executor to update the DigitalContentURL.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *DigitalContentURL) Update(exec boil.Executor, columns boil.Columns) (int64, error) {
	var err error
	key := makeCacheKey(columns, nil)
	digitalContentURLUpdateCacheMut.RLock()
	cache, cached := digitalContentURLUpdateCache[key]
	digitalContentURLUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			digitalContentURLAllColumns,
			digitalContentURLPrimaryKeyColumns,
		)
		if len(wl) == 0 {
			return 0, errors.New("model: unable to update digital_content_urls, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"digital_content_urls\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, digitalContentURLPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(digitalContentURLType, digitalContentURLMapping, append(wl, digitalContentURLPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	var result sql.Result
	result, err = exec.Exec(cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to update digital_content_urls row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by update for digital_content_urls")
	}

	if !cached {
		digitalContentURLUpdateCacheMut.Lock()
		digitalContentURLUpdateCache[key] = cache
		digitalContentURLUpdateCacheMut.Unlock()
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values.
func (q digitalContentURLQuery) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to update all for digital_content_urls")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to retrieve rows affected for digital_content_urls")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o DigitalContentURLSlice) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("model: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), digitalContentURLPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"digital_content_urls\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, digitalContentURLPrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to update all in digitalContentURL slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to retrieve rows affected all in update all digitalContentURL")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *DigitalContentURL) Upsert(exec boil.Executor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("model: no digital_content_urls provided for upsert")
	}

	nzDefaults := queries.NonZeroDefaultSet(digitalContentURLColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	digitalContentURLUpsertCacheMut.RLock()
	cache, cached := digitalContentURLUpsertCache[key]
	digitalContentURLUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			digitalContentURLAllColumns,
			digitalContentURLColumnsWithDefault,
			digitalContentURLColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			digitalContentURLAllColumns,
			digitalContentURLPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("model: unable to upsert digital_content_urls, could not build update column list")
		}

		ret := strmangle.SetComplement(digitalContentURLAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(digitalContentURLPrimaryKeyColumns) == 0 {
				return errors.New("model: unable to upsert digital_content_urls, could not build conflict column list")
			}

			conflict = make([]string, len(digitalContentURLPrimaryKeyColumns))
			copy(conflict, digitalContentURLPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"digital_content_urls\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(digitalContentURLType, digitalContentURLMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(digitalContentURLType, digitalContentURLMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "model: unable to upsert digital_content_urls")
	}

	if !cached {
		digitalContentURLUpsertCacheMut.Lock()
		digitalContentURLUpsertCache[key] = cache
		digitalContentURLUpsertCacheMut.Unlock()
	}

	return nil
}

// Delete deletes a single DigitalContentURL record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *DigitalContentURL) Delete(exec boil.Executor) (int64, error) {
	if o == nil {
		return 0, errors.New("model: no DigitalContentURL provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), digitalContentURLPrimaryKeyMapping)
	sql := "DELETE FROM \"digital_content_urls\" WHERE \"id\"=$1"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to delete from digital_content_urls")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by delete for digital_content_urls")
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q digitalContentURLQuery) DeleteAll(exec boil.Executor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("model: no digitalContentURLQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to delete all from digital_content_urls")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by deleteall for digital_content_urls")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o DigitalContentURLSlice) DeleteAll(exec boil.Executor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), digitalContentURLPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"digital_content_urls\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, digitalContentURLPrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to delete all from digitalContentURL slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by deleteall for digital_content_urls")
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *DigitalContentURL) Reload(exec boil.Executor) error {
	ret, err := FindDigitalContentURL(exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *DigitalContentURLSlice) ReloadAll(exec boil.Executor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := DigitalContentURLSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), digitalContentURLPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"digital_content_urls\".* FROM \"digital_content_urls\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, digitalContentURLPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(nil, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "model: unable to reload all in DigitalContentURLSlice")
	}

	*o = slice

	return nil
}

// DigitalContentURLExists checks if the DigitalContentURL row exists.
func DigitalContentURLExists(exec boil.Executor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"digital_content_urls\" where \"id\"=$1 limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, iD)
	}
	row := exec.QueryRow(sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "model: unable to check if digital_content_urls exists")
	}

	return exists, nil
}

// Exists checks if the DigitalContentURL row exists.
func (o *DigitalContentURL) Exists(exec boil.Executor) (bool, error) {
	return DigitalContentURLExists(exec, o.ID)
}
