// Code generated by SQLBoiler 4.16.2 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package model

import (
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/sitename/sitename/modules/model_types"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// GiftcardEvent is an object representing the database table.
type GiftcardEvent struct {
	ID         string                 `boil:"id" json:"id" toml:"id" yaml:"id"`
	Date       int64                  `boil:"date" json:"date" toml:"date" yaml:"date"`
	Type       GiftcardEventType      `boil:"type" json:"type" toml:"type" yaml:"type"`
	Parameters model_types.JSONString `boil:"parameters" json:"parameters,omitempty" toml:"parameters" yaml:"parameters,omitempty"`
	UserID     model_types.NullString `boil:"user_id" json:"user_id,omitempty" toml:"user_id" yaml:"user_id,omitempty"`
	GiftcardID string                 `boil:"giftcard_id" json:"giftcard_id" toml:"giftcard_id" yaml:"giftcard_id"`

	R *giftcardEventR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L giftcardEventL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var GiftcardEventColumns = struct {
	ID         string
	Date       string
	Type       string
	Parameters string
	UserID     string
	GiftcardID string
}{
	ID:         "id",
	Date:       "date",
	Type:       "type",
	Parameters: "parameters",
	UserID:     "user_id",
	GiftcardID: "giftcard_id",
}

var GiftcardEventTableColumns = struct {
	ID         string
	Date       string
	Type       string
	Parameters string
	UserID     string
	GiftcardID string
}{
	ID:         "giftcard_events.id",
	Date:       "giftcard_events.date",
	Type:       "giftcard_events.type",
	Parameters: "giftcard_events.parameters",
	UserID:     "giftcard_events.user_id",
	GiftcardID: "giftcard_events.giftcard_id",
}

// Generated where

type whereHelperGiftcardEventType struct{ field string }

func (w whereHelperGiftcardEventType) EQ(x GiftcardEventType) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.EQ, x)
}
func (w whereHelperGiftcardEventType) NEQ(x GiftcardEventType) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.NEQ, x)
}
func (w whereHelperGiftcardEventType) LT(x GiftcardEventType) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LT, x)
}
func (w whereHelperGiftcardEventType) LTE(x GiftcardEventType) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LTE, x)
}
func (w whereHelperGiftcardEventType) GT(x GiftcardEventType) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GT, x)
}
func (w whereHelperGiftcardEventType) GTE(x GiftcardEventType) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GTE, x)
}
func (w whereHelperGiftcardEventType) IN(slice []GiftcardEventType) qm.QueryMod {
	values := make([]interface{}, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereIn(fmt.Sprintf("%s IN ?", w.field), values...)
}
func (w whereHelperGiftcardEventType) NIN(slice []GiftcardEventType) qm.QueryMod {
	values := make([]interface{}, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereNotIn(fmt.Sprintf("%s NOT IN ?", w.field), values...)
}

var GiftcardEventWhere = struct {
	ID         whereHelperstring
	Date       whereHelperint64
	Type       whereHelperGiftcardEventType
	Parameters whereHelpermodel_types_JSONString
	UserID     whereHelpermodel_types_NullString
	GiftcardID whereHelperstring
}{
	ID:         whereHelperstring{field: "\"giftcard_events\".\"id\""},
	Date:       whereHelperint64{field: "\"giftcard_events\".\"date\""},
	Type:       whereHelperGiftcardEventType{field: "\"giftcard_events\".\"type\""},
	Parameters: whereHelpermodel_types_JSONString{field: "\"giftcard_events\".\"parameters\""},
	UserID:     whereHelpermodel_types_NullString{field: "\"giftcard_events\".\"user_id\""},
	GiftcardID: whereHelperstring{field: "\"giftcard_events\".\"giftcard_id\""},
}

// GiftcardEventRels is where relationship names are stored.
var GiftcardEventRels = struct {
	Giftcard string
}{
	Giftcard: "Giftcard",
}

// giftcardEventR is where relationships are stored.
type giftcardEventR struct {
	Giftcard *Giftcard `boil:"Giftcard" json:"Giftcard" toml:"Giftcard" yaml:"Giftcard"`
}

// NewStruct creates a new relationship struct
func (*giftcardEventR) NewStruct() *giftcardEventR {
	return &giftcardEventR{}
}

func (r *giftcardEventR) GetGiftcard() *Giftcard {
	if r == nil {
		return nil
	}
	return r.Giftcard
}

// giftcardEventL is where Load methods for each relationship are stored.
type giftcardEventL struct{}

var (
	giftcardEventAllColumns            = []string{"id", "date", "type", "parameters", "user_id", "giftcard_id"}
	giftcardEventColumnsWithoutDefault = []string{"id", "date", "type", "giftcard_id"}
	giftcardEventColumnsWithDefault    = []string{"parameters", "user_id"}
	giftcardEventPrimaryKeyColumns     = []string{"id"}
	giftcardEventGeneratedColumns      = []string{}
)

type (
	// GiftcardEventSlice is an alias for a slice of pointers to GiftcardEvent.
	// This should almost always be used instead of []GiftcardEvent.
	GiftcardEventSlice []*GiftcardEvent

	giftcardEventQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	giftcardEventType                 = reflect.TypeOf(&GiftcardEvent{})
	giftcardEventMapping              = queries.MakeStructMapping(giftcardEventType)
	giftcardEventPrimaryKeyMapping, _ = queries.BindMapping(giftcardEventType, giftcardEventMapping, giftcardEventPrimaryKeyColumns)
	giftcardEventInsertCacheMut       sync.RWMutex
	giftcardEventInsertCache          = make(map[string]insertCache)
	giftcardEventUpdateCacheMut       sync.RWMutex
	giftcardEventUpdateCache          = make(map[string]updateCache)
	giftcardEventUpsertCacheMut       sync.RWMutex
	giftcardEventUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// One returns a single giftcardEvent record from the query.
func (q giftcardEventQuery) One(exec boil.Executor) (*GiftcardEvent, error) {
	o := &GiftcardEvent{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(nil, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "model: failed to execute a one query for giftcard_events")
	}

	return o, nil
}

// All returns all GiftcardEvent records from the query.
func (q giftcardEventQuery) All(exec boil.Executor) (GiftcardEventSlice, error) {
	var o []*GiftcardEvent

	err := q.Bind(nil, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "model: failed to assign all query results to GiftcardEvent slice")
	}

	return o, nil
}

// Count returns the count of all GiftcardEvent records in the query.
func (q giftcardEventQuery) Count(exec boil.Executor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to count giftcard_events rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q giftcardEventQuery) Exists(exec boil.Executor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "model: failed to check if giftcard_events exists")
	}

	return count > 0, nil
}

// Giftcard pointed to by the foreign key.
func (o *GiftcardEvent) Giftcard(mods ...qm.QueryMod) giftcardQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.GiftcardID),
	}

	queryMods = append(queryMods, mods...)

	return Giftcards(queryMods...)
}

// LoadGiftcard allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (giftcardEventL) LoadGiftcard(e boil.Executor, singular bool, maybeGiftcardEvent interface{}, mods queries.Applicator) error {
	var slice []*GiftcardEvent
	var object *GiftcardEvent

	if singular {
		var ok bool
		object, ok = maybeGiftcardEvent.(*GiftcardEvent)
		if !ok {
			object = new(GiftcardEvent)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeGiftcardEvent)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeGiftcardEvent))
			}
		}
	} else {
		s, ok := maybeGiftcardEvent.(*[]*GiftcardEvent)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeGiftcardEvent)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeGiftcardEvent))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &giftcardEventR{}
		}
		args[object.GiftcardID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &giftcardEventR{}
			}

			args[obj.GiftcardID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`giftcards`),
		qm.WhereIn(`giftcards.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.Query(e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Giftcard")
	}

	var resultSlice []*Giftcard
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Giftcard")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for giftcards")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for giftcards")
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Giftcard = foreign
		if foreign.R == nil {
			foreign.R = &giftcardR{}
		}
		foreign.R.GiftcardEvents = append(foreign.R.GiftcardEvents, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.GiftcardID == foreign.ID {
				local.R.Giftcard = foreign
				if foreign.R == nil {
					foreign.R = &giftcardR{}
				}
				foreign.R.GiftcardEvents = append(foreign.R.GiftcardEvents, local)
				break
			}
		}
	}

	return nil
}

// SetGiftcard of the giftcardEvent to the related item.
// Sets o.R.Giftcard to related.
// Adds o to related.R.GiftcardEvents.
func (o *GiftcardEvent) SetGiftcard(exec boil.Executor, insert bool, related *Giftcard) error {
	var err error
	if insert {
		if err = related.Insert(exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"giftcard_events\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"giftcard_id"}),
		strmangle.WhereClause("\"", "\"", 2, giftcardEventPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	if _, err = exec.Exec(updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.GiftcardID = related.ID
	if o.R == nil {
		o.R = &giftcardEventR{
			Giftcard: related,
		}
	} else {
		o.R.Giftcard = related
	}

	if related.R == nil {
		related.R = &giftcardR{
			GiftcardEvents: GiftcardEventSlice{o},
		}
	} else {
		related.R.GiftcardEvents = append(related.R.GiftcardEvents, o)
	}

	return nil
}

// GiftcardEvents retrieves all the records using an executor.
func GiftcardEvents(mods ...qm.QueryMod) giftcardEventQuery {
	mods = append(mods, qm.From("\"giftcard_events\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"giftcard_events\".*"})
	}

	return giftcardEventQuery{q}
}

// FindGiftcardEvent retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindGiftcardEvent(exec boil.Executor, iD string, selectCols ...string) (*GiftcardEvent, error) {
	giftcardEventObj := &GiftcardEvent{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"giftcard_events\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(nil, exec, giftcardEventObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "model: unable to select from giftcard_events")
	}

	return giftcardEventObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *GiftcardEvent) Insert(exec boil.Executor, columns boil.Columns) error {
	if o == nil {
		return errors.New("model: no giftcard_events provided for insertion")
	}

	var err error

	nzDefaults := queries.NonZeroDefaultSet(giftcardEventColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	giftcardEventInsertCacheMut.RLock()
	cache, cached := giftcardEventInsertCache[key]
	giftcardEventInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			giftcardEventAllColumns,
			giftcardEventColumnsWithDefault,
			giftcardEventColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(giftcardEventType, giftcardEventMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(giftcardEventType, giftcardEventMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"giftcard_events\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"giftcard_events\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "model: unable to insert into giftcard_events")
	}

	if !cached {
		giftcardEventInsertCacheMut.Lock()
		giftcardEventInsertCache[key] = cache
		giftcardEventInsertCacheMut.Unlock()
	}

	return nil
}

// Update uses an executor to update the GiftcardEvent.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *GiftcardEvent) Update(exec boil.Executor, columns boil.Columns) (int64, error) {
	var err error
	key := makeCacheKey(columns, nil)
	giftcardEventUpdateCacheMut.RLock()
	cache, cached := giftcardEventUpdateCache[key]
	giftcardEventUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			giftcardEventAllColumns,
			giftcardEventPrimaryKeyColumns,
		)
		if len(wl) == 0 {
			return 0, errors.New("model: unable to update giftcard_events, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"giftcard_events\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, giftcardEventPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(giftcardEventType, giftcardEventMapping, append(wl, giftcardEventPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	var result sql.Result
	result, err = exec.Exec(cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to update giftcard_events row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by update for giftcard_events")
	}

	if !cached {
		giftcardEventUpdateCacheMut.Lock()
		giftcardEventUpdateCache[key] = cache
		giftcardEventUpdateCacheMut.Unlock()
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values.
func (q giftcardEventQuery) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to update all for giftcard_events")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to retrieve rows affected for giftcard_events")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o GiftcardEventSlice) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("model: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), giftcardEventPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"giftcard_events\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, giftcardEventPrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to update all in giftcardEvent slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to retrieve rows affected all in update all giftcardEvent")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *GiftcardEvent) Upsert(exec boil.Executor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("model: no giftcard_events provided for upsert")
	}

	nzDefaults := queries.NonZeroDefaultSet(giftcardEventColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	giftcardEventUpsertCacheMut.RLock()
	cache, cached := giftcardEventUpsertCache[key]
	giftcardEventUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			giftcardEventAllColumns,
			giftcardEventColumnsWithDefault,
			giftcardEventColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			giftcardEventAllColumns,
			giftcardEventPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("model: unable to upsert giftcard_events, could not build update column list")
		}

		ret := strmangle.SetComplement(giftcardEventAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(giftcardEventPrimaryKeyColumns) == 0 {
				return errors.New("model: unable to upsert giftcard_events, could not build conflict column list")
			}

			conflict = make([]string, len(giftcardEventPrimaryKeyColumns))
			copy(conflict, giftcardEventPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"giftcard_events\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(giftcardEventType, giftcardEventMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(giftcardEventType, giftcardEventMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "model: unable to upsert giftcard_events")
	}

	if !cached {
		giftcardEventUpsertCacheMut.Lock()
		giftcardEventUpsertCache[key] = cache
		giftcardEventUpsertCacheMut.Unlock()
	}

	return nil
}

// Delete deletes a single GiftcardEvent record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *GiftcardEvent) Delete(exec boil.Executor) (int64, error) {
	if o == nil {
		return 0, errors.New("model: no GiftcardEvent provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), giftcardEventPrimaryKeyMapping)
	sql := "DELETE FROM \"giftcard_events\" WHERE \"id\"=$1"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to delete from giftcard_events")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by delete for giftcard_events")
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q giftcardEventQuery) DeleteAll(exec boil.Executor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("model: no giftcardEventQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to delete all from giftcard_events")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by deleteall for giftcard_events")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o GiftcardEventSlice) DeleteAll(exec boil.Executor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), giftcardEventPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"giftcard_events\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, giftcardEventPrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to delete all from giftcardEvent slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by deleteall for giftcard_events")
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *GiftcardEvent) Reload(exec boil.Executor) error {
	ret, err := FindGiftcardEvent(exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *GiftcardEventSlice) ReloadAll(exec boil.Executor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := GiftcardEventSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), giftcardEventPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"giftcard_events\".* FROM \"giftcard_events\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, giftcardEventPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(nil, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "model: unable to reload all in GiftcardEventSlice")
	}

	*o = slice

	return nil
}

// GiftcardEventExists checks if the GiftcardEvent row exists.
func GiftcardEventExists(exec boil.Executor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"giftcard_events\" where \"id\"=$1 limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, iD)
	}
	row := exec.QueryRow(sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "model: unable to check if giftcard_events exists")
	}

	return exists, nil
}

// Exists checks if the GiftcardEvent row exists.
func (o *GiftcardEvent) Exists(exec boil.Executor) (bool, error) {
	return GiftcardEventExists(exec, o.ID)
}
