// Code generated by SQLBoiler 4.16.2 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package model

import (
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// WishlistItemProductVariant is an object representing the database table.
type WishlistItemProductVariant struct {
	ID               string `boil:"id" json:"id" toml:"id" yaml:"id"`
	WishlistItemID   string `boil:"wishlist_item_id" json:"wishlist_item_id" toml:"wishlist_item_id" yaml:"wishlist_item_id"`
	ProductVariantID string `boil:"product_variant_id" json:"product_variant_id" toml:"product_variant_id" yaml:"product_variant_id"`

	R *wishlistItemProductVariantR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L wishlistItemProductVariantL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var WishlistItemProductVariantColumns = struct {
	ID               string
	WishlistItemID   string
	ProductVariantID string
}{
	ID:               "id",
	WishlistItemID:   "wishlist_item_id",
	ProductVariantID: "product_variant_id",
}

var WishlistItemProductVariantTableColumns = struct {
	ID               string
	WishlistItemID   string
	ProductVariantID string
}{
	ID:               "wishlist_item_product_variants.id",
	WishlistItemID:   "wishlist_item_product_variants.wishlist_item_id",
	ProductVariantID: "wishlist_item_product_variants.product_variant_id",
}

// Generated where

var WishlistItemProductVariantWhere = struct {
	ID               whereHelperstring
	WishlistItemID   whereHelperstring
	ProductVariantID whereHelperstring
}{
	ID:               whereHelperstring{field: "\"wishlist_item_product_variants\".\"id\""},
	WishlistItemID:   whereHelperstring{field: "\"wishlist_item_product_variants\".\"wishlist_item_id\""},
	ProductVariantID: whereHelperstring{field: "\"wishlist_item_product_variants\".\"product_variant_id\""},
}

// WishlistItemProductVariantRels is where relationship names are stored.
var WishlistItemProductVariantRels = struct {
	ProductVariant string
	WishlistItem   string
}{
	ProductVariant: "ProductVariant",
	WishlistItem:   "WishlistItem",
}

// wishlistItemProductVariantR is where relationships are stored.
type wishlistItemProductVariantR struct {
	ProductVariant *ProductVariant `boil:"ProductVariant" json:"ProductVariant" toml:"ProductVariant" yaml:"ProductVariant"`
	WishlistItem   *WishlistItem   `boil:"WishlistItem" json:"WishlistItem" toml:"WishlistItem" yaml:"WishlistItem"`
}

// NewStruct creates a new relationship struct
func (*wishlistItemProductVariantR) NewStruct() *wishlistItemProductVariantR {
	return &wishlistItemProductVariantR{}
}

func (r *wishlistItemProductVariantR) GetProductVariant() *ProductVariant {
	if r == nil {
		return nil
	}
	return r.ProductVariant
}

func (r *wishlistItemProductVariantR) GetWishlistItem() *WishlistItem {
	if r == nil {
		return nil
	}
	return r.WishlistItem
}

// wishlistItemProductVariantL is where Load methods for each relationship are stored.
type wishlistItemProductVariantL struct{}

var (
	wishlistItemProductVariantAllColumns            = []string{"id", "wishlist_item_id", "product_variant_id"}
	wishlistItemProductVariantColumnsWithoutDefault = []string{"wishlist_item_id", "product_variant_id"}
	wishlistItemProductVariantColumnsWithDefault    = []string{"id"}
	wishlistItemProductVariantPrimaryKeyColumns     = []string{"id"}
	wishlistItemProductVariantGeneratedColumns      = []string{}
)

type (
	// WishlistItemProductVariantSlice is an alias for a slice of pointers to WishlistItemProductVariant.
	// This should almost always be used instead of []WishlistItemProductVariant.
	WishlistItemProductVariantSlice []*WishlistItemProductVariant

	wishlistItemProductVariantQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	wishlistItemProductVariantType                 = reflect.TypeOf(&WishlistItemProductVariant{})
	wishlistItemProductVariantMapping              = queries.MakeStructMapping(wishlistItemProductVariantType)
	wishlistItemProductVariantPrimaryKeyMapping, _ = queries.BindMapping(wishlistItemProductVariantType, wishlistItemProductVariantMapping, wishlistItemProductVariantPrimaryKeyColumns)
	wishlistItemProductVariantInsertCacheMut       sync.RWMutex
	wishlistItemProductVariantInsertCache          = make(map[string]insertCache)
	wishlistItemProductVariantUpdateCacheMut       sync.RWMutex
	wishlistItemProductVariantUpdateCache          = make(map[string]updateCache)
	wishlistItemProductVariantUpsertCacheMut       sync.RWMutex
	wishlistItemProductVariantUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// One returns a single wishlistItemProductVariant record from the query.
func (q wishlistItemProductVariantQuery) One(exec boil.Executor) (*WishlistItemProductVariant, error) {
	o := &WishlistItemProductVariant{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(nil, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "model: failed to execute a one query for wishlist_item_product_variants")
	}

	return o, nil
}

// All returns all WishlistItemProductVariant records from the query.
func (q wishlistItemProductVariantQuery) All(exec boil.Executor) (WishlistItemProductVariantSlice, error) {
	var o []*WishlistItemProductVariant

	err := q.Bind(nil, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "model: failed to assign all query results to WishlistItemProductVariant slice")
	}

	return o, nil
}

// Count returns the count of all WishlistItemProductVariant records in the query.
func (q wishlistItemProductVariantQuery) Count(exec boil.Executor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to count wishlist_item_product_variants rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q wishlistItemProductVariantQuery) Exists(exec boil.Executor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "model: failed to check if wishlist_item_product_variants exists")
	}

	return count > 0, nil
}

// ProductVariant pointed to by the foreign key.
func (o *WishlistItemProductVariant) ProductVariant(mods ...qm.QueryMod) productVariantQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.ProductVariantID),
	}

	queryMods = append(queryMods, mods...)

	return ProductVariants(queryMods...)
}

// WishlistItem pointed to by the foreign key.
func (o *WishlistItemProductVariant) WishlistItem(mods ...qm.QueryMod) wishlistItemQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.WishlistItemID),
	}

	queryMods = append(queryMods, mods...)

	return WishlistItems(queryMods...)
}

// LoadProductVariant allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (wishlistItemProductVariantL) LoadProductVariant(e boil.Executor, singular bool, maybeWishlistItemProductVariant interface{}, mods queries.Applicator) error {
	var slice []*WishlistItemProductVariant
	var object *WishlistItemProductVariant

	if singular {
		var ok bool
		object, ok = maybeWishlistItemProductVariant.(*WishlistItemProductVariant)
		if !ok {
			object = new(WishlistItemProductVariant)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWishlistItemProductVariant)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWishlistItemProductVariant))
			}
		}
	} else {
		s, ok := maybeWishlistItemProductVariant.(*[]*WishlistItemProductVariant)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWishlistItemProductVariant)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWishlistItemProductVariant))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &wishlistItemProductVariantR{}
		}
		args[object.ProductVariantID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &wishlistItemProductVariantR{}
			}

			args[obj.ProductVariantID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`product_variants`),
		qm.WhereIn(`product_variants.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.Query(e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load ProductVariant")
	}

	var resultSlice []*ProductVariant
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice ProductVariant")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for product_variants")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for product_variants")
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.ProductVariant = foreign
		if foreign.R == nil {
			foreign.R = &productVariantR{}
		}
		foreign.R.WishlistItemProductVariants = append(foreign.R.WishlistItemProductVariants, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.ProductVariantID == foreign.ID {
				local.R.ProductVariant = foreign
				if foreign.R == nil {
					foreign.R = &productVariantR{}
				}
				foreign.R.WishlistItemProductVariants = append(foreign.R.WishlistItemProductVariants, local)
				break
			}
		}
	}

	return nil
}

// LoadWishlistItem allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (wishlistItemProductVariantL) LoadWishlistItem(e boil.Executor, singular bool, maybeWishlistItemProductVariant interface{}, mods queries.Applicator) error {
	var slice []*WishlistItemProductVariant
	var object *WishlistItemProductVariant

	if singular {
		var ok bool
		object, ok = maybeWishlistItemProductVariant.(*WishlistItemProductVariant)
		if !ok {
			object = new(WishlistItemProductVariant)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWishlistItemProductVariant)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWishlistItemProductVariant))
			}
		}
	} else {
		s, ok := maybeWishlistItemProductVariant.(*[]*WishlistItemProductVariant)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWishlistItemProductVariant)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWishlistItemProductVariant))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &wishlistItemProductVariantR{}
		}
		args[object.WishlistItemID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &wishlistItemProductVariantR{}
			}

			args[obj.WishlistItemID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`wishlist_items`),
		qm.WhereIn(`wishlist_items.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.Query(e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load WishlistItem")
	}

	var resultSlice []*WishlistItem
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice WishlistItem")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for wishlist_items")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for wishlist_items")
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.WishlistItem = foreign
		if foreign.R == nil {
			foreign.R = &wishlistItemR{}
		}
		foreign.R.WishlistItemProductVariants = append(foreign.R.WishlistItemProductVariants, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.WishlistItemID == foreign.ID {
				local.R.WishlistItem = foreign
				if foreign.R == nil {
					foreign.R = &wishlistItemR{}
				}
				foreign.R.WishlistItemProductVariants = append(foreign.R.WishlistItemProductVariants, local)
				break
			}
		}
	}

	return nil
}

// SetProductVariant of the wishlistItemProductVariant to the related item.
// Sets o.R.ProductVariant to related.
// Adds o to related.R.WishlistItemProductVariants.
func (o *WishlistItemProductVariant) SetProductVariant(exec boil.Executor, insert bool, related *ProductVariant) error {
	var err error
	if insert {
		if err = related.Insert(exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"wishlist_item_product_variants\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"product_variant_id"}),
		strmangle.WhereClause("\"", "\"", 2, wishlistItemProductVariantPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	if _, err = exec.Exec(updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.ProductVariantID = related.ID
	if o.R == nil {
		o.R = &wishlistItemProductVariantR{
			ProductVariant: related,
		}
	} else {
		o.R.ProductVariant = related
	}

	if related.R == nil {
		related.R = &productVariantR{
			WishlistItemProductVariants: WishlistItemProductVariantSlice{o},
		}
	} else {
		related.R.WishlistItemProductVariants = append(related.R.WishlistItemProductVariants, o)
	}

	return nil
}

// SetWishlistItem of the wishlistItemProductVariant to the related item.
// Sets o.R.WishlistItem to related.
// Adds o to related.R.WishlistItemProductVariants.
func (o *WishlistItemProductVariant) SetWishlistItem(exec boil.Executor, insert bool, related *WishlistItem) error {
	var err error
	if insert {
		if err = related.Insert(exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"wishlist_item_product_variants\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"wishlist_item_id"}),
		strmangle.WhereClause("\"", "\"", 2, wishlistItemProductVariantPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	if _, err = exec.Exec(updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.WishlistItemID = related.ID
	if o.R == nil {
		o.R = &wishlistItemProductVariantR{
			WishlistItem: related,
		}
	} else {
		o.R.WishlistItem = related
	}

	if related.R == nil {
		related.R = &wishlistItemR{
			WishlistItemProductVariants: WishlistItemProductVariantSlice{o},
		}
	} else {
		related.R.WishlistItemProductVariants = append(related.R.WishlistItemProductVariants, o)
	}

	return nil
}

// WishlistItemProductVariants retrieves all the records using an executor.
func WishlistItemProductVariants(mods ...qm.QueryMod) wishlistItemProductVariantQuery {
	mods = append(mods, qm.From("\"wishlist_item_product_variants\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"wishlist_item_product_variants\".*"})
	}

	return wishlistItemProductVariantQuery{q}
}

// FindWishlistItemProductVariant retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindWishlistItemProductVariant(exec boil.Executor, iD string, selectCols ...string) (*WishlistItemProductVariant, error) {
	wishlistItemProductVariantObj := &WishlistItemProductVariant{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"wishlist_item_product_variants\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(nil, exec, wishlistItemProductVariantObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "model: unable to select from wishlist_item_product_variants")
	}

	return wishlistItemProductVariantObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *WishlistItemProductVariant) Insert(exec boil.Executor, columns boil.Columns) error {
	if o == nil {
		return errors.New("model: no wishlist_item_product_variants provided for insertion")
	}

	var err error

	nzDefaults := queries.NonZeroDefaultSet(wishlistItemProductVariantColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	wishlistItemProductVariantInsertCacheMut.RLock()
	cache, cached := wishlistItemProductVariantInsertCache[key]
	wishlistItemProductVariantInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			wishlistItemProductVariantAllColumns,
			wishlistItemProductVariantColumnsWithDefault,
			wishlistItemProductVariantColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(wishlistItemProductVariantType, wishlistItemProductVariantMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(wishlistItemProductVariantType, wishlistItemProductVariantMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"wishlist_item_product_variants\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"wishlist_item_product_variants\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "model: unable to insert into wishlist_item_product_variants")
	}

	if !cached {
		wishlistItemProductVariantInsertCacheMut.Lock()
		wishlistItemProductVariantInsertCache[key] = cache
		wishlistItemProductVariantInsertCacheMut.Unlock()
	}

	return nil
}

// Update uses an executor to update the WishlistItemProductVariant.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *WishlistItemProductVariant) Update(exec boil.Executor, columns boil.Columns) (int64, error) {
	var err error
	key := makeCacheKey(columns, nil)
	wishlistItemProductVariantUpdateCacheMut.RLock()
	cache, cached := wishlistItemProductVariantUpdateCache[key]
	wishlistItemProductVariantUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			wishlistItemProductVariantAllColumns,
			wishlistItemProductVariantPrimaryKeyColumns,
		)
		if len(wl) == 0 {
			return 0, errors.New("model: unable to update wishlist_item_product_variants, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"wishlist_item_product_variants\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, wishlistItemProductVariantPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(wishlistItemProductVariantType, wishlistItemProductVariantMapping, append(wl, wishlistItemProductVariantPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	var result sql.Result
	result, err = exec.Exec(cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to update wishlist_item_product_variants row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by update for wishlist_item_product_variants")
	}

	if !cached {
		wishlistItemProductVariantUpdateCacheMut.Lock()
		wishlistItemProductVariantUpdateCache[key] = cache
		wishlistItemProductVariantUpdateCacheMut.Unlock()
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values.
func (q wishlistItemProductVariantQuery) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to update all for wishlist_item_product_variants")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to retrieve rows affected for wishlist_item_product_variants")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o WishlistItemProductVariantSlice) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("model: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), wishlistItemProductVariantPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"wishlist_item_product_variants\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, wishlistItemProductVariantPrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to update all in wishlistItemProductVariant slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to retrieve rows affected all in update all wishlistItemProductVariant")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *WishlistItemProductVariant) Upsert(exec boil.Executor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("model: no wishlist_item_product_variants provided for upsert")
	}

	nzDefaults := queries.NonZeroDefaultSet(wishlistItemProductVariantColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	wishlistItemProductVariantUpsertCacheMut.RLock()
	cache, cached := wishlistItemProductVariantUpsertCache[key]
	wishlistItemProductVariantUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			wishlistItemProductVariantAllColumns,
			wishlistItemProductVariantColumnsWithDefault,
			wishlistItemProductVariantColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			wishlistItemProductVariantAllColumns,
			wishlistItemProductVariantPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("model: unable to upsert wishlist_item_product_variants, could not build update column list")
		}

		ret := strmangle.SetComplement(wishlistItemProductVariantAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(wishlistItemProductVariantPrimaryKeyColumns) == 0 {
				return errors.New("model: unable to upsert wishlist_item_product_variants, could not build conflict column list")
			}

			conflict = make([]string, len(wishlistItemProductVariantPrimaryKeyColumns))
			copy(conflict, wishlistItemProductVariantPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"wishlist_item_product_variants\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(wishlistItemProductVariantType, wishlistItemProductVariantMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(wishlistItemProductVariantType, wishlistItemProductVariantMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "model: unable to upsert wishlist_item_product_variants")
	}

	if !cached {
		wishlistItemProductVariantUpsertCacheMut.Lock()
		wishlistItemProductVariantUpsertCache[key] = cache
		wishlistItemProductVariantUpsertCacheMut.Unlock()
	}

	return nil
}

// Delete deletes a single WishlistItemProductVariant record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *WishlistItemProductVariant) Delete(exec boil.Executor) (int64, error) {
	if o == nil {
		return 0, errors.New("model: no WishlistItemProductVariant provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), wishlistItemProductVariantPrimaryKeyMapping)
	sql := "DELETE FROM \"wishlist_item_product_variants\" WHERE \"id\"=$1"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to delete from wishlist_item_product_variants")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by delete for wishlist_item_product_variants")
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q wishlistItemProductVariantQuery) DeleteAll(exec boil.Executor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("model: no wishlistItemProductVariantQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to delete all from wishlist_item_product_variants")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by deleteall for wishlist_item_product_variants")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o WishlistItemProductVariantSlice) DeleteAll(exec boil.Executor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), wishlistItemProductVariantPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"wishlist_item_product_variants\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, wishlistItemProductVariantPrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to delete all from wishlistItemProductVariant slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by deleteall for wishlist_item_product_variants")
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *WishlistItemProductVariant) Reload(exec boil.Executor) error {
	ret, err := FindWishlistItemProductVariant(exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *WishlistItemProductVariantSlice) ReloadAll(exec boil.Executor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := WishlistItemProductVariantSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), wishlistItemProductVariantPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"wishlist_item_product_variants\".* FROM \"wishlist_item_product_variants\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, wishlistItemProductVariantPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(nil, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "model: unable to reload all in WishlistItemProductVariantSlice")
	}

	*o = slice

	return nil
}

// WishlistItemProductVariantExists checks if the WishlistItemProductVariant row exists.
func WishlistItemProductVariantExists(exec boil.Executor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"wishlist_item_product_variants\" where \"id\"=$1 limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, iD)
	}
	row := exec.QueryRow(sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "model: unable to check if wishlist_item_product_variants exists")
	}

	return exists, nil
}

// Exists checks if the WishlistItemProductVariant row exists.
func (o *WishlistItemProductVariant) Exists(exec boil.Executor) (bool, error) {
	return WishlistItemProductVariantExists(exec, o.ID)
}
