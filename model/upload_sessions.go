// Code generated by SQLBoiler 4.16.2 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package model

import (
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// UploadSession is an object representing the database table.
type UploadSession struct {
	ID         string     `boil:"id" json:"id" toml:"id" yaml:"id"`
	Type       UploadType `boil:"type" json:"type" toml:"type" yaml:"type"`
	CreatedAt  int64      `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UserID     string     `boil:"user_id" json:"user_id" toml:"user_id" yaml:"user_id"`
	FileName   string     `boil:"file_name" json:"file_name" toml:"file_name" yaml:"file_name"`
	Path       string     `boil:"path" json:"path" toml:"path" yaml:"path"`
	FileSize   int64      `boil:"file_size" json:"file_size" toml:"file_size" yaml:"file_size"`
	FileOffset int64      `boil:"file_offset" json:"file_offset" toml:"file_offset" yaml:"file_offset"`

	R *uploadSessionR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L uploadSessionL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var UploadSessionColumns = struct {
	ID         string
	Type       string
	CreatedAt  string
	UserID     string
	FileName   string
	Path       string
	FileSize   string
	FileOffset string
}{
	ID:         "id",
	Type:       "type",
	CreatedAt:  "created_at",
	UserID:     "user_id",
	FileName:   "file_name",
	Path:       "path",
	FileSize:   "file_size",
	FileOffset: "file_offset",
}

var UploadSessionTableColumns = struct {
	ID         string
	Type       string
	CreatedAt  string
	UserID     string
	FileName   string
	Path       string
	FileSize   string
	FileOffset string
}{
	ID:         "upload_sessions.id",
	Type:       "upload_sessions.type",
	CreatedAt:  "upload_sessions.created_at",
	UserID:     "upload_sessions.user_id",
	FileName:   "upload_sessions.file_name",
	Path:       "upload_sessions.path",
	FileSize:   "upload_sessions.file_size",
	FileOffset: "upload_sessions.file_offset",
}

// Generated where

type whereHelperUploadType struct{ field string }

func (w whereHelperUploadType) EQ(x UploadType) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.EQ, x)
}
func (w whereHelperUploadType) NEQ(x UploadType) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.NEQ, x)
}
func (w whereHelperUploadType) LT(x UploadType) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LT, x)
}
func (w whereHelperUploadType) LTE(x UploadType) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LTE, x)
}
func (w whereHelperUploadType) GT(x UploadType) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GT, x)
}
func (w whereHelperUploadType) GTE(x UploadType) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GTE, x)
}
func (w whereHelperUploadType) IN(slice []UploadType) qm.QueryMod {
	values := make([]any, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereIn(fmt.Sprintf("%s IN ?", w.field), values...)
}
func (w whereHelperUploadType) NIN(slice []UploadType) qm.QueryMod {
	values := make([]any, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereNotIn(fmt.Sprintf("%s NOT IN ?", w.field), values...)
}

var UploadSessionWhere = struct {
	ID         whereHelperstring
	Type       whereHelperUploadType
	CreatedAt  whereHelperint64
	UserID     whereHelperstring
	FileName   whereHelperstring
	Path       whereHelperstring
	FileSize   whereHelperint64
	FileOffset whereHelperint64
}{
	ID:         whereHelperstring{field: "\"upload_sessions\".\"id\""},
	Type:       whereHelperUploadType{field: "\"upload_sessions\".\"type\""},
	CreatedAt:  whereHelperint64{field: "\"upload_sessions\".\"created_at\""},
	UserID:     whereHelperstring{field: "\"upload_sessions\".\"user_id\""},
	FileName:   whereHelperstring{field: "\"upload_sessions\".\"file_name\""},
	Path:       whereHelperstring{field: "\"upload_sessions\".\"path\""},
	FileSize:   whereHelperint64{field: "\"upload_sessions\".\"file_size\""},
	FileOffset: whereHelperint64{field: "\"upload_sessions\".\"file_offset\""},
}

// UploadSessionRels is where relationship names are stored.
var UploadSessionRels = struct {
}{}

// uploadSessionR is where relationships are stored.
type uploadSessionR struct {
}

// NewStruct creates a new relationship struct
func (*uploadSessionR) NewStruct() *uploadSessionR {
	return &uploadSessionR{}
}

// uploadSessionL is where Load methods for each relationship are stored.
type uploadSessionL struct{}

var (
	uploadSessionAllColumns            = []string{"id", "type", "created_at", "user_id", "file_name", "path", "file_size", "file_offset"}
	uploadSessionColumnsWithoutDefault = []string{"id", "type", "created_at", "user_id", "file_name", "path", "file_size", "file_offset"}
	uploadSessionColumnsWithDefault    = []string{}
	uploadSessionPrimaryKeyColumns     = []string{"id"}
	uploadSessionGeneratedColumns      = []string{}
)

type (
	// UploadSessionSlice is an alias for a slice of pointers to UploadSession.
	// This should almost always be used instead of []UploadSession.
	UploadSessionSlice []*UploadSession

	uploadSessionQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	uploadSessionType                 = reflect.TypeOf(&UploadSession{})
	uploadSessionMapping              = queries.MakeStructMapping(uploadSessionType)
	uploadSessionPrimaryKeyMapping, _ = queries.BindMapping(uploadSessionType, uploadSessionMapping, uploadSessionPrimaryKeyColumns)
	uploadSessionInsertCacheMut       sync.RWMutex
	uploadSessionInsertCache          = make(map[string]insertCache)
	uploadSessionUpdateCacheMut       sync.RWMutex
	uploadSessionUpdateCache          = make(map[string]updateCache)
	uploadSessionUpsertCacheMut       sync.RWMutex
	uploadSessionUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// One returns a single uploadSession record from the query.
func (q uploadSessionQuery) One(exec boil.Executor) (*UploadSession, error) {
	o := &UploadSession{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(nil, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "model: failed to execute a one query for upload_sessions")
	}

	return o, nil
}

// All returns all UploadSession records from the query.
func (q uploadSessionQuery) All(exec boil.Executor) (UploadSessionSlice, error) {
	var o []*UploadSession

	err := q.Bind(nil, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "model: failed to assign all query results to UploadSession slice")
	}

	return o, nil
}

// Count returns the count of all UploadSession records in the query.
func (q uploadSessionQuery) Count(exec boil.Executor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to count upload_sessions rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q uploadSessionQuery) Exists(exec boil.Executor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "model: failed to check if upload_sessions exists")
	}

	return count > 0, nil
}

// UploadSessions retrieves all the records using an executor.
func UploadSessions(mods ...qm.QueryMod) uploadSessionQuery {
	mods = append(mods, qm.From("\"upload_sessions\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"upload_sessions\".*"})
	}

	return uploadSessionQuery{q}
}

// FindUploadSession retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindUploadSession(exec boil.Executor, iD string, selectCols ...string) (*UploadSession, error) {
	uploadSessionObj := &UploadSession{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"upload_sessions\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(nil, exec, uploadSessionObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "model: unable to select from upload_sessions")
	}

	return uploadSessionObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *UploadSession) Insert(exec boil.Executor, columns boil.Columns) error {
	if o == nil {
		return errors.New("model: no upload_sessions provided for insertion")
	}

	var err error

	nzDefaults := queries.NonZeroDefaultSet(uploadSessionColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	uploadSessionInsertCacheMut.RLock()
	cache, cached := uploadSessionInsertCache[key]
	uploadSessionInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			uploadSessionAllColumns,
			uploadSessionColumnsWithDefault,
			uploadSessionColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(uploadSessionType, uploadSessionMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(uploadSessionType, uploadSessionMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"upload_sessions\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"upload_sessions\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "model: unable to insert into upload_sessions")
	}

	if !cached {
		uploadSessionInsertCacheMut.Lock()
		uploadSessionInsertCache[key] = cache
		uploadSessionInsertCacheMut.Unlock()
	}

	return nil
}

// Update uses an executor to update the UploadSession.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *UploadSession) Update(exec boil.Executor, columns boil.Columns) (int64, error) {
	var err error
	key := makeCacheKey(columns, nil)
	uploadSessionUpdateCacheMut.RLock()
	cache, cached := uploadSessionUpdateCache[key]
	uploadSessionUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			uploadSessionAllColumns,
			uploadSessionPrimaryKeyColumns,
		)
		if len(wl) == 0 {
			return 0, errors.New("model: unable to update upload_sessions, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"upload_sessions\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, uploadSessionPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(uploadSessionType, uploadSessionMapping, append(wl, uploadSessionPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	var result sql.Result
	result, err = exec.Exec(cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to update upload_sessions row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by update for upload_sessions")
	}

	if !cached {
		uploadSessionUpdateCacheMut.Lock()
		uploadSessionUpdateCache[key] = cache
		uploadSessionUpdateCacheMut.Unlock()
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values.
func (q uploadSessionQuery) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to update all for upload_sessions")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to retrieve rows affected for upload_sessions")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o UploadSessionSlice) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("model: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]any, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), uploadSessionPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"upload_sessions\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, uploadSessionPrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to update all in uploadSession slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to retrieve rows affected all in update all uploadSession")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *UploadSession) Upsert(exec boil.Executor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("model: no upload_sessions provided for upsert")
	}

	nzDefaults := queries.NonZeroDefaultSet(uploadSessionColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	uploadSessionUpsertCacheMut.RLock()
	cache, cached := uploadSessionUpsertCache[key]
	uploadSessionUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			uploadSessionAllColumns,
			uploadSessionColumnsWithDefault,
			uploadSessionColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			uploadSessionAllColumns,
			uploadSessionPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("model: unable to upsert upload_sessions, could not build update column list")
		}

		ret := strmangle.SetComplement(uploadSessionAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(uploadSessionPrimaryKeyColumns) == 0 {
				return errors.New("model: unable to upsert upload_sessions, could not build conflict column list")
			}

			conflict = make([]string, len(uploadSessionPrimaryKeyColumns))
			copy(conflict, uploadSessionPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"upload_sessions\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(uploadSessionType, uploadSessionMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(uploadSessionType, uploadSessionMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []any
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "model: unable to upsert upload_sessions")
	}

	if !cached {
		uploadSessionUpsertCacheMut.Lock()
		uploadSessionUpsertCache[key] = cache
		uploadSessionUpsertCacheMut.Unlock()
	}

	return nil
}

// Delete deletes a single UploadSession record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *UploadSession) Delete(exec boil.Executor) (int64, error) {
	if o == nil {
		return 0, errors.New("model: no UploadSession provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uploadSessionPrimaryKeyMapping)
	sql := "DELETE FROM \"upload_sessions\" WHERE \"id\"=$1"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to delete from upload_sessions")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by delete for upload_sessions")
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q uploadSessionQuery) DeleteAll(exec boil.Executor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("model: no uploadSessionQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to delete all from upload_sessions")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by deleteall for upload_sessions")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o UploadSessionSlice) DeleteAll(exec boil.Executor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var args []any
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), uploadSessionPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"upload_sessions\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, uploadSessionPrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to delete all from uploadSession slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by deleteall for upload_sessions")
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *UploadSession) Reload(exec boil.Executor) error {
	ret, err := FindUploadSession(exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *UploadSessionSlice) ReloadAll(exec boil.Executor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := UploadSessionSlice{}
	var args []any
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), uploadSessionPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"upload_sessions\".* FROM \"upload_sessions\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, uploadSessionPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(nil, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "model: unable to reload all in UploadSessionSlice")
	}

	*o = slice

	return nil
}

// UploadSessionExists checks if the UploadSession row exists.
func UploadSessionExists(exec boil.Executor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"upload_sessions\" where \"id\"=$1 limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, iD)
	}
	row := exec.QueryRow(sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "model: unable to check if upload_sessions exists")
	}

	return exists, nil
}

// Exists checks if the UploadSession row exists.
func (o *UploadSession) Exists(exec boil.Executor) (bool, error) {
	return UploadSessionExists(exec, o.ID)
}
