// Code generated by SQLBoiler 4.16.2 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package model

import (
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// UserAccessToken is an object representing the database table.
type UserAccessToken struct {
	ID          string `boil:"id" json:"id" toml:"id" yaml:"id"`
	Token       string `boil:"token" json:"token" toml:"token" yaml:"token"`
	UserID      string `boil:"user_id" json:"user_id" toml:"user_id" yaml:"user_id"`
	Description string `boil:"description" json:"description" toml:"description" yaml:"description"`
	IsActive    bool   `boil:"is_active" json:"is_active" toml:"is_active" yaml:"is_active"`

	R *userAccessTokenR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L userAccessTokenL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var UserAccessTokenColumns = struct {
	ID          string
	Token       string
	UserID      string
	Description string
	IsActive    string
}{
	ID:          "id",
	Token:       "token",
	UserID:      "user_id",
	Description: "description",
	IsActive:    "is_active",
}

var UserAccessTokenTableColumns = struct {
	ID          string
	Token       string
	UserID      string
	Description string
	IsActive    string
}{
	ID:          "user_access_tokens.id",
	Token:       "user_access_tokens.token",
	UserID:      "user_access_tokens.user_id",
	Description: "user_access_tokens.description",
	IsActive:    "user_access_tokens.is_active",
}

// Generated where

var UserAccessTokenWhere = struct {
	ID          whereHelperstring
	Token       whereHelperstring
	UserID      whereHelperstring
	Description whereHelperstring
	IsActive    whereHelperbool
}{
	ID:          whereHelperstring{field: "\"user_access_tokens\".\"id\""},
	Token:       whereHelperstring{field: "\"user_access_tokens\".\"token\""},
	UserID:      whereHelperstring{field: "\"user_access_tokens\".\"user_id\""},
	Description: whereHelperstring{field: "\"user_access_tokens\".\"description\""},
	IsActive:    whereHelperbool{field: "\"user_access_tokens\".\"is_active\""},
}

// UserAccessTokenRels is where relationship names are stored.
var UserAccessTokenRels = struct {
	User string
}{
	User: "User",
}

// userAccessTokenR is where relationships are stored.
type userAccessTokenR struct {
	User *User `boil:"User" json:"User" toml:"User" yaml:"User"`
}

// NewStruct creates a new relationship struct
func (*userAccessTokenR) NewStruct() *userAccessTokenR {
	return &userAccessTokenR{}
}

func (r *userAccessTokenR) GetUser() *User {
	if r == nil {
		return nil
	}
	return r.User
}

// userAccessTokenL is where Load methods for each relationship are stored.
type userAccessTokenL struct{}

var (
	userAccessTokenAllColumns            = []string{"id", "token", "user_id", "description", "is_active"}
	userAccessTokenColumnsWithoutDefault = []string{"id", "token", "user_id", "description"}
	userAccessTokenColumnsWithDefault    = []string{"is_active"}
	userAccessTokenPrimaryKeyColumns     = []string{"id"}
	userAccessTokenGeneratedColumns      = []string{}
)

type (
	// UserAccessTokenSlice is an alias for a slice of pointers to UserAccessToken.
	// This should almost always be used instead of []UserAccessToken.
	UserAccessTokenSlice []*UserAccessToken

	userAccessTokenQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	userAccessTokenType                 = reflect.TypeOf(&UserAccessToken{})
	userAccessTokenMapping              = queries.MakeStructMapping(userAccessTokenType)
	userAccessTokenPrimaryKeyMapping, _ = queries.BindMapping(userAccessTokenType, userAccessTokenMapping, userAccessTokenPrimaryKeyColumns)
	userAccessTokenInsertCacheMut       sync.RWMutex
	userAccessTokenInsertCache          = make(map[string]insertCache)
	userAccessTokenUpdateCacheMut       sync.RWMutex
	userAccessTokenUpdateCache          = make(map[string]updateCache)
	userAccessTokenUpsertCacheMut       sync.RWMutex
	userAccessTokenUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// One returns a single userAccessToken record from the query.
func (q userAccessTokenQuery) One(exec boil.Executor) (*UserAccessToken, error) {
	o := &UserAccessToken{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(nil, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "model: failed to execute a one query for user_access_tokens")
	}

	return o, nil
}

// All returns all UserAccessToken records from the query.
func (q userAccessTokenQuery) All(exec boil.Executor) (UserAccessTokenSlice, error) {
	var o []*UserAccessToken

	err := q.Bind(nil, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "model: failed to assign all query results to UserAccessToken slice")
	}

	return o, nil
}

// Count returns the count of all UserAccessToken records in the query.
func (q userAccessTokenQuery) Count(exec boil.Executor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to count user_access_tokens rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q userAccessTokenQuery) Exists(exec boil.Executor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "model: failed to check if user_access_tokens exists")
	}

	return count > 0, nil
}

// User pointed to by the foreign key.
func (o *UserAccessToken) User(mods ...qm.QueryMod) userQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.UserID),
	}

	queryMods = append(queryMods, mods...)

	return Users(queryMods...)
}

// LoadUser allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (userAccessTokenL) LoadUser(e boil.Executor, singular bool, maybeUserAccessToken any, mods queries.Applicator) error {
	var slice []*UserAccessToken
	var object *UserAccessToken

	if singular {
		var ok bool
		object, ok = maybeUserAccessToken.(*UserAccessToken)
		if !ok {
			object = new(UserAccessToken)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeUserAccessToken)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeUserAccessToken))
			}
		}
	} else {
		s, ok := maybeUserAccessToken.(*[]*UserAccessToken)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeUserAccessToken)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeUserAccessToken))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &userAccessTokenR{}
		}
		args[object.UserID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &userAccessTokenR{}
			}

			args[obj.UserID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`users`),
		qm.WhereIn(`users.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.Query(e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load User")
	}

	var resultSlice []*User
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice User")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for users")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for users")
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.User = foreign
		if foreign.R == nil {
			foreign.R = &userR{}
		}
		foreign.R.UserAccessTokens = append(foreign.R.UserAccessTokens, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.UserID == foreign.ID {
				local.R.User = foreign
				if foreign.R == nil {
					foreign.R = &userR{}
				}
				foreign.R.UserAccessTokens = append(foreign.R.UserAccessTokens, local)
				break
			}
		}
	}

	return nil
}

// SetUser of the userAccessToken to the related item.
// Sets o.R.User to related.
// Adds o to related.R.UserAccessTokens.
func (o *UserAccessToken) SetUser(exec boil.Executor, insert bool, related *User) error {
	var err error
	if insert {
		if err = related.Insert(exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"user_access_tokens\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"user_id"}),
		strmangle.WhereClause("\"", "\"", 2, userAccessTokenPrimaryKeyColumns),
	)
	values := []any{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	if _, err = exec.Exec(updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.UserID = related.ID
	if o.R == nil {
		o.R = &userAccessTokenR{
			User: related,
		}
	} else {
		o.R.User = related
	}

	if related.R == nil {
		related.R = &userR{
			UserAccessTokens: UserAccessTokenSlice{o},
		}
	} else {
		related.R.UserAccessTokens = append(related.R.UserAccessTokens, o)
	}

	return nil
}

// UserAccessTokens retrieves all the records using an executor.
func UserAccessTokens(mods ...qm.QueryMod) userAccessTokenQuery {
	mods = append(mods, qm.From("\"user_access_tokens\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"user_access_tokens\".*"})
	}

	return userAccessTokenQuery{q}
}

// FindUserAccessToken retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindUserAccessToken(exec boil.Executor, iD string, selectCols ...string) (*UserAccessToken, error) {
	userAccessTokenObj := &UserAccessToken{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"user_access_tokens\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(nil, exec, userAccessTokenObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "model: unable to select from user_access_tokens")
	}

	return userAccessTokenObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *UserAccessToken) Insert(exec boil.Executor, columns boil.Columns) error {
	if o == nil {
		return errors.New("model: no user_access_tokens provided for insertion")
	}

	var err error

	nzDefaults := queries.NonZeroDefaultSet(userAccessTokenColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	userAccessTokenInsertCacheMut.RLock()
	cache, cached := userAccessTokenInsertCache[key]
	userAccessTokenInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			userAccessTokenAllColumns,
			userAccessTokenColumnsWithDefault,
			userAccessTokenColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(userAccessTokenType, userAccessTokenMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(userAccessTokenType, userAccessTokenMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"user_access_tokens\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"user_access_tokens\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "model: unable to insert into user_access_tokens")
	}

	if !cached {
		userAccessTokenInsertCacheMut.Lock()
		userAccessTokenInsertCache[key] = cache
		userAccessTokenInsertCacheMut.Unlock()
	}

	return nil
}

// Update uses an executor to update the UserAccessToken.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *UserAccessToken) Update(exec boil.Executor, columns boil.Columns) (int64, error) {
	var err error
	key := makeCacheKey(columns, nil)
	userAccessTokenUpdateCacheMut.RLock()
	cache, cached := userAccessTokenUpdateCache[key]
	userAccessTokenUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			userAccessTokenAllColumns,
			userAccessTokenPrimaryKeyColumns,
		)
		if len(wl) == 0 {
			return 0, errors.New("model: unable to update user_access_tokens, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"user_access_tokens\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, userAccessTokenPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(userAccessTokenType, userAccessTokenMapping, append(wl, userAccessTokenPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	var result sql.Result
	result, err = exec.Exec(cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to update user_access_tokens row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by update for user_access_tokens")
	}

	if !cached {
		userAccessTokenUpdateCacheMut.Lock()
		userAccessTokenUpdateCache[key] = cache
		userAccessTokenUpdateCacheMut.Unlock()
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values.
func (q userAccessTokenQuery) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to update all for user_access_tokens")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to retrieve rows affected for user_access_tokens")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o UserAccessTokenSlice) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("model: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]any, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), userAccessTokenPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"user_access_tokens\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, userAccessTokenPrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to update all in userAccessToken slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to retrieve rows affected all in update all userAccessToken")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *UserAccessToken) Upsert(exec boil.Executor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("model: no user_access_tokens provided for upsert")
	}

	nzDefaults := queries.NonZeroDefaultSet(userAccessTokenColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	userAccessTokenUpsertCacheMut.RLock()
	cache, cached := userAccessTokenUpsertCache[key]
	userAccessTokenUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			userAccessTokenAllColumns,
			userAccessTokenColumnsWithDefault,
			userAccessTokenColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			userAccessTokenAllColumns,
			userAccessTokenPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("model: unable to upsert user_access_tokens, could not build update column list")
		}

		ret := strmangle.SetComplement(userAccessTokenAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(userAccessTokenPrimaryKeyColumns) == 0 {
				return errors.New("model: unable to upsert user_access_tokens, could not build conflict column list")
			}

			conflict = make([]string, len(userAccessTokenPrimaryKeyColumns))
			copy(conflict, userAccessTokenPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"user_access_tokens\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(userAccessTokenType, userAccessTokenMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(userAccessTokenType, userAccessTokenMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []any
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "model: unable to upsert user_access_tokens")
	}

	if !cached {
		userAccessTokenUpsertCacheMut.Lock()
		userAccessTokenUpsertCache[key] = cache
		userAccessTokenUpsertCacheMut.Unlock()
	}

	return nil
}

// Delete deletes a single UserAccessToken record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *UserAccessToken) Delete(exec boil.Executor) (int64, error) {
	if o == nil {
		return 0, errors.New("model: no UserAccessToken provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), userAccessTokenPrimaryKeyMapping)
	sql := "DELETE FROM \"user_access_tokens\" WHERE \"id\"=$1"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to delete from user_access_tokens")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by delete for user_access_tokens")
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q userAccessTokenQuery) DeleteAll(exec boil.Executor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("model: no userAccessTokenQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to delete all from user_access_tokens")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by deleteall for user_access_tokens")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o UserAccessTokenSlice) DeleteAll(exec boil.Executor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var args []any
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), userAccessTokenPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"user_access_tokens\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, userAccessTokenPrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to delete all from userAccessToken slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by deleteall for user_access_tokens")
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *UserAccessToken) Reload(exec boil.Executor) error {
	ret, err := FindUserAccessToken(exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *UserAccessTokenSlice) ReloadAll(exec boil.Executor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := UserAccessTokenSlice{}
	var args []any
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), userAccessTokenPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"user_access_tokens\".* FROM \"user_access_tokens\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, userAccessTokenPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(nil, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "model: unable to reload all in UserAccessTokenSlice")
	}

	*o = slice

	return nil
}

// UserAccessTokenExists checks if the UserAccessToken row exists.
func UserAccessTokenExists(exec boil.Executor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"user_access_tokens\" where \"id\"=$1 limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, iD)
	}
	row := exec.QueryRow(sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "model: unable to check if user_access_tokens exists")
	}

	return exists, nil
}

// Exists checks if the UserAccessToken row exists.
func (o *UserAccessToken) Exists(exec boil.Executor) (bool, error) {
	return UserAccessTokenExists(exec, o.ID)
}
