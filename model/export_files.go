// Code generated by SQLBoiler 4.16.1 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package model

import (
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/sitename/sitename/modules/model_types"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// ExportFile is an object representing the database table.
type ExportFile struct {
	ID          string                 `boil:"id" json:"id" toml:"id" yaml:"id"`
	UserID      model_types.NullString `boil:"user_id" json:"user_id,omitempty" toml:"user_id" yaml:"user_id,omitempty"`
	ContentFile model_types.NullString `boil:"content_file" json:"content_file,omitempty" toml:"content_file" yaml:"content_file,omitempty"`
	CreatedAt   int64                  `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt   int64                  `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`

	R *exportFileR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L exportFileL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var ExportFileColumns = struct {
	ID          string
	UserID      string
	ContentFile string
	CreatedAt   string
	UpdatedAt   string
}{
	ID:          "id",
	UserID:      "user_id",
	ContentFile: "content_file",
	CreatedAt:   "created_at",
	UpdatedAt:   "updated_at",
}

var ExportFileTableColumns = struct {
	ID          string
	UserID      string
	ContentFile string
	CreatedAt   string
	UpdatedAt   string
}{
	ID:          "export_files.id",
	UserID:      "export_files.user_id",
	ContentFile: "export_files.content_file",
	CreatedAt:   "export_files.created_at",
	UpdatedAt:   "export_files.updated_at",
}

// Generated where

var ExportFileWhere = struct {
	ID          whereHelperstring
	UserID      whereHelpermodel_types_NullString
	ContentFile whereHelpermodel_types_NullString
	CreatedAt   whereHelperint64
	UpdatedAt   whereHelperint64
}{
	ID:          whereHelperstring{field: "\"export_files\".\"id\""},
	UserID:      whereHelpermodel_types_NullString{field: "\"export_files\".\"user_id\""},
	ContentFile: whereHelpermodel_types_NullString{field: "\"export_files\".\"content_file\""},
	CreatedAt:   whereHelperint64{field: "\"export_files\".\"created_at\""},
	UpdatedAt:   whereHelperint64{field: "\"export_files\".\"updated_at\""},
}

// ExportFileRels is where relationship names are stored.
var ExportFileRels = struct {
}{}

// exportFileR is where relationships are stored.
type exportFileR struct {
}

// NewStruct creates a new relationship struct
func (*exportFileR) NewStruct() *exportFileR {
	return &exportFileR{}
}

// exportFileL is where Load methods for each relationship are stored.
type exportFileL struct{}

var (
	exportFileAllColumns            = []string{"id", "user_id", "content_file", "created_at", "updated_at"}
	exportFileColumnsWithoutDefault = []string{"created_at", "updated_at"}
	exportFileColumnsWithDefault    = []string{"id", "user_id", "content_file"}
	exportFilePrimaryKeyColumns     = []string{"id"}
	exportFileGeneratedColumns      = []string{}
)

type (
	// ExportFileSlice is an alias for a slice of pointers to ExportFile.
	// This should almost always be used instead of []ExportFile.
	ExportFileSlice []*ExportFile

	exportFileQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	exportFileType                 = reflect.TypeOf(&ExportFile{})
	exportFileMapping              = queries.MakeStructMapping(exportFileType)
	exportFilePrimaryKeyMapping, _ = queries.BindMapping(exportFileType, exportFileMapping, exportFilePrimaryKeyColumns)
	exportFileInsertCacheMut       sync.RWMutex
	exportFileInsertCache          = make(map[string]insertCache)
	exportFileUpdateCacheMut       sync.RWMutex
	exportFileUpdateCache          = make(map[string]updateCache)
	exportFileUpsertCacheMut       sync.RWMutex
	exportFileUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// One returns a single exportFile record from the query.
func (q exportFileQuery) One(exec boil.Executor) (*ExportFile, error) {
	o := &ExportFile{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(nil, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "model: failed to execute a one query for export_files")
	}

	return o, nil
}

// All returns all ExportFile records from the query.
func (q exportFileQuery) All(exec boil.Executor) (ExportFileSlice, error) {
	var o []*ExportFile

	err := q.Bind(nil, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "model: failed to assign all query results to ExportFile slice")
	}

	return o, nil
}

// Count returns the count of all ExportFile records in the query.
func (q exportFileQuery) Count(exec boil.Executor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to count export_files rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q exportFileQuery) Exists(exec boil.Executor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "model: failed to check if export_files exists")
	}

	return count > 0, nil
}

// ExportFiles retrieves all the records using an executor.
func ExportFiles(mods ...qm.QueryMod) exportFileQuery {
	mods = append(mods, qm.From("\"export_files\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"export_files\".*"})
	}

	return exportFileQuery{q}
}

// FindExportFile retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindExportFile(exec boil.Executor, iD string, selectCols ...string) (*ExportFile, error) {
	exportFileObj := &ExportFile{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"export_files\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(nil, exec, exportFileObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "model: unable to select from export_files")
	}

	return exportFileObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *ExportFile) Insert(exec boil.Executor, columns boil.Columns) error {
	if o == nil {
		return errors.New("model: no export_files provided for insertion")
	}

	var err error

	nzDefaults := queries.NonZeroDefaultSet(exportFileColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	exportFileInsertCacheMut.RLock()
	cache, cached := exportFileInsertCache[key]
	exportFileInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			exportFileAllColumns,
			exportFileColumnsWithDefault,
			exportFileColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(exportFileType, exportFileMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(exportFileType, exportFileMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"export_files\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"export_files\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "model: unable to insert into export_files")
	}

	if !cached {
		exportFileInsertCacheMut.Lock()
		exportFileInsertCache[key] = cache
		exportFileInsertCacheMut.Unlock()
	}

	return nil
}

// Update uses an executor to update the ExportFile.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *ExportFile) Update(exec boil.Executor, columns boil.Columns) (int64, error) {
	var err error
	key := makeCacheKey(columns, nil)
	exportFileUpdateCacheMut.RLock()
	cache, cached := exportFileUpdateCache[key]
	exportFileUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			exportFileAllColumns,
			exportFilePrimaryKeyColumns,
		)
		if len(wl) == 0 {
			return 0, errors.New("model: unable to update export_files, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"export_files\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, exportFilePrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(exportFileType, exportFileMapping, append(wl, exportFilePrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	var result sql.Result
	result, err = exec.Exec(cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to update export_files row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by update for export_files")
	}

	if !cached {
		exportFileUpdateCacheMut.Lock()
		exportFileUpdateCache[key] = cache
		exportFileUpdateCacheMut.Unlock()
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values.
func (q exportFileQuery) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to update all for export_files")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to retrieve rows affected for export_files")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o ExportFileSlice) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("model: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), exportFilePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"export_files\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, exportFilePrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to update all in exportFile slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to retrieve rows affected all in update all exportFile")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *ExportFile) Upsert(exec boil.Executor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("model: no export_files provided for upsert")
	}

	nzDefaults := queries.NonZeroDefaultSet(exportFileColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	exportFileUpsertCacheMut.RLock()
	cache, cached := exportFileUpsertCache[key]
	exportFileUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			exportFileAllColumns,
			exportFileColumnsWithDefault,
			exportFileColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			exportFileAllColumns,
			exportFilePrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("model: unable to upsert export_files, could not build update column list")
		}

		ret := strmangle.SetComplement(exportFileAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(exportFilePrimaryKeyColumns) == 0 {
				return errors.New("model: unable to upsert export_files, could not build conflict column list")
			}

			conflict = make([]string, len(exportFilePrimaryKeyColumns))
			copy(conflict, exportFilePrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"export_files\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(exportFileType, exportFileMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(exportFileType, exportFileMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "model: unable to upsert export_files")
	}

	if !cached {
		exportFileUpsertCacheMut.Lock()
		exportFileUpsertCache[key] = cache
		exportFileUpsertCacheMut.Unlock()
	}

	return nil
}

// Delete deletes a single ExportFile record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *ExportFile) Delete(exec boil.Executor) (int64, error) {
	if o == nil {
		return 0, errors.New("model: no ExportFile provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), exportFilePrimaryKeyMapping)
	sql := "DELETE FROM \"export_files\" WHERE \"id\"=$1"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to delete from export_files")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by delete for export_files")
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q exportFileQuery) DeleteAll(exec boil.Executor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("model: no exportFileQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to delete all from export_files")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by deleteall for export_files")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o ExportFileSlice) DeleteAll(exec boil.Executor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), exportFilePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"export_files\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, exportFilePrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to delete all from exportFile slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by deleteall for export_files")
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *ExportFile) Reload(exec boil.Executor) error {
	ret, err := FindExportFile(exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *ExportFileSlice) ReloadAll(exec boil.Executor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := ExportFileSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), exportFilePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"export_files\".* FROM \"export_files\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, exportFilePrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(nil, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "model: unable to reload all in ExportFileSlice")
	}

	*o = slice

	return nil
}

// ExportFileExists checks if the ExportFile row exists.
func ExportFileExists(exec boil.Executor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"export_files\" where \"id\"=$1 limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, iD)
	}
	row := exec.QueryRow(sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "model: unable to check if export_files exists")
	}

	return exists, nil
}

// Exists checks if the ExportFile row exists.
func (o *ExportFile) Exists(exec boil.Executor) (bool, error) {
	return ExportFileExists(exec, o.ID)
}
