// Code generated by SQLBoiler 4.16.2 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package model

import (
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// ClusterDiscovery is an object representing the database table.
type ClusterDiscovery struct {
	ID          string `boil:"id" json:"id" toml:"id" yaml:"id"`
	Type        string `boil:"type" json:"type" toml:"type" yaml:"type"`
	ClusterName string `boil:"cluster_name" json:"cluster_name" toml:"cluster_name" yaml:"cluster_name"`
	HostName    string `boil:"host_name" json:"host_name" toml:"host_name" yaml:"host_name"`
	GossipPort  int    `boil:"gossip_port" json:"gossip_port" toml:"gossip_port" yaml:"gossip_port"`
	Port        int    `boil:"port" json:"port" toml:"port" yaml:"port"`
	CreatedAt   int64  `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	LastPingAt  int64  `boil:"last_ping_at" json:"last_ping_at" toml:"last_ping_at" yaml:"last_ping_at"`

	R *clusterDiscoveryR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L clusterDiscoveryL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var ClusterDiscoveryColumns = struct {
	ID          string
	Type        string
	ClusterName string
	HostName    string
	GossipPort  string
	Port        string
	CreatedAt   string
	LastPingAt  string
}{
	ID:          "id",
	Type:        "type",
	ClusterName: "cluster_name",
	HostName:    "host_name",
	GossipPort:  "gossip_port",
	Port:        "port",
	CreatedAt:   "created_at",
	LastPingAt:  "last_ping_at",
}

var ClusterDiscoveryTableColumns = struct {
	ID          string
	Type        string
	ClusterName string
	HostName    string
	GossipPort  string
	Port        string
	CreatedAt   string
	LastPingAt  string
}{
	ID:          "cluster_discoveries.id",
	Type:        "cluster_discoveries.type",
	ClusterName: "cluster_discoveries.cluster_name",
	HostName:    "cluster_discoveries.host_name",
	GossipPort:  "cluster_discoveries.gossip_port",
	Port:        "cluster_discoveries.port",
	CreatedAt:   "cluster_discoveries.created_at",
	LastPingAt:  "cluster_discoveries.last_ping_at",
}

// Generated where

var ClusterDiscoveryWhere = struct {
	ID          whereHelperstring
	Type        whereHelperstring
	ClusterName whereHelperstring
	HostName    whereHelperstring
	GossipPort  whereHelperint
	Port        whereHelperint
	CreatedAt   whereHelperint64
	LastPingAt  whereHelperint64
}{
	ID:          whereHelperstring{field: "\"cluster_discoveries\".\"id\""},
	Type:        whereHelperstring{field: "\"cluster_discoveries\".\"type\""},
	ClusterName: whereHelperstring{field: "\"cluster_discoveries\".\"cluster_name\""},
	HostName:    whereHelperstring{field: "\"cluster_discoveries\".\"host_name\""},
	GossipPort:  whereHelperint{field: "\"cluster_discoveries\".\"gossip_port\""},
	Port:        whereHelperint{field: "\"cluster_discoveries\".\"port\""},
	CreatedAt:   whereHelperint64{field: "\"cluster_discoveries\".\"created_at\""},
	LastPingAt:  whereHelperint64{field: "\"cluster_discoveries\".\"last_ping_at\""},
}

// ClusterDiscoveryRels is where relationship names are stored.
var ClusterDiscoveryRels = struct {
}{}

// clusterDiscoveryR is where relationships are stored.
type clusterDiscoveryR struct {
}

// NewStruct creates a new relationship struct
func (*clusterDiscoveryR) NewStruct() *clusterDiscoveryR {
	return &clusterDiscoveryR{}
}

// clusterDiscoveryL is where Load methods for each relationship are stored.
type clusterDiscoveryL struct{}

var (
	clusterDiscoveryAllColumns            = []string{"id", "type", "cluster_name", "host_name", "gossip_port", "port", "created_at", "last_ping_at"}
	clusterDiscoveryColumnsWithoutDefault = []string{"id", "type", "cluster_name", "host_name", "gossip_port", "port", "created_at", "last_ping_at"}
	clusterDiscoveryColumnsWithDefault    = []string{}
	clusterDiscoveryPrimaryKeyColumns     = []string{"id"}
	clusterDiscoveryGeneratedColumns      = []string{}
)

type (
	// ClusterDiscoverySlice is an alias for a slice of pointers to ClusterDiscovery.
	// This should almost always be used instead of []ClusterDiscovery.
	ClusterDiscoverySlice []*ClusterDiscovery

	clusterDiscoveryQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	clusterDiscoveryType                 = reflect.TypeOf(&ClusterDiscovery{})
	clusterDiscoveryMapping              = queries.MakeStructMapping(clusterDiscoveryType)
	clusterDiscoveryPrimaryKeyMapping, _ = queries.BindMapping(clusterDiscoveryType, clusterDiscoveryMapping, clusterDiscoveryPrimaryKeyColumns)
	clusterDiscoveryInsertCacheMut       sync.RWMutex
	clusterDiscoveryInsertCache          = make(map[string]insertCache)
	clusterDiscoveryUpdateCacheMut       sync.RWMutex
	clusterDiscoveryUpdateCache          = make(map[string]updateCache)
	clusterDiscoveryUpsertCacheMut       sync.RWMutex
	clusterDiscoveryUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// One returns a single clusterDiscovery record from the query.
func (q clusterDiscoveryQuery) One(exec boil.Executor) (*ClusterDiscovery, error) {
	o := &ClusterDiscovery{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(nil, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "model: failed to execute a one query for cluster_discoveries")
	}

	return o, nil
}

// All returns all ClusterDiscovery records from the query.
func (q clusterDiscoveryQuery) All(exec boil.Executor) (ClusterDiscoverySlice, error) {
	var o []*ClusterDiscovery

	err := q.Bind(nil, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "model: failed to assign all query results to ClusterDiscovery slice")
	}

	return o, nil
}

// Count returns the count of all ClusterDiscovery records in the query.
func (q clusterDiscoveryQuery) Count(exec boil.Executor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to count cluster_discoveries rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q clusterDiscoveryQuery) Exists(exec boil.Executor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "model: failed to check if cluster_discoveries exists")
	}

	return count > 0, nil
}

// ClusterDiscoveries retrieves all the records using an executor.
func ClusterDiscoveries(mods ...qm.QueryMod) clusterDiscoveryQuery {
	mods = append(mods, qm.From("\"cluster_discoveries\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"cluster_discoveries\".*"})
	}

	return clusterDiscoveryQuery{q}
}

// FindClusterDiscovery retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindClusterDiscovery(exec boil.Executor, iD string, selectCols ...string) (*ClusterDiscovery, error) {
	clusterDiscoveryObj := &ClusterDiscovery{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"cluster_discoveries\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(nil, exec, clusterDiscoveryObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "model: unable to select from cluster_discoveries")
	}

	return clusterDiscoveryObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *ClusterDiscovery) Insert(exec boil.Executor, columns boil.Columns) error {
	if o == nil {
		return errors.New("model: no cluster_discoveries provided for insertion")
	}

	var err error

	nzDefaults := queries.NonZeroDefaultSet(clusterDiscoveryColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	clusterDiscoveryInsertCacheMut.RLock()
	cache, cached := clusterDiscoveryInsertCache[key]
	clusterDiscoveryInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			clusterDiscoveryAllColumns,
			clusterDiscoveryColumnsWithDefault,
			clusterDiscoveryColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(clusterDiscoveryType, clusterDiscoveryMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(clusterDiscoveryType, clusterDiscoveryMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"cluster_discoveries\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"cluster_discoveries\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "model: unable to insert into cluster_discoveries")
	}

	if !cached {
		clusterDiscoveryInsertCacheMut.Lock()
		clusterDiscoveryInsertCache[key] = cache
		clusterDiscoveryInsertCacheMut.Unlock()
	}

	return nil
}

// Update uses an executor to update the ClusterDiscovery.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *ClusterDiscovery) Update(exec boil.Executor, columns boil.Columns) (int64, error) {
	var err error
	key := makeCacheKey(columns, nil)
	clusterDiscoveryUpdateCacheMut.RLock()
	cache, cached := clusterDiscoveryUpdateCache[key]
	clusterDiscoveryUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			clusterDiscoveryAllColumns,
			clusterDiscoveryPrimaryKeyColumns,
		)
		if len(wl) == 0 {
			return 0, errors.New("model: unable to update cluster_discoveries, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"cluster_discoveries\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, clusterDiscoveryPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(clusterDiscoveryType, clusterDiscoveryMapping, append(wl, clusterDiscoveryPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	var result sql.Result
	result, err = exec.Exec(cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to update cluster_discoveries row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by update for cluster_discoveries")
	}

	if !cached {
		clusterDiscoveryUpdateCacheMut.Lock()
		clusterDiscoveryUpdateCache[key] = cache
		clusterDiscoveryUpdateCacheMut.Unlock()
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values.
func (q clusterDiscoveryQuery) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to update all for cluster_discoveries")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to retrieve rows affected for cluster_discoveries")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o ClusterDiscoverySlice) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("model: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), clusterDiscoveryPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"cluster_discoveries\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, clusterDiscoveryPrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to update all in clusterDiscovery slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to retrieve rows affected all in update all clusterDiscovery")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *ClusterDiscovery) Upsert(exec boil.Executor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("model: no cluster_discoveries provided for upsert")
	}

	nzDefaults := queries.NonZeroDefaultSet(clusterDiscoveryColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	clusterDiscoveryUpsertCacheMut.RLock()
	cache, cached := clusterDiscoveryUpsertCache[key]
	clusterDiscoveryUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			clusterDiscoveryAllColumns,
			clusterDiscoveryColumnsWithDefault,
			clusterDiscoveryColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			clusterDiscoveryAllColumns,
			clusterDiscoveryPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("model: unable to upsert cluster_discoveries, could not build update column list")
		}

		ret := strmangle.SetComplement(clusterDiscoveryAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(clusterDiscoveryPrimaryKeyColumns) == 0 {
				return errors.New("model: unable to upsert cluster_discoveries, could not build conflict column list")
			}

			conflict = make([]string, len(clusterDiscoveryPrimaryKeyColumns))
			copy(conflict, clusterDiscoveryPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"cluster_discoveries\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(clusterDiscoveryType, clusterDiscoveryMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(clusterDiscoveryType, clusterDiscoveryMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "model: unable to upsert cluster_discoveries")
	}

	if !cached {
		clusterDiscoveryUpsertCacheMut.Lock()
		clusterDiscoveryUpsertCache[key] = cache
		clusterDiscoveryUpsertCacheMut.Unlock()
	}

	return nil
}

// Delete deletes a single ClusterDiscovery record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *ClusterDiscovery) Delete(exec boil.Executor) (int64, error) {
	if o == nil {
		return 0, errors.New("model: no ClusterDiscovery provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), clusterDiscoveryPrimaryKeyMapping)
	sql := "DELETE FROM \"cluster_discoveries\" WHERE \"id\"=$1"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to delete from cluster_discoveries")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by delete for cluster_discoveries")
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q clusterDiscoveryQuery) DeleteAll(exec boil.Executor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("model: no clusterDiscoveryQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to delete all from cluster_discoveries")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by deleteall for cluster_discoveries")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o ClusterDiscoverySlice) DeleteAll(exec boil.Executor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), clusterDiscoveryPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"cluster_discoveries\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, clusterDiscoveryPrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to delete all from clusterDiscovery slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by deleteall for cluster_discoveries")
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *ClusterDiscovery) Reload(exec boil.Executor) error {
	ret, err := FindClusterDiscovery(exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *ClusterDiscoverySlice) ReloadAll(exec boil.Executor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := ClusterDiscoverySlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), clusterDiscoveryPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"cluster_discoveries\".* FROM \"cluster_discoveries\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, clusterDiscoveryPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(nil, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "model: unable to reload all in ClusterDiscoverySlice")
	}

	*o = slice

	return nil
}

// ClusterDiscoveryExists checks if the ClusterDiscovery row exists.
func ClusterDiscoveryExists(exec boil.Executor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"cluster_discoveries\" where \"id\"=$1 limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, iD)
	}
	row := exec.QueryRow(sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "model: unable to check if cluster_discoveries exists")
	}

	return exists, nil
}

// Exists checks if the ClusterDiscovery row exists.
func (o *ClusterDiscovery) Exists(exec boil.Executor) (bool, error) {
	return ClusterDiscoveryExists(exec, o.ID)
}
