// Code generated by SQLBoiler 4.17.1 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package model

import (
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/site-name/decimal"
	"github.com/sitename/sitename/modules/model_types"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// CheckoutDiscount is an object representing the database table.
type CheckoutDiscount struct {
	ID              string                 `boil:"id" json:"id" toml:"id" yaml:"id"`
	CheckoutID      model_types.NullString `boil:"checkout_id" json:"checkout_id,omitempty" toml:"checkout_id" yaml:"checkout_id,omitempty"`
	Type            DiscountType           `boil:"type" json:"type" toml:"type" yaml:"type"`
	ValueType       DiscountValueType      `boil:"value_type" json:"value_type" toml:"value_type" yaml:"value_type"`
	Value           decimal.Decimal        `boil:"value" json:"value" toml:"value" yaml:"value"`
	AmountValue     decimal.Decimal        `boil:"amount_value" json:"amount_value" toml:"amount_value" yaml:"amount_value"`
	Currency        Currency               `boil:"currency" json:"currency" toml:"currency" yaml:"currency"`
	Name            model_types.NullString `boil:"name" json:"name,omitempty" toml:"name" yaml:"name,omitempty"`
	TranslatedName  model_types.NullString `boil:"translated_name" json:"translated_name,omitempty" toml:"translated_name" yaml:"translated_name,omitempty"`
	Reason          model_types.NullString `boil:"reason" json:"reason,omitempty" toml:"reason" yaml:"reason,omitempty"`
	PromotionRuleID model_types.NullString `boil:"promotion_rule_id" json:"promotion_rule_id,omitempty" toml:"promotion_rule_id" yaml:"promotion_rule_id,omitempty"`
	VoucherID       model_types.NullString `boil:"voucher_id" json:"voucher_id,omitempty" toml:"voucher_id" yaml:"voucher_id,omitempty"`
	VoucherCode     model_types.NullString `boil:"voucher_code" json:"voucher_code,omitempty" toml:"voucher_code" yaml:"voucher_code,omitempty"`
	CreatedAt       int64                  `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`

	R *checkoutDiscountR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L checkoutDiscountL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var CheckoutDiscountColumns = struct {
	ID              string
	CheckoutID      string
	Type            string
	ValueType       string
	Value           string
	AmountValue     string
	Currency        string
	Name            string
	TranslatedName  string
	Reason          string
	PromotionRuleID string
	VoucherID       string
	VoucherCode     string
	CreatedAt       string
}{
	ID:              "id",
	CheckoutID:      "checkout_id",
	Type:            "type",
	ValueType:       "value_type",
	Value:           "value",
	AmountValue:     "amount_value",
	Currency:        "currency",
	Name:            "name",
	TranslatedName:  "translated_name",
	Reason:          "reason",
	PromotionRuleID: "promotion_rule_id",
	VoucherID:       "voucher_id",
	VoucherCode:     "voucher_code",
	CreatedAt:       "created_at",
}

var CheckoutDiscountTableColumns = struct {
	ID              string
	CheckoutID      string
	Type            string
	ValueType       string
	Value           string
	AmountValue     string
	Currency        string
	Name            string
	TranslatedName  string
	Reason          string
	PromotionRuleID string
	VoucherID       string
	VoucherCode     string
	CreatedAt       string
}{
	ID:              "checkout_discounts.id",
	CheckoutID:      "checkout_discounts.checkout_id",
	Type:            "checkout_discounts.type",
	ValueType:       "checkout_discounts.value_type",
	Value:           "checkout_discounts.value",
	AmountValue:     "checkout_discounts.amount_value",
	Currency:        "checkout_discounts.currency",
	Name:            "checkout_discounts.name",
	TranslatedName:  "checkout_discounts.translated_name",
	Reason:          "checkout_discounts.reason",
	PromotionRuleID: "checkout_discounts.promotion_rule_id",
	VoucherID:       "checkout_discounts.voucher_id",
	VoucherCode:     "checkout_discounts.voucher_code",
	CreatedAt:       "checkout_discounts.created_at",
}

// Generated where

type whereHelperDiscountType struct{ field string }

func (w whereHelperDiscountType) EQ(x DiscountType) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.EQ, x)
}
func (w whereHelperDiscountType) NEQ(x DiscountType) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.NEQ, x)
}
func (w whereHelperDiscountType) LT(x DiscountType) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LT, x)
}
func (w whereHelperDiscountType) LTE(x DiscountType) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LTE, x)
}
func (w whereHelperDiscountType) GT(x DiscountType) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GT, x)
}
func (w whereHelperDiscountType) GTE(x DiscountType) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GTE, x)
}
func (w whereHelperDiscountType) IN(slice []DiscountType) qm.QueryMod {
	values := make([]interface{}, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereIn(fmt.Sprintf("%s IN ?", w.field), values...)
}
func (w whereHelperDiscountType) NIN(slice []DiscountType) qm.QueryMod {
	values := make([]interface{}, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereNotIn(fmt.Sprintf("%s NOT IN ?", w.field), values...)
}

type whereHelperDiscountValueType struct{ field string }

func (w whereHelperDiscountValueType) EQ(x DiscountValueType) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.EQ, x)
}
func (w whereHelperDiscountValueType) NEQ(x DiscountValueType) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.NEQ, x)
}
func (w whereHelperDiscountValueType) LT(x DiscountValueType) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LT, x)
}
func (w whereHelperDiscountValueType) LTE(x DiscountValueType) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LTE, x)
}
func (w whereHelperDiscountValueType) GT(x DiscountValueType) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GT, x)
}
func (w whereHelperDiscountValueType) GTE(x DiscountValueType) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GTE, x)
}
func (w whereHelperDiscountValueType) IN(slice []DiscountValueType) qm.QueryMod {
	values := make([]interface{}, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereIn(fmt.Sprintf("%s IN ?", w.field), values...)
}
func (w whereHelperDiscountValueType) NIN(slice []DiscountValueType) qm.QueryMod {
	values := make([]interface{}, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereNotIn(fmt.Sprintf("%s NOT IN ?", w.field), values...)
}

type whereHelperdecimal_Decimal struct{ field string }

func (w whereHelperdecimal_Decimal) EQ(x decimal.Decimal) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.EQ, x)
}
func (w whereHelperdecimal_Decimal) NEQ(x decimal.Decimal) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.NEQ, x)
}
func (w whereHelperdecimal_Decimal) LT(x decimal.Decimal) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LT, x)
}
func (w whereHelperdecimal_Decimal) LTE(x decimal.Decimal) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LTE, x)
}
func (w whereHelperdecimal_Decimal) GT(x decimal.Decimal) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GT, x)
}
func (w whereHelperdecimal_Decimal) GTE(x decimal.Decimal) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GTE, x)
}

var CheckoutDiscountWhere = struct {
	ID              whereHelperstring
	CheckoutID      whereHelpermodel_types_NullString
	Type            whereHelperDiscountType
	ValueType       whereHelperDiscountValueType
	Value           whereHelperdecimal_Decimal
	AmountValue     whereHelperdecimal_Decimal
	Currency        whereHelperCurrency
	Name            whereHelpermodel_types_NullString
	TranslatedName  whereHelpermodel_types_NullString
	Reason          whereHelpermodel_types_NullString
	PromotionRuleID whereHelpermodel_types_NullString
	VoucherID       whereHelpermodel_types_NullString
	VoucherCode     whereHelpermodel_types_NullString
	CreatedAt       whereHelperint64
}{
	ID:              whereHelperstring{field: "\"checkout_discounts\".\"id\""},
	CheckoutID:      whereHelpermodel_types_NullString{field: "\"checkout_discounts\".\"checkout_id\""},
	Type:            whereHelperDiscountType{field: "\"checkout_discounts\".\"type\""},
	ValueType:       whereHelperDiscountValueType{field: "\"checkout_discounts\".\"value_type\""},
	Value:           whereHelperdecimal_Decimal{field: "\"checkout_discounts\".\"value\""},
	AmountValue:     whereHelperdecimal_Decimal{field: "\"checkout_discounts\".\"amount_value\""},
	Currency:        whereHelperCurrency{field: "\"checkout_discounts\".\"currency\""},
	Name:            whereHelpermodel_types_NullString{field: "\"checkout_discounts\".\"name\""},
	TranslatedName:  whereHelpermodel_types_NullString{field: "\"checkout_discounts\".\"translated_name\""},
	Reason:          whereHelpermodel_types_NullString{field: "\"checkout_discounts\".\"reason\""},
	PromotionRuleID: whereHelpermodel_types_NullString{field: "\"checkout_discounts\".\"promotion_rule_id\""},
	VoucherID:       whereHelpermodel_types_NullString{field: "\"checkout_discounts\".\"voucher_id\""},
	VoucherCode:     whereHelpermodel_types_NullString{field: "\"checkout_discounts\".\"voucher_code\""},
	CreatedAt:       whereHelperint64{field: "\"checkout_discounts\".\"created_at\""},
}

// CheckoutDiscountRels is where relationship names are stored.
var CheckoutDiscountRels = struct {
	Checkout      string
	PromotionRule string
	Voucher       string
}{
	Checkout:      "Checkout",
	PromotionRule: "PromotionRule",
	Voucher:       "Voucher",
}

// checkoutDiscountR is where relationships are stored.
type checkoutDiscountR struct {
	Checkout      *Checkout      `boil:"Checkout" json:"Checkout" toml:"Checkout" yaml:"Checkout"`
	PromotionRule *PromotionRule `boil:"PromotionRule" json:"PromotionRule" toml:"PromotionRule" yaml:"PromotionRule"`
	Voucher       *Voucher       `boil:"Voucher" json:"Voucher" toml:"Voucher" yaml:"Voucher"`
}

// NewStruct creates a new relationship struct
func (*checkoutDiscountR) NewStruct() *checkoutDiscountR {
	return &checkoutDiscountR{}
}

func (r *checkoutDiscountR) GetCheckout() *Checkout {
	if r == nil {
		return nil
	}
	return r.Checkout
}

func (r *checkoutDiscountR) GetPromotionRule() *PromotionRule {
	if r == nil {
		return nil
	}
	return r.PromotionRule
}

func (r *checkoutDiscountR) GetVoucher() *Voucher {
	if r == nil {
		return nil
	}
	return r.Voucher
}

// checkoutDiscountL is where Load methods for each relationship are stored.
type checkoutDiscountL struct{}

var (
	checkoutDiscountAllColumns            = []string{"id", "checkout_id", "type", "value_type", "value", "amount_value", "currency", "name", "translated_name", "reason", "promotion_rule_id", "voucher_id", "voucher_code", "created_at"}
	checkoutDiscountColumnsWithoutDefault = []string{"id", "type", "value_type", "currency", "created_at"}
	checkoutDiscountColumnsWithDefault    = []string{"checkout_id", "value", "amount_value", "name", "translated_name", "reason", "promotion_rule_id", "voucher_id", "voucher_code"}
	checkoutDiscountPrimaryKeyColumns     = []string{"id"}
	checkoutDiscountGeneratedColumns      = []string{}
)

type (
	// CheckoutDiscountSlice is an alias for a slice of pointers to CheckoutDiscount.
	// This should almost always be used instead of []CheckoutDiscount.
	CheckoutDiscountSlice []*CheckoutDiscount

	checkoutDiscountQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	checkoutDiscountType                 = reflect.TypeOf(&CheckoutDiscount{})
	checkoutDiscountMapping              = queries.MakeStructMapping(checkoutDiscountType)
	checkoutDiscountPrimaryKeyMapping, _ = queries.BindMapping(checkoutDiscountType, checkoutDiscountMapping, checkoutDiscountPrimaryKeyColumns)
	checkoutDiscountInsertCacheMut       sync.RWMutex
	checkoutDiscountInsertCache          = make(map[string]insertCache)
	checkoutDiscountUpdateCacheMut       sync.RWMutex
	checkoutDiscountUpdateCache          = make(map[string]updateCache)
	checkoutDiscountUpsertCacheMut       sync.RWMutex
	checkoutDiscountUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// One returns a single checkoutDiscount record from the query.
func (q checkoutDiscountQuery) One(exec boil.Executor) (*CheckoutDiscount, error) {
	o := &CheckoutDiscount{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(nil, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "model: failed to execute a one query for checkout_discounts")
	}

	return o, nil
}

// All returns all CheckoutDiscount records from the query.
func (q checkoutDiscountQuery) All(exec boil.Executor) (CheckoutDiscountSlice, error) {
	var o []*CheckoutDiscount

	err := q.Bind(nil, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "model: failed to assign all query results to CheckoutDiscount slice")
	}

	return o, nil
}

// Count returns the count of all CheckoutDiscount records in the query.
func (q checkoutDiscountQuery) Count(exec boil.Executor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to count checkout_discounts rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q checkoutDiscountQuery) Exists(exec boil.Executor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "model: failed to check if checkout_discounts exists")
	}

	return count > 0, nil
}

// Checkout pointed to by the foreign key.
func (o *CheckoutDiscount) Checkout(mods ...qm.QueryMod) checkoutQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"token\" = ?", o.CheckoutID),
	}

	queryMods = append(queryMods, mods...)

	return Checkouts(queryMods...)
}

// PromotionRule pointed to by the foreign key.
func (o *CheckoutDiscount) PromotionRule(mods ...qm.QueryMod) promotionRuleQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.PromotionRuleID),
	}

	queryMods = append(queryMods, mods...)

	return PromotionRules(queryMods...)
}

// Voucher pointed to by the foreign key.
func (o *CheckoutDiscount) Voucher(mods ...qm.QueryMod) voucherQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.VoucherID),
	}

	queryMods = append(queryMods, mods...)

	return Vouchers(queryMods...)
}

// LoadCheckout allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (checkoutDiscountL) LoadCheckout(e boil.Executor, singular bool, maybeCheckoutDiscount interface{}, mods queries.Applicator) error {
	var slice []*CheckoutDiscount
	var object *CheckoutDiscount

	if singular {
		var ok bool
		object, ok = maybeCheckoutDiscount.(*CheckoutDiscount)
		if !ok {
			object = new(CheckoutDiscount)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeCheckoutDiscount)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeCheckoutDiscount))
			}
		}
	} else {
		s, ok := maybeCheckoutDiscount.(*[]*CheckoutDiscount)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeCheckoutDiscount)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeCheckoutDiscount))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &checkoutDiscountR{}
		}
		if !queries.IsNil(object.CheckoutID) {
			args[object.CheckoutID] = struct{}{}
		}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &checkoutDiscountR{}
			}

			if !queries.IsNil(obj.CheckoutID) {
				args[obj.CheckoutID] = struct{}{}
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`checkouts`),
		qm.WhereIn(`checkouts.token in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.Query(e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Checkout")
	}

	var resultSlice []*Checkout
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Checkout")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for checkouts")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for checkouts")
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Checkout = foreign
		if foreign.R == nil {
			foreign.R = &checkoutR{}
		}
		foreign.R.CheckoutDiscounts = append(foreign.R.CheckoutDiscounts, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.CheckoutID, foreign.Token) {
				local.R.Checkout = foreign
				if foreign.R == nil {
					foreign.R = &checkoutR{}
				}
				foreign.R.CheckoutDiscounts = append(foreign.R.CheckoutDiscounts, local)
				break
			}
		}
	}

	return nil
}

// LoadPromotionRule allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (checkoutDiscountL) LoadPromotionRule(e boil.Executor, singular bool, maybeCheckoutDiscount interface{}, mods queries.Applicator) error {
	var slice []*CheckoutDiscount
	var object *CheckoutDiscount

	if singular {
		var ok bool
		object, ok = maybeCheckoutDiscount.(*CheckoutDiscount)
		if !ok {
			object = new(CheckoutDiscount)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeCheckoutDiscount)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeCheckoutDiscount))
			}
		}
	} else {
		s, ok := maybeCheckoutDiscount.(*[]*CheckoutDiscount)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeCheckoutDiscount)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeCheckoutDiscount))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &checkoutDiscountR{}
		}
		if !queries.IsNil(object.PromotionRuleID) {
			args[object.PromotionRuleID] = struct{}{}
		}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &checkoutDiscountR{}
			}

			if !queries.IsNil(obj.PromotionRuleID) {
				args[obj.PromotionRuleID] = struct{}{}
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`promotion_rules`),
		qm.WhereIn(`promotion_rules.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.Query(e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load PromotionRule")
	}

	var resultSlice []*PromotionRule
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice PromotionRule")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for promotion_rules")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for promotion_rules")
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.PromotionRule = foreign
		if foreign.R == nil {
			foreign.R = &promotionRuleR{}
		}
		foreign.R.CheckoutDiscounts = append(foreign.R.CheckoutDiscounts, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.PromotionRuleID, foreign.ID) {
				local.R.PromotionRule = foreign
				if foreign.R == nil {
					foreign.R = &promotionRuleR{}
				}
				foreign.R.CheckoutDiscounts = append(foreign.R.CheckoutDiscounts, local)
				break
			}
		}
	}

	return nil
}

// LoadVoucher allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (checkoutDiscountL) LoadVoucher(e boil.Executor, singular bool, maybeCheckoutDiscount interface{}, mods queries.Applicator) error {
	var slice []*CheckoutDiscount
	var object *CheckoutDiscount

	if singular {
		var ok bool
		object, ok = maybeCheckoutDiscount.(*CheckoutDiscount)
		if !ok {
			object = new(CheckoutDiscount)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeCheckoutDiscount)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeCheckoutDiscount))
			}
		}
	} else {
		s, ok := maybeCheckoutDiscount.(*[]*CheckoutDiscount)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeCheckoutDiscount)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeCheckoutDiscount))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &checkoutDiscountR{}
		}
		if !queries.IsNil(object.VoucherID) {
			args[object.VoucherID] = struct{}{}
		}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &checkoutDiscountR{}
			}

			if !queries.IsNil(obj.VoucherID) {
				args[obj.VoucherID] = struct{}{}
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`vouchers`),
		qm.WhereIn(`vouchers.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.Query(e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Voucher")
	}

	var resultSlice []*Voucher
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Voucher")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for vouchers")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for vouchers")
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Voucher = foreign
		if foreign.R == nil {
			foreign.R = &voucherR{}
		}
		foreign.R.CheckoutDiscounts = append(foreign.R.CheckoutDiscounts, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.VoucherID, foreign.ID) {
				local.R.Voucher = foreign
				if foreign.R == nil {
					foreign.R = &voucherR{}
				}
				foreign.R.CheckoutDiscounts = append(foreign.R.CheckoutDiscounts, local)
				break
			}
		}
	}

	return nil
}

// SetCheckout of the checkoutDiscount to the related item.
// Sets o.R.Checkout to related.
// Adds o to related.R.CheckoutDiscounts.
func (o *CheckoutDiscount) SetCheckout(exec boil.Executor, insert bool, related *Checkout) error {
	var err error
	if insert {
		if err = related.Insert(exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"checkout_discounts\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"checkout_id"}),
		strmangle.WhereClause("\"", "\"", 2, checkoutDiscountPrimaryKeyColumns),
	)
	values := []interface{}{related.Token, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	if _, err = exec.Exec(updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.CheckoutID, related.Token)
	if o.R == nil {
		o.R = &checkoutDiscountR{
			Checkout: related,
		}
	} else {
		o.R.Checkout = related
	}

	if related.R == nil {
		related.R = &checkoutR{
			CheckoutDiscounts: CheckoutDiscountSlice{o},
		}
	} else {
		related.R.CheckoutDiscounts = append(related.R.CheckoutDiscounts, o)
	}

	return nil
}

// RemoveCheckout relationship.
// Sets o.R.Checkout to nil.
// Removes o from all passed in related items' relationships struct.
func (o *CheckoutDiscount) RemoveCheckout(exec boil.Executor, related *Checkout) error {
	var err error

	queries.SetScanner(&o.CheckoutID, nil)
	if _, err = o.Update(exec, boil.Whitelist("checkout_id")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	if o.R != nil {
		o.R.Checkout = nil
	}
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.CheckoutDiscounts {
		if queries.Equal(o.CheckoutID, ri.CheckoutID) {
			continue
		}

		ln := len(related.R.CheckoutDiscounts)
		if ln > 1 && i < ln-1 {
			related.R.CheckoutDiscounts[i] = related.R.CheckoutDiscounts[ln-1]
		}
		related.R.CheckoutDiscounts = related.R.CheckoutDiscounts[:ln-1]
		break
	}
	return nil
}

// SetPromotionRule of the checkoutDiscount to the related item.
// Sets o.R.PromotionRule to related.
// Adds o to related.R.CheckoutDiscounts.
func (o *CheckoutDiscount) SetPromotionRule(exec boil.Executor, insert bool, related *PromotionRule) error {
	var err error
	if insert {
		if err = related.Insert(exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"checkout_discounts\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"promotion_rule_id"}),
		strmangle.WhereClause("\"", "\"", 2, checkoutDiscountPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	if _, err = exec.Exec(updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.PromotionRuleID, related.ID)
	if o.R == nil {
		o.R = &checkoutDiscountR{
			PromotionRule: related,
		}
	} else {
		o.R.PromotionRule = related
	}

	if related.R == nil {
		related.R = &promotionRuleR{
			CheckoutDiscounts: CheckoutDiscountSlice{o},
		}
	} else {
		related.R.CheckoutDiscounts = append(related.R.CheckoutDiscounts, o)
	}

	return nil
}

// RemovePromotionRule relationship.
// Sets o.R.PromotionRule to nil.
// Removes o from all passed in related items' relationships struct.
func (o *CheckoutDiscount) RemovePromotionRule(exec boil.Executor, related *PromotionRule) error {
	var err error

	queries.SetScanner(&o.PromotionRuleID, nil)
	if _, err = o.Update(exec, boil.Whitelist("promotion_rule_id")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	if o.R != nil {
		o.R.PromotionRule = nil
	}
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.CheckoutDiscounts {
		if queries.Equal(o.PromotionRuleID, ri.PromotionRuleID) {
			continue
		}

		ln := len(related.R.CheckoutDiscounts)
		if ln > 1 && i < ln-1 {
			related.R.CheckoutDiscounts[i] = related.R.CheckoutDiscounts[ln-1]
		}
		related.R.CheckoutDiscounts = related.R.CheckoutDiscounts[:ln-1]
		break
	}
	return nil
}

// SetVoucher of the checkoutDiscount to the related item.
// Sets o.R.Voucher to related.
// Adds o to related.R.CheckoutDiscounts.
func (o *CheckoutDiscount) SetVoucher(exec boil.Executor, insert bool, related *Voucher) error {
	var err error
	if insert {
		if err = related.Insert(exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"checkout_discounts\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"voucher_id"}),
		strmangle.WhereClause("\"", "\"", 2, checkoutDiscountPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	if _, err = exec.Exec(updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.VoucherID, related.ID)
	if o.R == nil {
		o.R = &checkoutDiscountR{
			Voucher: related,
		}
	} else {
		o.R.Voucher = related
	}

	if related.R == nil {
		related.R = &voucherR{
			CheckoutDiscounts: CheckoutDiscountSlice{o},
		}
	} else {
		related.R.CheckoutDiscounts = append(related.R.CheckoutDiscounts, o)
	}

	return nil
}

// RemoveVoucher relationship.
// Sets o.R.Voucher to nil.
// Removes o from all passed in related items' relationships struct.
func (o *CheckoutDiscount) RemoveVoucher(exec boil.Executor, related *Voucher) error {
	var err error

	queries.SetScanner(&o.VoucherID, nil)
	if _, err = o.Update(exec, boil.Whitelist("voucher_id")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	if o.R != nil {
		o.R.Voucher = nil
	}
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.CheckoutDiscounts {
		if queries.Equal(o.VoucherID, ri.VoucherID) {
			continue
		}

		ln := len(related.R.CheckoutDiscounts)
		if ln > 1 && i < ln-1 {
			related.R.CheckoutDiscounts[i] = related.R.CheckoutDiscounts[ln-1]
		}
		related.R.CheckoutDiscounts = related.R.CheckoutDiscounts[:ln-1]
		break
	}
	return nil
}

// CheckoutDiscounts retrieves all the records using an executor.
func CheckoutDiscounts(mods ...qm.QueryMod) checkoutDiscountQuery {
	mods = append(mods, qm.From("\"checkout_discounts\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"checkout_discounts\".*"})
	}

	return checkoutDiscountQuery{q}
}

// FindCheckoutDiscount retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindCheckoutDiscount(exec boil.Executor, iD string, selectCols ...string) (*CheckoutDiscount, error) {
	checkoutDiscountObj := &CheckoutDiscount{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"checkout_discounts\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(nil, exec, checkoutDiscountObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "model: unable to select from checkout_discounts")
	}

	return checkoutDiscountObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *CheckoutDiscount) Insert(exec boil.Executor, columns boil.Columns) error {
	if o == nil {
		return errors.New("model: no checkout_discounts provided for insertion")
	}

	var err error

	nzDefaults := queries.NonZeroDefaultSet(checkoutDiscountColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	checkoutDiscountInsertCacheMut.RLock()
	cache, cached := checkoutDiscountInsertCache[key]
	checkoutDiscountInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			checkoutDiscountAllColumns,
			checkoutDiscountColumnsWithDefault,
			checkoutDiscountColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(checkoutDiscountType, checkoutDiscountMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(checkoutDiscountType, checkoutDiscountMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"checkout_discounts\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"checkout_discounts\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "model: unable to insert into checkout_discounts")
	}

	if !cached {
		checkoutDiscountInsertCacheMut.Lock()
		checkoutDiscountInsertCache[key] = cache
		checkoutDiscountInsertCacheMut.Unlock()
	}

	return nil
}

// Update uses an executor to update the CheckoutDiscount.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *CheckoutDiscount) Update(exec boil.Executor, columns boil.Columns) (int64, error) {
	var err error
	key := makeCacheKey(columns, nil)
	checkoutDiscountUpdateCacheMut.RLock()
	cache, cached := checkoutDiscountUpdateCache[key]
	checkoutDiscountUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			checkoutDiscountAllColumns,
			checkoutDiscountPrimaryKeyColumns,
		)
		if len(wl) == 0 {
			return 0, errors.New("model: unable to update checkout_discounts, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"checkout_discounts\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, checkoutDiscountPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(checkoutDiscountType, checkoutDiscountMapping, append(wl, checkoutDiscountPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	var result sql.Result
	result, err = exec.Exec(cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to update checkout_discounts row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by update for checkout_discounts")
	}

	if !cached {
		checkoutDiscountUpdateCacheMut.Lock()
		checkoutDiscountUpdateCache[key] = cache
		checkoutDiscountUpdateCacheMut.Unlock()
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values.
func (q checkoutDiscountQuery) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to update all for checkout_discounts")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to retrieve rows affected for checkout_discounts")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o CheckoutDiscountSlice) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("model: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), checkoutDiscountPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"checkout_discounts\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, checkoutDiscountPrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to update all in checkoutDiscount slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to retrieve rows affected all in update all checkoutDiscount")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *CheckoutDiscount) Upsert(exec boil.Executor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("model: no checkout_discounts provided for upsert")
	}

	nzDefaults := queries.NonZeroDefaultSet(checkoutDiscountColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	checkoutDiscountUpsertCacheMut.RLock()
	cache, cached := checkoutDiscountUpsertCache[key]
	checkoutDiscountUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			checkoutDiscountAllColumns,
			checkoutDiscountColumnsWithDefault,
			checkoutDiscountColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			checkoutDiscountAllColumns,
			checkoutDiscountPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("model: unable to upsert checkout_discounts, could not build update column list")
		}

		ret := strmangle.SetComplement(checkoutDiscountAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(checkoutDiscountPrimaryKeyColumns) == 0 {
				return errors.New("model: unable to upsert checkout_discounts, could not build conflict column list")
			}

			conflict = make([]string, len(checkoutDiscountPrimaryKeyColumns))
			copy(conflict, checkoutDiscountPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"checkout_discounts\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(checkoutDiscountType, checkoutDiscountMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(checkoutDiscountType, checkoutDiscountMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "model: unable to upsert checkout_discounts")
	}

	if !cached {
		checkoutDiscountUpsertCacheMut.Lock()
		checkoutDiscountUpsertCache[key] = cache
		checkoutDiscountUpsertCacheMut.Unlock()
	}

	return nil
}

// Delete deletes a single CheckoutDiscount record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *CheckoutDiscount) Delete(exec boil.Executor) (int64, error) {
	if o == nil {
		return 0, errors.New("model: no CheckoutDiscount provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), checkoutDiscountPrimaryKeyMapping)
	sql := "DELETE FROM \"checkout_discounts\" WHERE \"id\"=$1"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to delete from checkout_discounts")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by delete for checkout_discounts")
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q checkoutDiscountQuery) DeleteAll(exec boil.Executor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("model: no checkoutDiscountQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to delete all from checkout_discounts")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by deleteall for checkout_discounts")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o CheckoutDiscountSlice) DeleteAll(exec boil.Executor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), checkoutDiscountPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"checkout_discounts\" WHERE " +
		strmangle.WhereInClause(string(dialect.LQ), string(dialect.RQ), 1, checkoutDiscountPrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to delete all from checkoutDiscount slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by deleteall for checkout_discounts")
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *CheckoutDiscount) Reload(exec boil.Executor) error {
	ret, err := FindCheckoutDiscount(exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *CheckoutDiscountSlice) ReloadAll(exec boil.Executor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := CheckoutDiscountSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), checkoutDiscountPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"checkout_discounts\".* FROM \"checkout_discounts\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, checkoutDiscountPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(nil, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "model: unable to reload all in CheckoutDiscountSlice")
	}

	*o = slice

	return nil
}

// CheckoutDiscountExists checks if the CheckoutDiscount row exists.
func CheckoutDiscountExists(exec boil.Executor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"checkout_discounts\" where \"id\"=$1 limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, iD)
	}
	row := exec.QueryRow(sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "model: unable to check if checkout_discounts exists")
	}

	return exists, nil
}

// Exists checks if the CheckoutDiscount row exists.
func (o *CheckoutDiscount) Exists(exec boil.Executor) (bool, error) {
	return CheckoutDiscountExists(exec, o.ID)
}
