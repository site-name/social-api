// Code generated by SQLBoiler 4.16.2 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package model

import (
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/sitename/sitename/modules/model_types"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// CollectionTranslation is an object representing the database table.
type CollectionTranslation struct {
	ID             string                 `boil:"id" json:"id" toml:"id" yaml:"id"`
	LanguageCode   LanguageCode           `boil:"language_code" json:"language_code" toml:"language_code" yaml:"language_code"`
	CollectionID   string                 `boil:"collection_id" json:"collection_id" toml:"collection_id" yaml:"collection_id"`
	Name           string                 `boil:"name" json:"name" toml:"name" yaml:"name"`
	Description    string                 `boil:"description" json:"description" toml:"description" yaml:"description"`
	SeoTitle       model_types.NullString `boil:"seo_title" json:"seo_title,omitempty" toml:"seo_title" yaml:"seo_title,omitempty"`
	SeoDescription model_types.NullString `boil:"seo_description" json:"seo_description,omitempty" toml:"seo_description" yaml:"seo_description,omitempty"`

	R *collectionTranslationR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L collectionTranslationL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var CollectionTranslationColumns = struct {
	ID             string
	LanguageCode   string
	CollectionID   string
	Name           string
	Description    string
	SeoTitle       string
	SeoDescription string
}{
	ID:             "id",
	LanguageCode:   "language_code",
	CollectionID:   "collection_id",
	Name:           "name",
	Description:    "description",
	SeoTitle:       "seo_title",
	SeoDescription: "seo_description",
}

var CollectionTranslationTableColumns = struct {
	ID             string
	LanguageCode   string
	CollectionID   string
	Name           string
	Description    string
	SeoTitle       string
	SeoDescription string
}{
	ID:             "collection_translations.id",
	LanguageCode:   "collection_translations.language_code",
	CollectionID:   "collection_translations.collection_id",
	Name:           "collection_translations.name",
	Description:    "collection_translations.description",
	SeoTitle:       "collection_translations.seo_title",
	SeoDescription: "collection_translations.seo_description",
}

// Generated where

var CollectionTranslationWhere = struct {
	ID             whereHelperstring
	LanguageCode   whereHelperLanguageCode
	CollectionID   whereHelperstring
	Name           whereHelperstring
	Description    whereHelperstring
	SeoTitle       whereHelpermodel_types_NullString
	SeoDescription whereHelpermodel_types_NullString
}{
	ID:             whereHelperstring{field: "\"collection_translations\".\"id\""},
	LanguageCode:   whereHelperLanguageCode{field: "\"collection_translations\".\"language_code\""},
	CollectionID:   whereHelperstring{field: "\"collection_translations\".\"collection_id\""},
	Name:           whereHelperstring{field: "\"collection_translations\".\"name\""},
	Description:    whereHelperstring{field: "\"collection_translations\".\"description\""},
	SeoTitle:       whereHelpermodel_types_NullString{field: "\"collection_translations\".\"seo_title\""},
	SeoDescription: whereHelpermodel_types_NullString{field: "\"collection_translations\".\"seo_description\""},
}

// CollectionTranslationRels is where relationship names are stored.
var CollectionTranslationRels = struct {
	Collection string
}{
	Collection: "Collection",
}

// collectionTranslationR is where relationships are stored.
type collectionTranslationR struct {
	Collection *Collection `boil:"Collection" json:"Collection" toml:"Collection" yaml:"Collection"`
}

// NewStruct creates a new relationship struct
func (*collectionTranslationR) NewStruct() *collectionTranslationR {
	return &collectionTranslationR{}
}

func (r *collectionTranslationR) GetCollection() *Collection {
	if r == nil {
		return nil
	}
	return r.Collection
}

// collectionTranslationL is where Load methods for each relationship are stored.
type collectionTranslationL struct{}

var (
	collectionTranslationAllColumns            = []string{"id", "language_code", "collection_id", "name", "description", "seo_title", "seo_description"}
	collectionTranslationColumnsWithoutDefault = []string{"language_code", "collection_id", "name", "description"}
	collectionTranslationColumnsWithDefault    = []string{"id", "seo_title", "seo_description"}
	collectionTranslationPrimaryKeyColumns     = []string{"id"}
	collectionTranslationGeneratedColumns      = []string{}
)

type (
	// CollectionTranslationSlice is an alias for a slice of pointers to CollectionTranslation.
	// This should almost always be used instead of []CollectionTranslation.
	CollectionTranslationSlice []*CollectionTranslation

	collectionTranslationQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	collectionTranslationType                 = reflect.TypeOf(&CollectionTranslation{})
	collectionTranslationMapping              = queries.MakeStructMapping(collectionTranslationType)
	collectionTranslationPrimaryKeyMapping, _ = queries.BindMapping(collectionTranslationType, collectionTranslationMapping, collectionTranslationPrimaryKeyColumns)
	collectionTranslationInsertCacheMut       sync.RWMutex
	collectionTranslationInsertCache          = make(map[string]insertCache)
	collectionTranslationUpdateCacheMut       sync.RWMutex
	collectionTranslationUpdateCache          = make(map[string]updateCache)
	collectionTranslationUpsertCacheMut       sync.RWMutex
	collectionTranslationUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// One returns a single collectionTranslation record from the query.
func (q collectionTranslationQuery) One(exec boil.Executor) (*CollectionTranslation, error) {
	o := &CollectionTranslation{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(nil, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "model: failed to execute a one query for collection_translations")
	}

	return o, nil
}

// All returns all CollectionTranslation records from the query.
func (q collectionTranslationQuery) All(exec boil.Executor) (CollectionTranslationSlice, error) {
	var o []*CollectionTranslation

	err := q.Bind(nil, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "model: failed to assign all query results to CollectionTranslation slice")
	}

	return o, nil
}

// Count returns the count of all CollectionTranslation records in the query.
func (q collectionTranslationQuery) Count(exec boil.Executor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to count collection_translations rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q collectionTranslationQuery) Exists(exec boil.Executor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "model: failed to check if collection_translations exists")
	}

	return count > 0, nil
}

// Collection pointed to by the foreign key.
func (o *CollectionTranslation) Collection(mods ...qm.QueryMod) collectionQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.CollectionID),
	}

	queryMods = append(queryMods, mods...)

	return Collections(queryMods...)
}

// LoadCollection allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (collectionTranslationL) LoadCollection(e boil.Executor, singular bool, maybeCollectionTranslation interface{}, mods queries.Applicator) error {
	var slice []*CollectionTranslation
	var object *CollectionTranslation

	if singular {
		var ok bool
		object, ok = maybeCollectionTranslation.(*CollectionTranslation)
		if !ok {
			object = new(CollectionTranslation)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeCollectionTranslation)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeCollectionTranslation))
			}
		}
	} else {
		s, ok := maybeCollectionTranslation.(*[]*CollectionTranslation)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeCollectionTranslation)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeCollectionTranslation))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &collectionTranslationR{}
		}
		args[object.CollectionID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &collectionTranslationR{}
			}

			args[obj.CollectionID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`collections`),
		qm.WhereIn(`collections.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.Query(e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Collection")
	}

	var resultSlice []*Collection
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Collection")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for collections")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for collections")
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Collection = foreign
		if foreign.R == nil {
			foreign.R = &collectionR{}
		}
		foreign.R.CollectionTranslations = append(foreign.R.CollectionTranslations, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.CollectionID == foreign.ID {
				local.R.Collection = foreign
				if foreign.R == nil {
					foreign.R = &collectionR{}
				}
				foreign.R.CollectionTranslations = append(foreign.R.CollectionTranslations, local)
				break
			}
		}
	}

	return nil
}

// SetCollection of the collectionTranslation to the related item.
// Sets o.R.Collection to related.
// Adds o to related.R.CollectionTranslations.
func (o *CollectionTranslation) SetCollection(exec boil.Executor, insert bool, related *Collection) error {
	var err error
	if insert {
		if err = related.Insert(exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"collection_translations\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"collection_id"}),
		strmangle.WhereClause("\"", "\"", 2, collectionTranslationPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	if _, err = exec.Exec(updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.CollectionID = related.ID
	if o.R == nil {
		o.R = &collectionTranslationR{
			Collection: related,
		}
	} else {
		o.R.Collection = related
	}

	if related.R == nil {
		related.R = &collectionR{
			CollectionTranslations: CollectionTranslationSlice{o},
		}
	} else {
		related.R.CollectionTranslations = append(related.R.CollectionTranslations, o)
	}

	return nil
}

// CollectionTranslations retrieves all the records using an executor.
func CollectionTranslations(mods ...qm.QueryMod) collectionTranslationQuery {
	mods = append(mods, qm.From("\"collection_translations\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"collection_translations\".*"})
	}

	return collectionTranslationQuery{q}
}

// FindCollectionTranslation retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindCollectionTranslation(exec boil.Executor, iD string, selectCols ...string) (*CollectionTranslation, error) {
	collectionTranslationObj := &CollectionTranslation{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"collection_translations\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(nil, exec, collectionTranslationObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "model: unable to select from collection_translations")
	}

	return collectionTranslationObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *CollectionTranslation) Insert(exec boil.Executor, columns boil.Columns) error {
	if o == nil {
		return errors.New("model: no collection_translations provided for insertion")
	}

	var err error

	nzDefaults := queries.NonZeroDefaultSet(collectionTranslationColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	collectionTranslationInsertCacheMut.RLock()
	cache, cached := collectionTranslationInsertCache[key]
	collectionTranslationInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			collectionTranslationAllColumns,
			collectionTranslationColumnsWithDefault,
			collectionTranslationColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(collectionTranslationType, collectionTranslationMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(collectionTranslationType, collectionTranslationMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"collection_translations\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"collection_translations\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "model: unable to insert into collection_translations")
	}

	if !cached {
		collectionTranslationInsertCacheMut.Lock()
		collectionTranslationInsertCache[key] = cache
		collectionTranslationInsertCacheMut.Unlock()
	}

	return nil
}

// Update uses an executor to update the CollectionTranslation.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *CollectionTranslation) Update(exec boil.Executor, columns boil.Columns) (int64, error) {
	var err error
	key := makeCacheKey(columns, nil)
	collectionTranslationUpdateCacheMut.RLock()
	cache, cached := collectionTranslationUpdateCache[key]
	collectionTranslationUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			collectionTranslationAllColumns,
			collectionTranslationPrimaryKeyColumns,
		)
		if len(wl) == 0 {
			return 0, errors.New("model: unable to update collection_translations, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"collection_translations\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, collectionTranslationPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(collectionTranslationType, collectionTranslationMapping, append(wl, collectionTranslationPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	var result sql.Result
	result, err = exec.Exec(cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to update collection_translations row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by update for collection_translations")
	}

	if !cached {
		collectionTranslationUpdateCacheMut.Lock()
		collectionTranslationUpdateCache[key] = cache
		collectionTranslationUpdateCacheMut.Unlock()
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values.
func (q collectionTranslationQuery) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to update all for collection_translations")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to retrieve rows affected for collection_translations")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o CollectionTranslationSlice) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("model: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), collectionTranslationPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"collection_translations\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, collectionTranslationPrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to update all in collectionTranslation slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to retrieve rows affected all in update all collectionTranslation")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *CollectionTranslation) Upsert(exec boil.Executor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("model: no collection_translations provided for upsert")
	}

	nzDefaults := queries.NonZeroDefaultSet(collectionTranslationColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	collectionTranslationUpsertCacheMut.RLock()
	cache, cached := collectionTranslationUpsertCache[key]
	collectionTranslationUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			collectionTranslationAllColumns,
			collectionTranslationColumnsWithDefault,
			collectionTranslationColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			collectionTranslationAllColumns,
			collectionTranslationPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("model: unable to upsert collection_translations, could not build update column list")
		}

		ret := strmangle.SetComplement(collectionTranslationAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(collectionTranslationPrimaryKeyColumns) == 0 {
				return errors.New("model: unable to upsert collection_translations, could not build conflict column list")
			}

			conflict = make([]string, len(collectionTranslationPrimaryKeyColumns))
			copy(conflict, collectionTranslationPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"collection_translations\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(collectionTranslationType, collectionTranslationMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(collectionTranslationType, collectionTranslationMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "model: unable to upsert collection_translations")
	}

	if !cached {
		collectionTranslationUpsertCacheMut.Lock()
		collectionTranslationUpsertCache[key] = cache
		collectionTranslationUpsertCacheMut.Unlock()
	}

	return nil
}

// Delete deletes a single CollectionTranslation record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *CollectionTranslation) Delete(exec boil.Executor) (int64, error) {
	if o == nil {
		return 0, errors.New("model: no CollectionTranslation provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), collectionTranslationPrimaryKeyMapping)
	sql := "DELETE FROM \"collection_translations\" WHERE \"id\"=$1"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to delete from collection_translations")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by delete for collection_translations")
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q collectionTranslationQuery) DeleteAll(exec boil.Executor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("model: no collectionTranslationQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to delete all from collection_translations")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by deleteall for collection_translations")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o CollectionTranslationSlice) DeleteAll(exec boil.Executor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), collectionTranslationPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"collection_translations\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, collectionTranslationPrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to delete all from collectionTranslation slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by deleteall for collection_translations")
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *CollectionTranslation) Reload(exec boil.Executor) error {
	ret, err := FindCollectionTranslation(exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *CollectionTranslationSlice) ReloadAll(exec boil.Executor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := CollectionTranslationSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), collectionTranslationPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"collection_translations\".* FROM \"collection_translations\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, collectionTranslationPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(nil, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "model: unable to reload all in CollectionTranslationSlice")
	}

	*o = slice

	return nil
}

// CollectionTranslationExists checks if the CollectionTranslation row exists.
func CollectionTranslationExists(exec boil.Executor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"collection_translations\" where \"id\"=$1 limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, iD)
	}
	row := exec.QueryRow(sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "model: unable to check if collection_translations exists")
	}

	return exists, nil
}

// Exists checks if the CollectionTranslation row exists.
func (o *CollectionTranslation) Exists(exec boil.Executor) (bool, error) {
	return CollectionTranslationExists(exec, o.ID)
}
