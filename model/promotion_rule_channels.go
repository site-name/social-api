// Code generated by SQLBoiler 4.17.1 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package model

import (
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// PromotionRuleChannel is an object representing the database table.
type PromotionRuleChannel struct {
	ID              string `boil:"id" json:"id" toml:"id" yaml:"id"`
	PromotionRuleID string `boil:"promotion_rule_id" json:"promotion_rule_id" toml:"promotion_rule_id" yaml:"promotion_rule_id"`
	ChannelID       string `boil:"channel_id" json:"channel_id" toml:"channel_id" yaml:"channel_id"`

	R *promotionRuleChannelR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L promotionRuleChannelL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var PromotionRuleChannelColumns = struct {
	ID              string
	PromotionRuleID string
	ChannelID       string
}{
	ID:              "id",
	PromotionRuleID: "promotion_rule_id",
	ChannelID:       "channel_id",
}

var PromotionRuleChannelTableColumns = struct {
	ID              string
	PromotionRuleID string
	ChannelID       string
}{
	ID:              "promotion_rule_channels.id",
	PromotionRuleID: "promotion_rule_channels.promotion_rule_id",
	ChannelID:       "promotion_rule_channels.channel_id",
}

// Generated where

var PromotionRuleChannelWhere = struct {
	ID              whereHelperstring
	PromotionRuleID whereHelperstring
	ChannelID       whereHelperstring
}{
	ID:              whereHelperstring{field: "\"promotion_rule_channels\".\"id\""},
	PromotionRuleID: whereHelperstring{field: "\"promotion_rule_channels\".\"promotion_rule_id\""},
	ChannelID:       whereHelperstring{field: "\"promotion_rule_channels\".\"channel_id\""},
}

// PromotionRuleChannelRels is where relationship names are stored.
var PromotionRuleChannelRels = struct {
	Channel       string
	PromotionRule string
}{
	Channel:       "Channel",
	PromotionRule: "PromotionRule",
}

// promotionRuleChannelR is where relationships are stored.
type promotionRuleChannelR struct {
	Channel       *Channel       `boil:"Channel" json:"Channel" toml:"Channel" yaml:"Channel"`
	PromotionRule *PromotionRule `boil:"PromotionRule" json:"PromotionRule" toml:"PromotionRule" yaml:"PromotionRule"`
}

// NewStruct creates a new relationship struct
func (*promotionRuleChannelR) NewStruct() *promotionRuleChannelR {
	return &promotionRuleChannelR{}
}

func (r *promotionRuleChannelR) GetChannel() *Channel {
	if r == nil {
		return nil
	}
	return r.Channel
}

func (r *promotionRuleChannelR) GetPromotionRule() *PromotionRule {
	if r == nil {
		return nil
	}
	return r.PromotionRule
}

// promotionRuleChannelL is where Load methods for each relationship are stored.
type promotionRuleChannelL struct{}

var (
	promotionRuleChannelAllColumns            = []string{"id", "promotion_rule_id", "channel_id"}
	promotionRuleChannelColumnsWithoutDefault = []string{"id", "promotion_rule_id", "channel_id"}
	promotionRuleChannelColumnsWithDefault    = []string{}
	promotionRuleChannelPrimaryKeyColumns     = []string{"id"}
	promotionRuleChannelGeneratedColumns      = []string{}
)

type (
	// PromotionRuleChannelSlice is an alias for a slice of pointers to PromotionRuleChannel.
	// This should almost always be used instead of []PromotionRuleChannel.
	PromotionRuleChannelSlice []*PromotionRuleChannel

	promotionRuleChannelQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	promotionRuleChannelType                 = reflect.TypeOf(&PromotionRuleChannel{})
	promotionRuleChannelMapping              = queries.MakeStructMapping(promotionRuleChannelType)
	promotionRuleChannelPrimaryKeyMapping, _ = queries.BindMapping(promotionRuleChannelType, promotionRuleChannelMapping, promotionRuleChannelPrimaryKeyColumns)
	promotionRuleChannelInsertCacheMut       sync.RWMutex
	promotionRuleChannelInsertCache          = make(map[string]insertCache)
	promotionRuleChannelUpdateCacheMut       sync.RWMutex
	promotionRuleChannelUpdateCache          = make(map[string]updateCache)
	promotionRuleChannelUpsertCacheMut       sync.RWMutex
	promotionRuleChannelUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// One returns a single promotionRuleChannel record from the query.
func (q promotionRuleChannelQuery) One(exec boil.Executor) (*PromotionRuleChannel, error) {
	o := &PromotionRuleChannel{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(nil, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "model: failed to execute a one query for promotion_rule_channels")
	}

	return o, nil
}

// All returns all PromotionRuleChannel records from the query.
func (q promotionRuleChannelQuery) All(exec boil.Executor) (PromotionRuleChannelSlice, error) {
	var o []*PromotionRuleChannel

	err := q.Bind(nil, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "model: failed to assign all query results to PromotionRuleChannel slice")
	}

	return o, nil
}

// Count returns the count of all PromotionRuleChannel records in the query.
func (q promotionRuleChannelQuery) Count(exec boil.Executor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to count promotion_rule_channels rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q promotionRuleChannelQuery) Exists(exec boil.Executor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "model: failed to check if promotion_rule_channels exists")
	}

	return count > 0, nil
}

// Channel pointed to by the foreign key.
func (o *PromotionRuleChannel) Channel(mods ...qm.QueryMod) channelQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.ChannelID),
	}

	queryMods = append(queryMods, mods...)

	return Channels(queryMods...)
}

// PromotionRule pointed to by the foreign key.
func (o *PromotionRuleChannel) PromotionRule(mods ...qm.QueryMod) promotionRuleQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.PromotionRuleID),
	}

	queryMods = append(queryMods, mods...)

	return PromotionRules(queryMods...)
}

// LoadChannel allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (promotionRuleChannelL) LoadChannel(e boil.Executor, singular bool, maybePromotionRuleChannel interface{}, mods queries.Applicator) error {
	var slice []*PromotionRuleChannel
	var object *PromotionRuleChannel

	if singular {
		var ok bool
		object, ok = maybePromotionRuleChannel.(*PromotionRuleChannel)
		if !ok {
			object = new(PromotionRuleChannel)
			ok = queries.SetFromEmbeddedStruct(&object, &maybePromotionRuleChannel)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybePromotionRuleChannel))
			}
		}
	} else {
		s, ok := maybePromotionRuleChannel.(*[]*PromotionRuleChannel)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybePromotionRuleChannel)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybePromotionRuleChannel))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &promotionRuleChannelR{}
		}
		args[object.ChannelID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &promotionRuleChannelR{}
			}

			args[obj.ChannelID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`channels`),
		qm.WhereIn(`channels.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.Query(e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Channel")
	}

	var resultSlice []*Channel
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Channel")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for channels")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for channels")
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Channel = foreign
		if foreign.R == nil {
			foreign.R = &channelR{}
		}
		foreign.R.PromotionRuleChannels = append(foreign.R.PromotionRuleChannels, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.ChannelID == foreign.ID {
				local.R.Channel = foreign
				if foreign.R == nil {
					foreign.R = &channelR{}
				}
				foreign.R.PromotionRuleChannels = append(foreign.R.PromotionRuleChannels, local)
				break
			}
		}
	}

	return nil
}

// LoadPromotionRule allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (promotionRuleChannelL) LoadPromotionRule(e boil.Executor, singular bool, maybePromotionRuleChannel interface{}, mods queries.Applicator) error {
	var slice []*PromotionRuleChannel
	var object *PromotionRuleChannel

	if singular {
		var ok bool
		object, ok = maybePromotionRuleChannel.(*PromotionRuleChannel)
		if !ok {
			object = new(PromotionRuleChannel)
			ok = queries.SetFromEmbeddedStruct(&object, &maybePromotionRuleChannel)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybePromotionRuleChannel))
			}
		}
	} else {
		s, ok := maybePromotionRuleChannel.(*[]*PromotionRuleChannel)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybePromotionRuleChannel)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybePromotionRuleChannel))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &promotionRuleChannelR{}
		}
		args[object.PromotionRuleID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &promotionRuleChannelR{}
			}

			args[obj.PromotionRuleID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`promotion_rules`),
		qm.WhereIn(`promotion_rules.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.Query(e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load PromotionRule")
	}

	var resultSlice []*PromotionRule
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice PromotionRule")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for promotion_rules")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for promotion_rules")
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.PromotionRule = foreign
		if foreign.R == nil {
			foreign.R = &promotionRuleR{}
		}
		foreign.R.PromotionRuleChannels = append(foreign.R.PromotionRuleChannels, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.PromotionRuleID == foreign.ID {
				local.R.PromotionRule = foreign
				if foreign.R == nil {
					foreign.R = &promotionRuleR{}
				}
				foreign.R.PromotionRuleChannels = append(foreign.R.PromotionRuleChannels, local)
				break
			}
		}
	}

	return nil
}

// SetChannel of the promotionRuleChannel to the related item.
// Sets o.R.Channel to related.
// Adds o to related.R.PromotionRuleChannels.
func (o *PromotionRuleChannel) SetChannel(exec boil.Executor, insert bool, related *Channel) error {
	var err error
	if insert {
		if err = related.Insert(exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"promotion_rule_channels\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"channel_id"}),
		strmangle.WhereClause("\"", "\"", 2, promotionRuleChannelPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	if _, err = exec.Exec(updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.ChannelID = related.ID
	if o.R == nil {
		o.R = &promotionRuleChannelR{
			Channel: related,
		}
	} else {
		o.R.Channel = related
	}

	if related.R == nil {
		related.R = &channelR{
			PromotionRuleChannels: PromotionRuleChannelSlice{o},
		}
	} else {
		related.R.PromotionRuleChannels = append(related.R.PromotionRuleChannels, o)
	}

	return nil
}

// SetPromotionRule of the promotionRuleChannel to the related item.
// Sets o.R.PromotionRule to related.
// Adds o to related.R.PromotionRuleChannels.
func (o *PromotionRuleChannel) SetPromotionRule(exec boil.Executor, insert bool, related *PromotionRule) error {
	var err error
	if insert {
		if err = related.Insert(exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"promotion_rule_channels\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"promotion_rule_id"}),
		strmangle.WhereClause("\"", "\"", 2, promotionRuleChannelPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	if _, err = exec.Exec(updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.PromotionRuleID = related.ID
	if o.R == nil {
		o.R = &promotionRuleChannelR{
			PromotionRule: related,
		}
	} else {
		o.R.PromotionRule = related
	}

	if related.R == nil {
		related.R = &promotionRuleR{
			PromotionRuleChannels: PromotionRuleChannelSlice{o},
		}
	} else {
		related.R.PromotionRuleChannels = append(related.R.PromotionRuleChannels, o)
	}

	return nil
}

// PromotionRuleChannels retrieves all the records using an executor.
func PromotionRuleChannels(mods ...qm.QueryMod) promotionRuleChannelQuery {
	mods = append(mods, qm.From("\"promotion_rule_channels\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"promotion_rule_channels\".*"})
	}

	return promotionRuleChannelQuery{q}
}

// FindPromotionRuleChannel retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindPromotionRuleChannel(exec boil.Executor, iD string, selectCols ...string) (*PromotionRuleChannel, error) {
	promotionRuleChannelObj := &PromotionRuleChannel{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"promotion_rule_channels\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(nil, exec, promotionRuleChannelObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "model: unable to select from promotion_rule_channels")
	}

	return promotionRuleChannelObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *PromotionRuleChannel) Insert(exec boil.Executor, columns boil.Columns) error {
	if o == nil {
		return errors.New("model: no promotion_rule_channels provided for insertion")
	}

	var err error

	nzDefaults := queries.NonZeroDefaultSet(promotionRuleChannelColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	promotionRuleChannelInsertCacheMut.RLock()
	cache, cached := promotionRuleChannelInsertCache[key]
	promotionRuleChannelInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			promotionRuleChannelAllColumns,
			promotionRuleChannelColumnsWithDefault,
			promotionRuleChannelColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(promotionRuleChannelType, promotionRuleChannelMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(promotionRuleChannelType, promotionRuleChannelMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"promotion_rule_channels\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"promotion_rule_channels\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "model: unable to insert into promotion_rule_channels")
	}

	if !cached {
		promotionRuleChannelInsertCacheMut.Lock()
		promotionRuleChannelInsertCache[key] = cache
		promotionRuleChannelInsertCacheMut.Unlock()
	}

	return nil
}

// Update uses an executor to update the PromotionRuleChannel.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *PromotionRuleChannel) Update(exec boil.Executor, columns boil.Columns) (int64, error) {
	var err error
	key := makeCacheKey(columns, nil)
	promotionRuleChannelUpdateCacheMut.RLock()
	cache, cached := promotionRuleChannelUpdateCache[key]
	promotionRuleChannelUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			promotionRuleChannelAllColumns,
			promotionRuleChannelPrimaryKeyColumns,
		)
		if len(wl) == 0 {
			return 0, errors.New("model: unable to update promotion_rule_channels, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"promotion_rule_channels\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, promotionRuleChannelPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(promotionRuleChannelType, promotionRuleChannelMapping, append(wl, promotionRuleChannelPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	var result sql.Result
	result, err = exec.Exec(cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to update promotion_rule_channels row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by update for promotion_rule_channels")
	}

	if !cached {
		promotionRuleChannelUpdateCacheMut.Lock()
		promotionRuleChannelUpdateCache[key] = cache
		promotionRuleChannelUpdateCacheMut.Unlock()
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values.
func (q promotionRuleChannelQuery) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to update all for promotion_rule_channels")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to retrieve rows affected for promotion_rule_channels")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o PromotionRuleChannelSlice) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("model: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), promotionRuleChannelPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"promotion_rule_channels\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, promotionRuleChannelPrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to update all in promotionRuleChannel slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to retrieve rows affected all in update all promotionRuleChannel")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *PromotionRuleChannel) Upsert(exec boil.Executor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("model: no promotion_rule_channels provided for upsert")
	}

	nzDefaults := queries.NonZeroDefaultSet(promotionRuleChannelColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	promotionRuleChannelUpsertCacheMut.RLock()
	cache, cached := promotionRuleChannelUpsertCache[key]
	promotionRuleChannelUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			promotionRuleChannelAllColumns,
			promotionRuleChannelColumnsWithDefault,
			promotionRuleChannelColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			promotionRuleChannelAllColumns,
			promotionRuleChannelPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("model: unable to upsert promotion_rule_channels, could not build update column list")
		}

		ret := strmangle.SetComplement(promotionRuleChannelAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(promotionRuleChannelPrimaryKeyColumns) == 0 {
				return errors.New("model: unable to upsert promotion_rule_channels, could not build conflict column list")
			}

			conflict = make([]string, len(promotionRuleChannelPrimaryKeyColumns))
			copy(conflict, promotionRuleChannelPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"promotion_rule_channels\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(promotionRuleChannelType, promotionRuleChannelMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(promotionRuleChannelType, promotionRuleChannelMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "model: unable to upsert promotion_rule_channels")
	}

	if !cached {
		promotionRuleChannelUpsertCacheMut.Lock()
		promotionRuleChannelUpsertCache[key] = cache
		promotionRuleChannelUpsertCacheMut.Unlock()
	}

	return nil
}

// Delete deletes a single PromotionRuleChannel record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *PromotionRuleChannel) Delete(exec boil.Executor) (int64, error) {
	if o == nil {
		return 0, errors.New("model: no PromotionRuleChannel provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), promotionRuleChannelPrimaryKeyMapping)
	sql := "DELETE FROM \"promotion_rule_channels\" WHERE \"id\"=$1"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to delete from promotion_rule_channels")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by delete for promotion_rule_channels")
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q promotionRuleChannelQuery) DeleteAll(exec boil.Executor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("model: no promotionRuleChannelQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to delete all from promotion_rule_channels")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by deleteall for promotion_rule_channels")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o PromotionRuleChannelSlice) DeleteAll(exec boil.Executor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), promotionRuleChannelPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"promotion_rule_channels\" WHERE " +
		strmangle.WhereInClause(string(dialect.LQ), string(dialect.RQ), 1, promotionRuleChannelPrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to delete all from promotionRuleChannel slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by deleteall for promotion_rule_channels")
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *PromotionRuleChannel) Reload(exec boil.Executor) error {
	ret, err := FindPromotionRuleChannel(exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *PromotionRuleChannelSlice) ReloadAll(exec boil.Executor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := PromotionRuleChannelSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), promotionRuleChannelPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"promotion_rule_channels\".* FROM \"promotion_rule_channels\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, promotionRuleChannelPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(nil, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "model: unable to reload all in PromotionRuleChannelSlice")
	}

	*o = slice

	return nil
}

// PromotionRuleChannelExists checks if the PromotionRuleChannel row exists.
func PromotionRuleChannelExists(exec boil.Executor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"promotion_rule_channels\" where \"id\"=$1 limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, iD)
	}
	row := exec.QueryRow(sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "model: unable to check if promotion_rule_channels exists")
	}

	return exists, nil
}

// Exists checks if the PromotionRuleChannel row exists.
func (o *PromotionRuleChannel) Exists(exec boil.Executor) (bool, error) {
	return PromotionRuleChannelExists(exec, o.ID)
}
