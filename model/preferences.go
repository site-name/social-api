// Code generated by SQLBoiler 4.16.2 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package model

import (
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// Preference is an object representing the database table.
type Preference struct {
	UserID   string `boil:"user_id" json:"user_id" toml:"user_id" yaml:"user_id"`
	Category string `boil:"category" json:"category" toml:"category" yaml:"category"`
	Name     string `boil:"name" json:"name" toml:"name" yaml:"name"`
	Value    string `boil:"value" json:"value" toml:"value" yaml:"value"`

	R *preferenceR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L preferenceL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var PreferenceColumns = struct {
	UserID   string
	Category string
	Name     string
	Value    string
}{
	UserID:   "user_id",
	Category: "category",
	Name:     "name",
	Value:    "value",
}

var PreferenceTableColumns = struct {
	UserID   string
	Category string
	Name     string
	Value    string
}{
	UserID:   "preferences.user_id",
	Category: "preferences.category",
	Name:     "preferences.name",
	Value:    "preferences.value",
}

// Generated where

var PreferenceWhere = struct {
	UserID   whereHelperstring
	Category whereHelperstring
	Name     whereHelperstring
	Value    whereHelperstring
}{
	UserID:   whereHelperstring{field: "\"preferences\".\"user_id\""},
	Category: whereHelperstring{field: "\"preferences\".\"category\""},
	Name:     whereHelperstring{field: "\"preferences\".\"name\""},
	Value:    whereHelperstring{field: "\"preferences\".\"value\""},
}

// PreferenceRels is where relationship names are stored.
var PreferenceRels = struct {
}{}

// preferenceR is where relationships are stored.
type preferenceR struct {
}

// NewStruct creates a new relationship struct
func (*preferenceR) NewStruct() *preferenceR {
	return &preferenceR{}
}

// preferenceL is where Load methods for each relationship are stored.
type preferenceL struct{}

var (
	preferenceAllColumns            = []string{"user_id", "category", "name", "value"}
	preferenceColumnsWithoutDefault = []string{"category", "name", "value"}
	preferenceColumnsWithDefault    = []string{"user_id"}
	preferencePrimaryKeyColumns     = []string{"user_id"}
	preferenceGeneratedColumns      = []string{}
)

type (
	// PreferenceSlice is an alias for a slice of pointers to Preference.
	// This should almost always be used instead of []Preference.
	PreferenceSlice []*Preference

	preferenceQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	preferenceType                 = reflect.TypeOf(&Preference{})
	preferenceMapping              = queries.MakeStructMapping(preferenceType)
	preferencePrimaryKeyMapping, _ = queries.BindMapping(preferenceType, preferenceMapping, preferencePrimaryKeyColumns)
	preferenceInsertCacheMut       sync.RWMutex
	preferenceInsertCache          = make(map[string]insertCache)
	preferenceUpdateCacheMut       sync.RWMutex
	preferenceUpdateCache          = make(map[string]updateCache)
	preferenceUpsertCacheMut       sync.RWMutex
	preferenceUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// One returns a single preference record from the query.
func (q preferenceQuery) One(exec boil.Executor) (*Preference, error) {
	o := &Preference{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(nil, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "model: failed to execute a one query for preferences")
	}

	return o, nil
}

// All returns all Preference records from the query.
func (q preferenceQuery) All(exec boil.Executor) (PreferenceSlice, error) {
	var o []*Preference

	err := q.Bind(nil, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "model: failed to assign all query results to Preference slice")
	}

	return o, nil
}

// Count returns the count of all Preference records in the query.
func (q preferenceQuery) Count(exec boil.Executor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to count preferences rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q preferenceQuery) Exists(exec boil.Executor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "model: failed to check if preferences exists")
	}

	return count > 0, nil
}

// Preferences retrieves all the records using an executor.
func Preferences(mods ...qm.QueryMod) preferenceQuery {
	mods = append(mods, qm.From("\"preferences\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"preferences\".*"})
	}

	return preferenceQuery{q}
}

// FindPreference retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindPreference(exec boil.Executor, userID string, selectCols ...string) (*Preference, error) {
	preferenceObj := &Preference{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"preferences\" where \"user_id\"=$1", sel,
	)

	q := queries.Raw(query, userID)

	err := q.Bind(nil, exec, preferenceObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "model: unable to select from preferences")
	}

	return preferenceObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *Preference) Insert(exec boil.Executor, columns boil.Columns) error {
	if o == nil {
		return errors.New("model: no preferences provided for insertion")
	}

	var err error

	nzDefaults := queries.NonZeroDefaultSet(preferenceColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	preferenceInsertCacheMut.RLock()
	cache, cached := preferenceInsertCache[key]
	preferenceInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			preferenceAllColumns,
			preferenceColumnsWithDefault,
			preferenceColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(preferenceType, preferenceMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(preferenceType, preferenceMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"preferences\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"preferences\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "model: unable to insert into preferences")
	}

	if !cached {
		preferenceInsertCacheMut.Lock()
		preferenceInsertCache[key] = cache
		preferenceInsertCacheMut.Unlock()
	}

	return nil
}

// Update uses an executor to update the Preference.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *Preference) Update(exec boil.Executor, columns boil.Columns) (int64, error) {
	var err error
	key := makeCacheKey(columns, nil)
	preferenceUpdateCacheMut.RLock()
	cache, cached := preferenceUpdateCache[key]
	preferenceUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			preferenceAllColumns,
			preferencePrimaryKeyColumns,
		)
		if len(wl) == 0 {
			return 0, errors.New("model: unable to update preferences, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"preferences\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, preferencePrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(preferenceType, preferenceMapping, append(wl, preferencePrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	var result sql.Result
	result, err = exec.Exec(cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to update preferences row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by update for preferences")
	}

	if !cached {
		preferenceUpdateCacheMut.Lock()
		preferenceUpdateCache[key] = cache
		preferenceUpdateCacheMut.Unlock()
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values.
func (q preferenceQuery) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to update all for preferences")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to retrieve rows affected for preferences")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o PreferenceSlice) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("model: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), preferencePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"preferences\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, preferencePrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to update all in preference slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to retrieve rows affected all in update all preference")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *Preference) Upsert(exec boil.Executor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("model: no preferences provided for upsert")
	}

	nzDefaults := queries.NonZeroDefaultSet(preferenceColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	preferenceUpsertCacheMut.RLock()
	cache, cached := preferenceUpsertCache[key]
	preferenceUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			preferenceAllColumns,
			preferenceColumnsWithDefault,
			preferenceColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			preferenceAllColumns,
			preferencePrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("model: unable to upsert preferences, could not build update column list")
		}

		ret := strmangle.SetComplement(preferenceAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(preferencePrimaryKeyColumns) == 0 {
				return errors.New("model: unable to upsert preferences, could not build conflict column list")
			}

			conflict = make([]string, len(preferencePrimaryKeyColumns))
			copy(conflict, preferencePrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"preferences\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(preferenceType, preferenceMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(preferenceType, preferenceMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "model: unable to upsert preferences")
	}

	if !cached {
		preferenceUpsertCacheMut.Lock()
		preferenceUpsertCache[key] = cache
		preferenceUpsertCacheMut.Unlock()
	}

	return nil
}

// Delete deletes a single Preference record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *Preference) Delete(exec boil.Executor) (int64, error) {
	if o == nil {
		return 0, errors.New("model: no Preference provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), preferencePrimaryKeyMapping)
	sql := "DELETE FROM \"preferences\" WHERE \"user_id\"=$1"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to delete from preferences")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by delete for preferences")
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q preferenceQuery) DeleteAll(exec boil.Executor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("model: no preferenceQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to delete all from preferences")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by deleteall for preferences")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o PreferenceSlice) DeleteAll(exec boil.Executor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), preferencePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"preferences\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, preferencePrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "model: unable to delete all from preference slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to get rows affected by deleteall for preferences")
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *Preference) Reload(exec boil.Executor) error {
	ret, err := FindPreference(exec, o.UserID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *PreferenceSlice) ReloadAll(exec boil.Executor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := PreferenceSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), preferencePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"preferences\".* FROM \"preferences\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, preferencePrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(nil, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "model: unable to reload all in PreferenceSlice")
	}

	*o = slice

	return nil
}

// PreferenceExists checks if the Preference row exists.
func PreferenceExists(exec boil.Executor, userID string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"preferences\" where \"user_id\"=$1 limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, userID)
	}
	row := exec.QueryRow(sql, userID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "model: unable to check if preferences exists")
	}

	return exists, nil
}

// Exists checks if the Preference row exists.
func (o *Preference) Exists(exec boil.Executor) (bool, error) {
	return PreferenceExists(exec, o.UserID)
}
